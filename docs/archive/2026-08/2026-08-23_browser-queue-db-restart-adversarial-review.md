# Browser 队列积压 + MySQL 重启对抗式审查（2026-08-23）

1. 是否错误复用了重启前连接？没有；恢复阶段使用重启后重新发现的 host port 和新 pool。
2. 是否把第一次失败隐藏？没有；第一次是旧动态端口导致的 Harness 输入失效，已作为测试拓扑发现记录，第二次才作为有效结果。
3. 是否漏领/重复/残留？120/120 claim，0 重复，0 残留。
4. 是否有付款或未知结果重试？没有，付款动作为 0。
5. 是否已证明生产级高可用？没有；主从/故障转移、长 soak 和真实 Browser 仍未验证。

结论：队列积压 + 单容器重启恢复窄子闸门通过，阶段 1 总闸门继续开放。
