# AI充值业务 MVP 验收与证据边界（2026-08-19）

## 已确认事实

### 2026-08-18 独立真实 Plus PoC

- 原始证据：生产机 `/opt/pojia/private/provider-poc-state.json`，权限 `0600`；敏感卡资料、Session、`card_key` 不进入本文。
- 参数：HNSKJ 卡型 ID `1`（`Z-43612081`），开卡 `$16`，最低卡余额 `$16`，`planType=plus`。
- 卡：Provider ID `493`，尾号 `8590`。
- ZZSHU 最终状态：`success`；实际支付 `982.14 PHP`；`is_subscription_cancelled=1`。
- HNSKJ 交易：`$16` 入金成功；`$15.97`（原币 `982.14 PHP`）支付成功并结算。
- 2026-08-19 只读复核时卡余额 `$0.02`。
- 2026-08-19 13:43:31 另有一笔 `OPENAI *CHATGPT SUBSCR`、`$78.24` 的失败支付尝试，没有实际扣款。用户已确认这是主动点击升级套餐后产生的失败支付，不是系统自动续费；原因归属来自用户确认。

结论：双 Provider 的真实开卡、Plus 创建、最终成功和取消标记已经验证；该 PoC 独立于正式订单数据库，不能替代正式订单状态机验收。

### 2026-08-19 正式订单付款前闭环

- 正式订单：`PJV1-m2wZr2ETzmiI8fTGfAT9`。
- 仅调用一次 HNSKJ 开卡，绑定 Provider 卡 `612`，状态 `active`，余额 `$16`，尾号 `1666`。
- 开卡任务执行 1 次；卡片就绪轮询 6 次。
- `PREPARE_RECHARGE` 完成，审计事件为 `submitted=false`。
- `SUBMIT_RECHARGE` 保持 `PENDING / attempts=0`；没有 ZZSHU `create_direct` 调用。
- 实测结束后 `accept_new_orders=false`、`dispatch_new_recharges=false`、三个 Provider 写开关均为 `false`。

结论：正式数据库、任务租约、开卡、缺失 ID 恢复基础、卡就绪和付款前请求构造已验证；没有重复花钱验证真实 Plus。

### 2026-08-19 自有后台运营入口

- 后台已提供开始/停止接单、人工补卡和单订单“确认充值/撤销放行”。
- 开始接单只让新订单进入自动付款前流程；真实直充仍必须逐单二次确认。
- 本次验收只检查按钮、确认窗、权限和隔离数据库语义；没有点击生产“确认充值”，没有新的 Provider 写请求。

### 2026-08-20 库存对账与付款前复核

- 发布：`/opt/pojia/releases/20260820-card-reconcile-1`，源码提交 `8154c12`；131 个发布文件的 SHA-256 清单已校验。
- 卡台只读全量对账：历史总卡数 16，active 5，未解析 0；本地可分配 3、已分配 1、已耗尽 1、核对中 0。
- Provider 卡 `493` 余额 `$0.02`，已标记为 `DEPLETED`；Provider 卡 `444` 余额 `$17`，已纳入可分配库存。
- 总览现在单独显示“待确认充值 1”，指标可跳转至对应订单列表；`CARD_READY` 不再误算为自动处理中。
- 真实后台订单详情已确认：Session 有效、Access Token 有效、卡片余额/凭据有效、卡片 15 分钟内已核对；服务端在签发 Permit 时会再次执行同样的复核。
- 未点击生产“确认充值”或“创建开卡任务”；新增开卡任务 0，有效充值 Permit 0，ZZSHU `create_direct` 历史调用仍为 0。

### 2026-08-20 CDK 批次恢复与管理

- 发布：`/opt/pojia/releases/20260820-cdk-batches-2`，运行代码提交 `37044a3`。
- CDK 生成增加唯一幂等键；隔离 MySQL 实测两个并发同键请求仅创建一个批次，另一个返回相同 CDK。
- 单个 CDK 仍只保存 SHA-256 哈希；批次恢复副本使用 AES-256-GCM 加密，测试确认数据库密文不包含 CDK 明文。
- 生产后台已显示批次总数、未使用、已兑换和已作废数；新版批次可重新下载，旧批次明确标记为“无明文”。
- 作废操作只修改 `AVAILABLE` CDK，不会修改已兑换订单；超过 10 个 CDK 要求额外确认。
- 所有已认证后台写接口已加入同源 `Origin` 校验和独立限流。
- 本次生产验收没有生成或作废任何 CDK；新批次表记录 0，开卡任务 0，有效充值 Permit 0，`create_direct` 历史调用 0。

### 2026-08-20 失败订单 CDK 补偿

- 发布：`/opt/pojia/releases/20260820-order-compensation-2`，运行代码提交 `9c7dde9`。
- 只有 `CREATED`、至少一个任务已 `DEAD`、没有活动任务、没有卡片且没有供应商调用的订单可人工补发；已进入开卡、充值或结果未知链路的订单均禁止补发。
- 补发会在同一事务中关闭原订单、创建一张同套餐 CDK、写入加密恢复副本和审计事件；重复请求返回同一张 CDK，不会重复生成。
- 补发后的原订单在客户查询页显示失败，不会继续显示排队中。
- 生产后台已只读验证：现有已进入开卡链路的订单显示“禁止补发”，页面不提供补发按钮。

### 2026-08-20 CDK 与后台安全加固

- 新 CDK 改用版本化 HMAC-SHA-256；迁移前 SHA-256 CDK 保持兼容。批次恢复改用独立于 Session 和哈希的密钥，最后一枚可用码兑换或作废后自动销毁恢复密文。
- 导入只接受 `PJ-` + 20 位高熵格式；生成不再自动下载或复制，明文离开页面或显示 10 分钟后清除。
- 管理会话增加数据库版本撤销；退出使全部旧会话失效。开卡、充值放行、取消/补发以及 CDK 资产操作增加绑定当前会话的 5 分钟密码复核。
- 后台只允许 `ops.vibebridge.top`；`plus`/`pay` 的后台路径以及 `ops` 的客户 API 均返回 404。外部验收确认 `ops` 未登录后台 API 返回 401。
- 生产切换前创建并校验加密备份；隔离 MySQL 从空库执行迁移 001–018 后 176/176 用例通过，候选 Caddy 配置由服务器真实 Caddy 校验通过。
- 用户确认旧可用 CDK 从未发给客户后，生产作废该旧码 1 枚并生成新 HMAC 码 1 枚；已兑换旧码 1 枚保持不变。新批次恢复、格式和数据库 HMAC 一致性已验证，明文未写入日志或验收输出。
- 切换后 Web/Worker 健康且无错误日志；卡台开卡写和直充写仍关闭，活动 Permit 0、活动开卡任务 0、`create_direct` 调用 0。
- 本次生产验收没有点击补发；补发记录保持 0，开卡/直充供应商调用计数部署前后不变，没有产生费用。

### 2026-08-20 未充值订单取消与卡片释放

- 发布：`/opt/pojia/releases/20260820-order-cancellation-1`，运行代码提交 `00b66d5`。
- 后台订单详情新增“取消未充值订单”；只有直充任务仍为 `PENDING / attempts=0`、没有 `create_direct` 调用或外部充值单号、卡片状态与余额合格且 15 分钟内已同步时显示执行按钮。
- 服务端在事务内锁定订单、卡片和提交任务，再次核验后关闭订单、停止等待任务、释放卡片回可用库存并写入时间线。
- 已提交、结果不明、卡片状态异常或数据过期时禁止取消；客户查询取消后的原订单显示失败。
- 生产页面已验证历史测试订单显示“可以安全取消”；经用户明确同意后，已通过同一后端取消服务关闭该订单并释放卡片。
- 执行前为可用卡 3、已分配卡 1、待确认订单 1；执行后为可用卡 4、已分配卡 0、待确认订单 0。取消任务与审计事件各 1 条，`create_direct` 调用始终为 0，没有充值或产生费用。

### 2026-08-20 卡片、订单与交易统一对账

- 发布：`/opt/pojia/releases/20260820-card-data-reconcile-1`，源码提交 `68de229`；147 个发布文件的 SHA-256 清单已校验。
- 后台卡片列表新增关联订单、交易数量、卡片/交易同步时间和对账状态；卡片详情可跳转关联订单，也可单卡或全库存发起只读同步。
- 独立只读队列与原有开卡任务完全分离；执行器显式关闭三个 Provider 写开关，只调用卡片详情和交易查询接口。失败任务安全重试，达到上限后停在 `REVIEW_REQUIRED`。
- 卡台全量对账结果：历史卡 16、active 5；本地卡 5、可分配 4、已耗尽 1、已分配 0。Provider 独有 active、本地缺失、状态冲突和未解析 active 均为 0。
- 生产 5 张卡的只读交易同步全部完成，卡 `444/612/616/617` 各 1 笔，卡 `493` 4 笔；所有卡均写入交易同步时间。无订单卡片也能独立保存交易，不会误建退款工单。
- 发布后供应商审计只有 `card_reconciliation_detail` 5 次和 `card_reconciliation_transactions` 5 次，均成功；活动开卡任务 0、有效充值 Permit 0，没有开卡或直充写调用。
- 切换时发现新发布的 `node_modules` 链接在 `current` 更新后自指，Web/Worker 首次启动失败；改为指向上一稳定发布的实际依赖目录后恢复。期间数据库迁移已成功，没有业务请求或 Provider 写调用。
- 生产切换前已创建并校验加密备份；隔离临时 MySQL 从空库执行迁移 001–019，180/180 项测试通过。

### 2026-08-20 订单、充值平台与卡片交易三方对账

- 发布：`/opt/pojia/releases/20260820-three-way-reconcile-1`，源码提交 `74f0519`；149 个发布文件的 SHA-256 清单已校验。
- 沿用现有 ZZSHU 订单状态轮询，不另建重复同步队列；新增统一证据判定层，比较本地订单状态、ZZSHU 外部订单号/确认金额和 HNSKJ 成功支付交易。
- 金额匹配优先使用卡片交易的原币金额与币种，容差 `$0.01`；同时要求交易类型为 `purchase`、状态为 `success`。`card_recharge`、失败授权和余额退回均不会被误认作成功支付。
- 后台总览新增“三方对账异常”，可直接筛选异常订单；订单列表显示对账摘要，详情显示判定依据、外部订单号、充值平台确认金额和卡片交易同步时间。
- 明确区分“等待证据”和“真实冲突”：未同步交易或尚未结算显示等待；成功订单缺支付金额、金额不匹配、失败订单出现成功扣款、充值创建后缺外部订单号才进入人工核对。
- 生产当前唯一订单为充值前人工取消，判定 `NOT_SUBMITTED / RECHARGE_NOT_SUBMITTED`；三方异常数 0，异常筛选结果 0。
- 生产验收时 ZZSHU `create_direct` 历史调用仍为 0，活动开卡任务 0、有效充值 Permit 0；本次功能没有调用任何 Provider 接口或产生费用。
- 发布前创建加密备份；隔离临时 MySQL 从空库执行迁移 001–019，并真实执行新总览、筛选和详情 SQL，185/185 项测试通过。

### 2026-08-20 后台可用性、卡段选择与 CDK 可读性

- 发布：`/opt/pojia/releases/20260820-admin-usability-1`，源码提交 `498f923`；发布文件 SHA-256 清单已校验。
- 登录页和退出能力原本已经存在；后台现在明确显示“管理员已登录 / 人工确认模式”，窄窗口下提供清晰的“安全退出”入口。
- 后台关键辅助文字、表格、状态标签和表单字号已提升到 11–13px；生产页实际计算字号复核通过。
- 人工补卡新增卡台实时卡段下拉框，当前读取到 7 个卡段；选项同时显示名称、卡段 ID 和 BIN，金额范围及费用按所选卡段实时重算。
- 前端提交卡段 ID，服务端仍以最新卡台快照验证卡段存在性、金额范围、余额和额度；伪造或失效 ID 会在任何付费调用前拒绝。开卡执行器仍保持单任务串行和逐卡幂等。
- 新生成 CDK 改为 `PJ-XXXXX-XXXXX-XXXXX-XXXXX` 分组格式，随机字符数与约 99 bit 熵不变；原 `PJ-` + 20 位无分隔格式继续可兑换。
- 生产切换前创建并验证加密备份；隔离临时 MySQL 从空库执行迁移 001–019，186/186 项测试通过。生产 Web、Worker、登录页和客户页健康，无 warning 日志。
- 本次验收只读取后台并切换页面，没有提交开卡表单、生成生产 CDK、放行充值或调用任何付费 Provider 操作。

### 2026-08-20 CDK 整批导出与开卡表单修复

- 发布：`/opt/pojia/releases/20260820-cdk-export-layout-1`，源码提交 `ec44e67`；发布文件 SHA-256 清单已校验。
- CDK 生成按钮改为“生成并导出整批 TXT”；一次生成 1–1000 个时，整个批次合并为一个 TXT 自动导出。当前结果和历史新版批次均提供明确的整批再次导出入口。
- 修正页面与实现不一致的说明：数据库保存版本化 HMAC，批次恢复副本独立加密，不再错误显示为普通 SHA-256。
- 开卡表单由三列改为两列；卡段下拉左对齐并收窄到 310px，规则卡独占整行，拆分显示卡段名称、ID/BIN 和允许金额。移动端改为单列。
- 后台金额统一显示两位小数，数据库 DECIMAL 精度不变。生产页面复核 `$35.71`、`$5.00–$200.00` 和卡片余额显示正常。
- 本地无数据库测试 171/187 通过，16 项明确标注因未配置 `TEST_DATABASE_URL` 而跳过；生产机隔离 MySQL 8.4 完整验证 187/187 通过。
- 本次生产验收没有生成 CDK、导出真实 CDK、提交开卡、放行充值或产生费用。

### 2026-08-20 CDK 全链路对抗式修复

- 发布：`/opt/pojia/releases/20260820-cdk-adversarial-1`，源码提交 `11ffa97`；271 个发布文件的 SHA-256 清单已重新生成并校验。
- 修复作废或生成已成功、但后续列表刷新失败时误报整个操作失败的问题；成功事实和刷新结果现分别反馈，未知结果要求先刷新核对而不是重复点击。
- 敏感操作密码复核从 5 分钟调整为 30 分钟，同一登录会话内复用并在提示中明确有效期；复核 Cookie 仍为 HttpOnly 且绑定当前管理员会话。
- 部分使用批次改称“导出原始整批”，导出前显示已兑换和已作废数量，并明确禁止把原始文件中的码重新发放。
- 作废批次改为大小写精确匹配，不存在批次明确拒绝；重复作废现有批次保持幂等并返回 0。
- 生产 MySQL 当前数据约 592 KiB、索引约 768 KiB、合计约 1.33 MiB；最新加密压缩备份 30,192 字节并通过完整性校验。
- 本地验证 171/187 通过，16 项只因未配置本地测试库跳过；服务器隔离 MySQL 8.4 从空库迁移 001–019 后 187/187 通过。
- 生产 Web、Worker、备份定时器、`/health/live` 和 `/health/ready` 均正常，无 warning 日志。CDK 仍为 4 条、可恢复批次 2 条、CDK 审计事件 4 条，证明本轮未生成、导出或作废生产 CDK；活动开卡任务、充值提交任务和 Permit 均为 0。
- 退款人工确认和余额提取按低频运营决策继续延期；当前只保留疑似退款识别与内部提醒。

### 2026-08-20 全局联动与故障恢复审查

- 发布：`/opt/pojia/releases/20260820-global-integration-1`，源码提交 `0117fde`；232 个 Git 跟踪发布文件的 SHA-256 清单已校验。
- 修复直充提交的两个关键中断窗口：Permit 消费、订单 `SUBMITTING` 和 Provider `STARTED` 意图现在原子落库；外部已成功但本地提交失败时必须进入 `SUBMIT_UNKNOWN`，不再误标为明确失败。
- 已分配卡新增每小时自动交易/退款观察，充值成功后立即追加一次只读同步；不自动确认退款或提取余额。
- Worker 每 15 秒写入心跳，后台总览显示“订单 Worker”；过期租约和超过 2 分钟的直充 `STARTED` 调用可见，后者自动进入对账异常。
- 修正卡交易同步与库存执行器的 systemd 依赖：生产数据库实际由 Docker 运行，两者不再依赖不存在的 `mysql.service`。
- 修复内存限流表可无界增长的 DoS 风险；生产依赖审计仍为 0 个已知漏洞。
- 服务器隔离 MySQL 8.4.11 从空库执行迁移 001–020，195/195 项通过；本地 177/195 通过，18 项只因未配置本地 MySQL 明确跳过。
- 发布前新建加密备份 `pojia-20260820T103038Z.sql.gz.enc`，并在无网络临时 MySQL 容器中真实恢复 19 张表。同机备份仍不等于异地灾备。
- 生产验收：Web、Worker、三个卡相关定时器和备份定时器均 active；后台 Worker 心跳正常；过期任务、滞留调用、活动 Permit、活动开卡任务和 ZZSHU `create_direct` 均为 0。
- 本次只调用两家供应商只读接口；未生成/作废 CDK，未开卡，未放行或提交充值，未产生费用。

## 安全执行规则

1. 直充任务没有针对该订单的有效 `rechargePermit` 时不可被 Worker 领取。
2. Permit 只允许 `CARD_READY + PENDING + attempts=0 + 从未调用 create_direct` 的订单。
3. 同一时间最多一个有效 Permit；有效期 1–30 分钟。
4. Worker 在外部请求前原子消费 Permit；消费后无论成功、明确拒绝或结果未知，都禁止自动第二次创建。
5. 正式 Worker 的卡台开卡权限恒为关闭；直充调用能力可用，但任务领取 SQL 和提交 handler 都必须校验指定订单的有效 Permit。
6. 后台“确认充值”只签发一个 10 分钟 Permit；可在未消费前撤销，消费后禁止重复放行。

## 明确未完成

- 没有在正式订单 `PJV1-m2wZr2ETzmiI8fTGfAT9` 上执行真实 Plus；这是有意的零成本边界。
- 真实退款样本、大批量并发和 Telegram 仍属于后续阶段。卡池、人工补卡和大于 10 张的额外确认已实现。
- 取消标记后的 `$78.24` 失败授权已由用户确认为手动升级操作；后续仍通过交易记录只读观察实际扣款，不把用户说明误写成供应商接口直接返回的字段。

## 生产固化

- 当前发布在每次生产验收后更新；发布目录使用不可变名称，`/opt/pojia/current` 原子指向当前版本。
- 每次保留上一个不可变发布作为回滚基线；迁移 001–019 均为向后兼容的新增表/列/索引。
- 发布清单：生产发布根目录 `RELEASE_MANIFEST.sha256`，包含关键运行文件的 SHA-256。
- 最新本地无数据库验证：171/187 通过，16 项 MySQL 用例明确因未配置测试库而跳过；生产机隔离临时 MySQL 8.4 完整验证：187/187 通过。
- 生产健康检查：Web、Worker、`/health/live`、`/health/ready` 均通过。
- 生产后台数据确认：付款前检查“已就绪”，直充“已锁定”，任务 `PENDING / attempts=0`，ZZSHU 创建调用 0 次。
