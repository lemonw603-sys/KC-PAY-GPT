# Agent 工作机制自查报告｜2026-09-04

## 结论

本项目已经存在明确的证据闸门和交接规则，但本轮回答没有严格执行。问题主要是执行纪律失效，不是项目缺少规则。

## 已核对的原始规则

- `CLAUDE.md` 已规定“回答事实前的强制三道闸门”：读取原始证据、读取业务规则、与事实源做冲突检查。
- 同文件已规定：生产事实必须核对当前 release、服务/进程、数据库或请求/日志；证据不足只能标为“尚未核实”。
- 同文件已规定：协作窗口结论必须读取原始回复/提交/报告后交叉核对。
- `AGENTS.md` 已要求重大结论回写事实源，并留下当前阶段、未验证事实和验证结果。

## 本轮发现的问题

1. 先引用历史 `PROJECT_MAP.md`/`CURRENT_STATE.md`，后做局部代码检查，顺序反了。
2. 把自动开卡/补余额的测试设计写成了当前生产行为。
3. 只执行了本地代码和 14 项单元测试，没有同步完成生产开关、库存、卡台余额和活动任务的实时核对，却给出了接近确定性的结论。
4. 没有在回答前明确标注“历史快照/代码已验证/生产未验证”。
5. 没有把“证据不足即停止”作为硬阻断。

## 修正措施

- 涉及当前状态的回答必须先完成三道闸门；未完成不得给确定结论。
- 每次报告固定分为：已验证事实、实际检查、尚未验证、结论边界。
- 生产状态只接受当前 release、服务/进程、数据库和日志证据；历史文档只能作索引。
- 方案与事实分开书写；测试方案不得表述为已完成能力。
- 多窗口结论必须读取原始报告、commit 和工作区后再汇报。

## 当前仍未确认

- 2026-09-04 此刻生产自动开卡/自动补余额开关及 runner 状态；
- 当前卡台余额与可分配卡；
- 下一笔真实订单将命中哪个供给分支。

本报告不改变生产配置，不创建订单，不执行 Provider 写入或付款。
