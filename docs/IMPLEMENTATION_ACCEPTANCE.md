# Foundation v2 实施与验收基线

## 实施任务

| ID | 任务 | 完成证据 |
| --- | --- | --- |
| F2-01 | 新增供应商账户、产品和不可变履约路线 | 迁移、仓储测试、历史回填报告 |
| F2-02 | 扩展卡片供应商账户唯一性和接入状态 | 并发导入与跨账户同ID测试 |
| F2-03 | 新增充值尝试资金栅栏 | 唯一约束、崩溃点和UNKNOWN测试 |
| F2-04 | 新增单笔/批量正式授权 | 冻结成员、一次消费、过期测试 |
| F2-05 | 解除充值授权对全局派发开关的依赖 | 回归测试和设置差异证明 |
| F2-06 | 新增卡接管批次、隔离和自动校验 | 50卡批量发现、去重、部分异常测试 |
| F2-07 | 新增分层同步和账户级请求预算 | 5,000卡调度、429/5xx退避测试 |
| F2-08 | 新增CDK发放、余额历史和异常对账 | 数据映射与导出测试 |
| F2-09 | 新增安全CSV导出 | 公式注入和敏感字段测试 |
| F2-10 | 新旧模型双写和证据化历史回填 | 可重跑迁移、差异为0或有明确清单 |
| F2-11 | 后台接入新能力 | 页面交互和权限验证 |
| F2-12 | 隔离MySQL全量验证 | 0 skipped、迁移重跑、故障注入 |
| F2-13 | 独立Agent对抗复核 | 发现、处置和剩余风险清单 |
| F2-14 | 生产备份、发布和只读验收 | 备份校验、服务状态、零付费调用 |

## 强制验收门槛

1. 同一供应商账户不能重复导入同一外部卡。
2. 不同供应商账户允许相同外部卡 ID。
3. 未通过隔离校验的卡永远不能被分配。
4. 一个订单最多存在一次资金风险活动尝试。
5. `SUBMIT_UNKNOWN` 后API、Browser和其他供应商自动提交次数均为0。
6. 批量授权创建后成员数量和订单ID集合不可增加。
7. 在请求前、请求中、响应后、本地提交前杀死Worker，均不得自动重复充值。
8. 5,000张合成卡下，高优先级资金核对不被历史同步饿死。
9. 手动连续点击刷新只产生一个有效同步任务。
10. 账户级限流遵守 `Retry-After`，退避包含随机抖动。
11. CSV阻断公式注入，不包含PAN、CVV、Session、API Key和可恢复CDK。
12. 历史迁移任意阶段中断后可安全重跑。
13. 新逻辑可通过关闭新Worker/开关回到兼容路径，不依赖删列回滚。
14. 重复充值、重复分配或无法解释的余额差异会自动停止新的资金提交。
15. 生产发布验收期间真实开卡和充值写调用均为0。

## 承诺矩阵规则

每次阶段报告必须把任务标记为：`完成`、`部分完成`、`未完成`或`延期`。测试通过只能证明对应断言，不自动证明真实API、生产链路或小批量运营完成。

