# 后台修复后对抗式复核（2026-08-22）

## 结论

本轮修复没有发现已证实的资金越权或生产写入问题；但修复仍不能视为公网验收完成。当前最大剩余风险是公网静态资源与本地已验收代码版本漂移，以及部分后台设置尚无统一页面。

## 已复核事实

1. 总览新增字段由 Overview API 返回，前端有对应渲染路径：累计/成功订单、等待 Session、等待补卡、取消续费状态、自动补卡用量、对账案件、卡同步积压、CDK/退款分布、Provider 新鲜度。
2. “累计订单”不再跳转今日筛选。
3. Provider 快照超过 2 分钟时前端不显示为正常。
4. 库存摘要包含耗尽卡；上游目录总记录不再显示为“历史成功开卡数”。
5. 卡片接管验证/接管 API 有后台入口；接管接口仍受敏感后台保护。
6. 隔离 MySQL 迁移 028–036 已执行，v1 全量 400/400 通过。

## 尚不能宣称已解决

1. 公网后台是否已经使用本地最新资源：尚未部署/登录态核验，不能宣称解决。
2. Provider 路线切换是否具备完整余额、卡段、目录新鲜度和写权限门槛：当前代码只具备基础门槛，仍是明确缺口。
3. 独立设置页面：当前没有统一页面，库存阈值和自动补卡日上限分散在库存页，其他运行设置仍需后台合同进一步收敛。
4. 生产真实数据是否与新总览字段一致：没有生产只读 API 证据，不能宣称一致。
5. legacy 长时间 Browser 仿真测试：尚未形成独立长测通过报告，不纳入 v1 后台回归结论。

## 反向检查结果

- 未发现本轮修复打开 Provider 写开关、开卡、充值或退款路径。
- 未发现把卡台目录总记录继续作为历史成功开卡数展示。
- 未发现把过期 Provider 快照显示为健康。
- 未发现测试跳过被误报成通过；隔离数据库套件已实际运行。

## 后续顺序

1. 先在隔离环境进行后台静态资源构建/发布包验收。
2. 生产只读核对当前 release 与静态文件哈希，不做写操作。
3. 仅当本地、发布包、公网资源一致后，才讨论生产部署。
4. 部署后登录态逐页验收，再进行下一轮对抗审查。

## 生产只读资源核对（用户同意后执行）

2026-08-22 对公网静态资源进行 HTTP 只读请求并计算 SHA-256：

| 资源 | 公网大小/哈希 | 本地大小/哈希 | 结论 |
|---|---:|---:|---|
| `/admin` | 1661 / `70f9242a…16edb343` | 22659 / `668a6587…e8ebc5ae` | 不一致 |
| `admin.js` | 92636 / `51a6b7cb…65f5a384` | 120933 / `7b0de1b3…4b0c4b99` | 不一致 |
| `admin.css` | 24056 / `fc2cb326…898d4e03` | 24205 / `db22ba85…e4556f9a31` | 不一致 |

公网响应时间均为 `Fri, 21 Aug 2026 23:57:26 GMT`，且公网登录入口引用旧版 `admin.css?v=8`。因此可以事实确认：公网资源仍不是当前本地修复版本。

本次只读请求没有登录、没有写请求、没有部署。

## 发布候选包（未部署）

已按当前 Git 提交 `f821305c6081d5dfb7bcbb8f7043ba9180b50149` 在本地生成只读发布候选包：

`/Users/lemon/code/AI充值业务/artifacts/release-candidate-20260822-f821305/`

- 文件数量：414
- 发布清单：`manifest.sha256`
- 清单 SHA-256：`2f7b83c47f4fa0c85d1556077e680002902d87a499c3deb37a04df32e9b130ee`
- 当前后台三项资源已纳入清单；其哈希与当前本地工作树一致。
- 该目录仅为本地候选包，没有上传、没有切换生产软链接、没有执行迁移。

## 发布候选包大阶段验收

候选包提交 `f821305c6081d5dfb7bcbb8f7043ba9180b50149` 已完成：

- 隔离 MySQL v1 全量：401/401 通过，0 失败，0 跳过；
- `git diff --check` 通过；
- 候选包关键后台资源哈希与候选包清单一致；
- 敏感值扫描未发现真实凭据值（命中的只是环境变量名称/示例脚本）；
- 未上传、未部署、未执行生产迁移。

生产对照目前已经确认公网旧资源与候选包不一致；生产 release 目录和服务器端清单仍需通过已登录的服务器只读终端完成最后一项核对。

## 生产只读核对新增阻断（2026-08-22）

通过 SSH 只读核对到生产：

- 主机：`elegant-unicorn-1.localdomain`
- 当前 release：`/opt/pojia/releases/20260822-stage3-8a3134d`
- Web/Worker/Bark active；只读卡同步和目录同步 timer active；付费开卡 timer inactive。
- 生产当前 `admin.js`/`admin.css` 与公网旧资源一致，与本地修复候选包不一致；当前 index 也与候选包不一致。
- Web 监听 `127.0.0.1:3100`；`/health/live` 返回 `{"status":"ok"}`。
- `/health/ready` 在本次只读核验中超过 5 秒无响应；公网 `/health/ready` 超过 15 秒无响应。数据库进程列表显示多条长时间执行的 Overview 查询，均卡在 `card_discoveries` 最新记录相关子查询。

这不是生产写入，也没有重启服务。该证据阻止直接部署原候选包：需要先修复 Overview 查询性能并重新生成候选包。

### 新修复

- 将 `card_discoveries` 最新记录统计由相关 `ORDER BY ... LIMIT 1` 子查询改为 anti-join；
- 新增迁移 `037_card_discovery_latest_index.sql`，为最新记录查询增加复合索引；
- 隔离 MySQL 迁移 037 后 v1 全量测试仍为 `401/401 pass, 0 fail, 0 skipped`；
- 新候选包：`artifacts/release-candidate-20260822-0a9c574/`，416 文件，清单 SHA-256：`607675eb618eb1605b3e921e6ea3ae34ec031406ebf764c771a25bbd196aef81`。

### 新查询的隔离环境验证

在已应用迁移 037 的隔离 MySQL 上直接执行与后台总览相同的 anti-join 统计：

- 返回 `0` 条待处理记录；
- 查询耗时约 `5 ms`；
- `EXPLAIN` 显示 `newer` 使用 `idx_card_discoveries_review`，`cards` 使用唯一索引 `uq_cards_provider_account_external`，两处均为 `Not exists` 反连接路径。

这只能证明查询在隔离数据集上的计划和耗时正常，不能替代生产部署后的 `/health/ready` 验证。生产当前仍是旧 release，不能据此宣称阻断已解除。
