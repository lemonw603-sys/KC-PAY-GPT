# AI充值业务｜项目地图
> **最新覆盖：2026-09-18 08:3x UTC 第⑤步完成（release `20260918-step5-740bc1d`：推送白名单 + 日对账；**bark 第一次跟着发布换代码**；日对账 timer 已装并首跑）。当前执行顺序＝§4 的 V2 落实 8 步；下一块是第⑥步，任务书已写。见 HANDOFF_NOW。**
> 上一版 4334dc2 的「统一成功时点」行为原样保留：撤回提前交付，D-240 其他提速保留；早交付表述是被替代方案，不可再实施。

最后核对：2026-09-20（北京时间）。

## 1. 目标与原则
- 客户自助提交CDK和Session，系统开通目标套餐；运营不必逐单在场。
- 本轮范围为Plus。付款未知不重付、不换卡、不换执行器；CDK/订单/消费账本仍为一套。
- 用户最终决定：确认Plus、关闭自动续费并完成现有内部核对后，才显示成功；不向客户展示取消续费细节。提前成功方案已撤回，不再扩展它的恢复分支。
- API与Browser共用底座；路线/卡源冻结，变更只影响新单。资金及生产动作须当次确认。

## 2. 事实入口
- 接班先读CLAUDE.md → HANDOFF_NOW.md → 本文 → CURRENT_STATE.md。
- **生产release、进程、开关、库存唯一事实表：CURRENT_STATE.md**。本文不复制运行数值。
- 最新实现/测试与发布证据：incidents/unified-success/README.md；D-240保留改动说明：tasks/2026-09-16-D240-delivery.md。
- 规则与方向：DECISIONS.md最新条目优先；PRODUCT_SIMPLIFICATION_DISCUSSION末尾基线、V2_ARCHITECTURE与V2.0_EXECUTION依最新决策解释，不机械照旧日期计划执行。

## 3. 已完成
- 原有CDK/订单/卡/资金锁定底座；Browser Plus自动真实开通与自动取消续费已有生产样本；常驻执行器已投入使用。
- 09-16原失败单：已定位为Session接口一次403后终态退码；上游403触发原因未确定。
- 同码重提单在**旧版**经自动付款后核实收口成功；证据incidents/2026-09-16-x-tIs-success-evidence.tsv。
- 已发布且保留：核验去重复、有界只读重试、圆环真实子阶段及付款阶段2秒轮询；Plus交付/内部收尾解耦已撤销。
- D-240本地测试、隔离MySQL串行集成、生产SQL/版本/健康/心跳/公网资源验证已完成；这些不等于新版本真实充值验收。

## 4. 当前执行顺序：V2 落实 8 步（唯一）

方向工作（D-243 步①②③）已全部完成，方案定稿在 `V2.0_EXECUTION.md` §3.A。**落实按下面 8 步走，每块开专门窗口、做完放回主线验**；每块的证据登记在账本 §6。

| 步 | 内容 | 状态 |
|---|---|---|
| ① | C2 真单（highvcc 卡走 ZZSHU）+ API `pro5/pro20` 映射验证 | ✅ C2 已出结论（D-253：API 固定 hnskj）；`pro5/pro20` 映射验证**未做**，并入第⑦步 |
| ② | 面四② 数据源三件（highvcc 流水/钱包、开卡费、账本补记） | ✅ **完成**。T1 生产已验证；T3 已 apply（7 单，账本 CONSUMED 12→19）；**T2 代码在生产但尚无真实样本**，第③步开卡时验 |
| ③ | 面一 C1 + 面二①③（选择表 / 切换校验 / 水位调度 / 三线合一 / 965 条归档） | ✅ **完成**（release `20260918-step3-supply-ba28273`，迁移 053，归档 965，调度 timer + 总闸已开，hnskj 真开 5622；D-259~D-264）。**两个遗留已在第④步了结**：highvcc 自动开卡已首次生产实跑（4022，D-269）；rehearsal 报价段已跑通（D-270） |
| ④ | 面二⑨⑩ + 面三②③④（按需同步 / 待销清单 / 三张契约表） | ✅ **完成**（release `20260918-step4-251a441`，迁移 054；D-267/D-269/D-270）。四批 apply 已写（RETIRED 21 张、同步残留归档 307）；**rehearsal 报价段跑通**（补上 D-264 遗留）；highvcc 自动开卡首次生产实跑。三张契约表落 `docs/contracts/2026-09-18_*`。**无真单样本的**：分卡当场同步、API 付款不明、崩溃进补核、备用卡台真证据被 worker 自动调用（见 UNVERIFIED） |
| ⑤ | 面四①③（推送白名单，先坐实静音根因 / 日对账） | ✅ **完成**（release `20260918-step5-740bc1d`，08:29:00 UTC 切换，无迁移；D-271/D-272）。**静音根因坐实**＝`pojia-bark-notifications` 不在发布重启名单里、五天跑旧 release（D-220 修过 worker 的同一个洞、漏了它）；脚本已修，**bark 第一次跟着发布换到新 release**（PID 1202626 独立核实）。白名单四类 + 三个新产生点（token 失效 / 卡台故障 / **拒付**——以前一条都没推过）。日对账 timer 已装（下次 09-19 04:01 UTC）并首跑成功，心跳已写。**只读复验**：上线后新增通知行只来自白名单类型，白名单外 0 条。剩 2 条金额差异原因未知（账本发现 7）。**⑤b 收窄 + Codex 复审补修已发布**（release `20260918-step5b-b0a36d4`，2026-09-18 13:07 UTC 切换，D-275 / F-47~F-60）：金额降级无法核对、未知扣款分无主/待登记、连续两次只认正式批次 + 输入存疑不升级、日报按天 key、删空开关/猜测拒付类型、F-47 押金修复、手动用卡=override RETIRED |
| ⑥ | 面五② 工作台 + 设置页（UI 出 2~3 版给 Lemon 挑） | 🔧 **代码层面完成，未部署**（下方为过程留痕）（step6 `20260919-step6-8cd6d7e` 发布后当天回滚——工作台做偏设计图：营业条被改成五决定按钮组而非原型 toggle 开关、sidebar/顶栏未换候光皮、残留「开始营业/刷新」，Lemon 不满意）。**正对照原型 `docs/design/prototypes/step6-workbench-compare.html` 重做**：候光 sidebar（6 导航）+ 顶栏仅标题/全局搜索 + 营业条 toggle 开关 + 卡台 + 简洁 CDK；数字墙去「待接入」占位换真实指标、队列 48 告警折叠。生产暂回 ⑤b 旧后台。代码在 commit `8cd6d7e`（含可复用的卡与钱按台聚合/队列资金核对/测试）。**2026-09-19 新窗口已闭合「付款不明一条链」（提交 `88037c4`，未推未部署）**：F-61 工作台付款不明 case 改「去核实收口」跳订单详情做正式收口、F-62 字段名、F-63 失败态与空态分开，外加新发现的 **B1**（Browser 收口原本不关自己的 case/告警，只有 API 侧关 → 已补对称关闭）。Lemon 裁定：营业条走方向 A（3 toggle + 路线切换块留工作台）、sidebar 四页做完统一换皮、自动完成率先空着标待接入。**发布前必须补隔离库端到端那一跑**（集成测试本地无 DB 全 skip，见 UNVERIFIED_LEDGER）。**2026-09-19 续**：审查批次 2 三条清完（F-68 未定义常量致页面崩／F-70 复位 9 条历史处置／F-71 部分反证）；**D-285** 数字墙与队列按原型 C 恢复（后端没有的三项标「待接入」不顶替）→ **F-64 在本块闭合**；**营业条方向 A 落实**（路线切换接回工作台，修好 `state.rechargeMethod` 从未赋值导致 `VERSION_MATCH` 恒失败的「永远切不动」根因，业务层已证明切得动：库状态翻转 + 审计行）。**界面层验收这条路已打通并沉淀 `RUNBOOK §2.8`**。以上全部未部署、生产仍 ⑤b。**2026-09-19 再续（CDK）**：D-279 ② 码前缀按产品已落（PLUS-/5X-/20X-，旧 PJ- 同收，核查推翻任务书「三处同改」——客户侧根本不校验格式）；**D-286**（Lemon 加做）交付负债可见 + 码有效期/下线保护，迁移 055 只加列、隔离库验过；四个端点（单码列表/负债/单码作废/已发出标记）已接线并**端到端验通**（owed 1 / stock 1 真的分开）。**CDK 前端已完成**（单码列表／生成即复制／状态说人话／单码作废／负债条／全局搜索贴码定位，提交 `eef31d1`~`1f60b8c`，皮肤按 Lemon 定沿用旧皮）。**迁移 055 仍未应用生产。** **2026-09-20（卡片页本轮，D-287）**：D-280 **①③⑤⑥ 完成**——两台并列四个数（可分配复用 `providerCardStockSql()`，与工作台同口径）／在役列表＋历史折叠／待销清单（复用 `classifyRetirementRow`）／手动用卡登记（走 overrides，卡仍留待销）；新增按台钱包底线设置键与写端点（provider_accounts 白名单）；比稿 `step6-cards-compare.html` Lemon 挑 A「台账优先」，皮肤用候光（受控打破 D-284② 字面——外壳早在 `7c1a5c0` 就换了候光，裁定前提不成立）。三层验收齐（业务层隔离库实跑并独立复核、界面层四态含上游注入 500、工程层新增 13 条回归测试）。**仍未部署。** **2026-09-20 续（D-288/D-289）**：卡片页 **②④⑦⑧ 完成，D-280 八条全部落地**——② 开卡只展开既有折叠区（不另造花钱入口）+ 新增 `POST /backup-cards/highvcc/refresh`；④ 点尾号看流水，订单只认账本记了交易号的（生产 76 笔中仅 7 笔可关联）；⑦ 补余额区块连同工作台那条待办一起退休，卡台切换搬工作台（**先补齐「同时接管」三态与三条安全行为**，再把卡片页改只读）；⑧ 导入备用卡降为折叠高级入口。**highvcc 余额改全部按需查**（原先随页面加载打外网）。**同轮事故**：删代码用「下一个 async function」当边界，误删 14 个顶层事件绑定，**951 条测试全绿而侧边栏导航点不动**；已按语法边界精确恢复并加守门测试（详见 D-289）。**仍未部署。** **2026-09-20 收尾（D-290）**：**设置页完成**（B「一张大表」）——`card_supply_policies`（台×产品：水位/开卡金额/每日上限）与 `provider_accounts.wallet_floor`/`wallet_alert_threshold` **第一次有了写路径**（此前只有读，只能改库）；字段走白名单、值参数化、事务 + FOR UPDATE 读旧值、仅在值真变时写 `admin_setting_events`；改完先变色出「保存」，点了才写（不做失焦即存——改错直接影响资金）。**两处缺口已裁并落盘**：每卡单数仍是全局值（D-221 要的按产品本轮不做，**5X/20X 自动化接入前必须补**）；**5X 最低余额应为 $95**（Lemon 定，生产现值 16.00，**尚未改**，等设置页上生产后用页面改、改前单独确认）。**⑥ 代码层面全部完成，仍未上生产。** **下一块：⑦（Pro 同型 + 客户页四态）** |
| ⑦ | 面三⑤ Pro 同型（PoC → 改造 → 重开 305/306）+ 面五① 客户页四态 | 未开始 |
| ⑧ | 面五④ 删表 | 未开始 |

### 4.1 八步做完之后（2026-09-18 定，D-278）

**⑧ 收尾后的下一步只有一件：D-243 步⑤「集成验收」**，不自行开新块。验收 = 一单真实客户单从 CDK 兑换到交付走完整链路，Lemon 在场，按产品各一单（Plus 走 API、Plus 走 Browser、20X 走 Browser），每单对照 `docs/contracts/2026-09-18_delivery-criteria-contract.md` 逐项勾：分卡按需同步留痕、付款、目标套餐确认、取消续费确认、账本 CONSUMED、卡进/不进待销、客户页四态、该推的推了不该推的没推、日对账次日无新差异。三单都过 = V2 交付。

**验收过后进入运营期观察**，不改代码只看数：自动完成率（不靠人收口的比例）、日均单量、叫人次数与原因、拒付、钱包水位、两台开卡成功率。看两周。

**已立项、未排期的候选（谁先做由 Lemon 排，做前照旧写任务书、走做/审/裁）**：
- V2.2 Browser 迁云 Windows（D-225）：日均单量逼近本机 1 lane 上限时才做。
- 自动销卡（D-232 推 V3）：手动删卡成为负担时。
- 免费试用号 → 该单改走 API（D-265）：先落页面特征坐实、换卡走正式路径。

**2026-09-20 窗口登记的欠账（每条写明触发条件，别等想起来）**：

| # | 欠什么 | 什么时候必须做 | 出处 |
|---|---|---|---|
| 1 | 开卡任务按 `product_code` **自动回写卡的产品归属**（写 PRODUCT_ONLY） | 给 5X/20X 设水位 > 0、让调度器自动开它们之前 | D-296 |
| 2 | 人工导入闸门：充值金额命中 5X/20X 开卡金额（$100/$150）时**不许默认进 Plus 池**，须先指定产品 | 往后台放第一张 5X/20X 卡之前 | D-296 |
| 3 | **每卡单数按产品**（Plus 3 / 5X 1 / 20X 1），现在是一个全局值 | 5X/20X 自动化接入前**必须**补 | D-221 / D-290 |
| 4 | ~~5X 最低余额改 $95~~ **已完成**（2026-09-20 06:08 UTC，`set-minimum-card-balance.mjs --apply`，审计 16.00→95.00，新连接独立复核） | — | D-290 / D-301 |
| 5 | **今日花费聚合**（口径已定＝只算给客户充值消费掉的钱；数据齐备、`todayCst8WindowSql` 现成） | 做工作台数字墙那格时 | D-294 / D-295 |
| 6 | **数字墙布局重调**（哪些指标留、轻重怎么排） | 上生产真跑一天、有了真实数据之后 | D-294 |
| 7 | **CSS 收敛阶段 2~4**（cards.css 65 处字面色 → token；admin.css 旧页并入） | 跟着每次页面重做逐块吃，不单独排期 | D-293 |
| 8 | `design` 插件未安装（安装卡片点过但未生效） | 想用 design-critique / design-system 时重试 | D-293 |
| 9 | `pro5/pro20` 映射验证 | 并入第⑦步 | 8 步表 ① |

前两条是同一件事的两个入口：**兜底已经有了**（D-296 的「>$75 默认不给 Plus」不依赖任何人打标记），所以不急；但源头回写仍值得做，否则每次都靠那条阈值兜着。
- 数据库集成测试环境（D-258）：发布门槛真正过得了之前，发布靠单测 + 生产只读复验。
- 第③④⑤块「只报未改」的发现里 Lemon 未定的：scheduler 去重桶、后台「放弃并放卡」按钮、手动用卡影响资格的更细做法、`REVIEW_REQUIRED` 老 flag 清理。
- 第⑥块 STEP6_REVIEW **F-65**：后端 `setSupplyAutomation` 仍同时写 `card_balance_recharge`（补余额已弃 D-218），重做供给控件时一并核正确单键语义。
  > **2026-09-20 订正（D-287 查清）**：F-65 比原记录严重——前端侧**供给开关根本没有渲染入口**，`data-supply-toggle` 只有 click handler、全项目无任何渲染处（工作台重做后控件丢了）。全量测试里唯一那条红（`admin overview does not describe disabled automatic card opening as enabled`）就是它，**有意保持红作为缺口信号，不要为了全绿改它**。做供给控件时前后端一并闭合。
- 面五 UI 第二轮：工作台用一周后按 Lemon 反馈改。

**不做**（已定，别重开）：接口三段统一（D-248）、早交付（D-240）、卡台解耦到 API（D-253）、自动登录续 token（D-249 补记）、完整财务总账（D-275）。

**仍未验证、不因 V2 推进而消失的**：当前 release 的真实单计时与异常恢复；旧 403 上游诱因未知；原 UNKNOWN 异常不能只靠代码推测定因。不要擅自创建付费验收单。具体遗留见 `UNVERIFIED_LEDGER`，其中旧日期的「从未成功/未上线」须按最新证据订正，不作为当前事实。

**已立项、未排期**：数据库集成测试在生产代码上 14/43 失败（D-258），AGENTS.md 把它列为发布门槛但实际过不了。不挡 V2 落实，挡的是「发布前能不能真验证」。

## 5. 仍受约束的后续工作
- Pro 5X/20X：两阶段（先 Plus 后升级）方案已退休（D-245，ChatGPT 可从 Free 直升 20X）；Pro 单与 Plus 同型，只是 Checkout 选的套餐不同。自动化接入排期＝§4 第⑦步。**305/306 接单位已于 2026-09-17 07:28 关闭（D-245）**，20X 码下单现返回 `ORDER_ROUTE_UNAVAILABLE` 503；重开条件＝Browser PoC 冻结合同 且 API 一单 pro5/pro20 验证，各自达标各自开（D-252 缝 b）。
- 多lane/云端迁移、长期稳定性、卡台交易独立对账与供给能力尚不能以本轮发布宣称完成。
- 不引入跨客户Session缓存，不提前释放尚需收尾的浏览器；人工待处理可能占用lane。
- 不为本轮追加住宅出口采购、自动换卡/自动重付、新资金账或新订单系统。

## 6. 维护与回滚
- 过程追加HANDOFF_LOG；决策更新DECISIONS；收尾重写HANDOFF_NOW；生产事实只改CURRENT_STATE。
- 发布只从单一提交prepare→校验→switch；数据库集成测试串行。
- 已核实撤回时无早交付待收尾run，未做状态迁移；后续回滚仍须现场查在途付款。不要将有已知缺口的b31a88a当推荐回滚点。
- 原09-14地图中已过期的“架构待确认/UNKNOWN未跑/按连续5单验收”等表述已移出当前入口，原文留档：archive/2026-09/PROJECT_MAP_pre_D240_2026-09-16.md。验收遵循最新D-222/D-240，不从旧地图重开方向讨论。
