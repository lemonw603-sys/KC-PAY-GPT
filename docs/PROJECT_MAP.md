# AI充值业务｜项目地图

最后核对：2026-09-26 03:4x（UTC+8）。本页只放目标、块序与状态、下一步、欠账、不做；**生产数值只看 `CURRENT_STATE.md`**，过程看 `HANDOFF_LOG.md`，决策看 `DECISIONS.md`。
2026-09-25 前的完整旧版（顶部 8 段「覆盖」+ 八步表逐步记录 + 已完成欠账）原文：`archive/2026-09/PROJECT_MAP_pre_onepage_2026-09-25.md`。

## 1. 目标与原则

- 客户自助提交 CDK 和 Session，系统开通目标套餐；运营不必逐单在场。北极星是一天几十到上百单。
- 付款结果不明时不重付、不换卡、不换执行器；CDK / 订单 / 消费账本只有一套。
- Plus 成功口径（用户定）：确认 Plus、关闭自动续费、内部核对完成后才显示成功；提前成功方案已撤回（D-240）。
- 路线 / 卡源冻结到单，变更只影响新单；资金与生产动作须当次确认。

## 2. 块序与状态（D-352 定序，唯一）

编号说明：D-352 起用「块」；旧「V2 八步」①～⑥ 已完成，⑦ = 块 6，⑧ = 块 7。

| 块 | 内容 | 状态 | 证据 |
|---|---|---|---|
| 0～3 | 落盘；highvcc token；A/B 只验已发布项；运营不在场三硬伤（付款不明只锁账号 / 入单查心跳 / 处理中超时转人工） | ✅ 已发布 | D-356～D-359，在当前 release（`git log 7e88952..0d06f41`） |
| 4 | 真钱 Plus 验收 | 并入「可离开」第一条（D-366） | D-366 |
| 5 | 订单页按真实单定需求后做一版 | ✅ 已发布 | D-356～D-359 同批 |
| 6 | Pro 5x（与 Plus 同型：付款 → 确认 Pro → 取消续费）；20x 官方暂停（D-370） | ✅ 代码已合 main 并发布（release `20260925-block6-pro5x-f748bb6`），常驻池已切固定目录；**305 仍关**，重开前先定 5x 卡从哪来（Lemon：以后再说） | D-370～D-383 |
| 7 | 删表 + 可离开收尾 | 删表 ✅（迁移 060/061，D-367）；可离开收尾等首张真实客户单 | D-367、D-366 |

**「可离开」四条**（D-352，第一条 D-366 改）：①首张真实客户 Plus 单做真钱验收（Bark 来单 → 开窗口按 RUNBOOK §1 盯、对照 `contracts/2026-09-18_delivery-criteria-contract.md`），此后连续 10 单真实客户单无人介入——**未达成**；②停摆时客户有明确答复且 Bark 有推——✅（块 3）；③付款不明只锁账号——✅（块 3）；④RUNBOOK 有每周自检命令——✅（`scripts/weekly-check.sh`，D-360）。

## 3. 下一步（按顺序，每步等 Lemon）

1. ~~highvcc token~~ ✅ 09-25 07:25:58 UTC 重贴，08:15 UTC 同步确认有效、告警已关（CURRENT_STATE token 行）。
2. ~~块 6 Plus 回归演练~~ ✅ 09-25 17:22 UTC 通过（D-382，PRE_SUBMIT_STOPPED ₱982.14 / 税 0）。
3. ~~合 main → 发布 → 池切固定目录~~ ✅ 09-25 17:4x UTC（D-383）。**重开路线 305**：先定 5x 卡从哪来（两台 pro_5x 水位 0；欠账 1、2），Lemon「以后再说」。
0. ~~P0 付款后核实窗口非法~~ ✅ 已止血（D-387，配置 300000）；代码层修在批 B。
4. 等首张真实客户 Plus 单（可离开第一条），之后数连续 10 单。
5. ~~贴 token 当场验证~~ ✅ 随块 6 发布（D-383）。
6. 块 7 余项：`checkout_artifacts` / `browser_artifact_secrets` 两张空表——建议暂不删，下次本来要改付款链路、本来要演练时顺手做（D-384）。

## 4. 欠账（每条带触发条件；已完成的见旧版原文）

| # | 欠什么 | 什么时候必须做 | 出处 |
|---|---|---|---|
| 1 | 开卡任务按 `product_code` 自动回写卡的产品归属（写 PRODUCT_ONLY）；兜底已有（>$75 默认不给 Plus） | 给 5X/20X 设水位 > 0 或让调度器自动开它们之前（块 6 重开 305 前要看） | D-296 |
| 2 | 人工导入闸门：金额命中 5X/20X 开卡金额时不许默认进 Plus 池 | 往后台放第一张 5X/20X 卡之前 | D-296 |
| 5 | 今日花费聚合（口径：只算给客户充值消费掉的钱） | 做工作台数字墙那格时 | D-294 / D-295 |
| 6 | 数字墙布局重调 | 上生产真跑一天、有真实数据之后 | D-294 |
| 7 | CSS 收敛阶段 2～4 | 跟着每次页面重做逐块吃 | D-293 |
| 9 | API 路线 `pro5/pro20` 映射验证（块 6 只做了 Browser） | API 路线要接 Pro 单之前 | 八步表 ① |
| 12 | 界面文案闸门只扫前端，后端产出的文案不在管辖内 | 下次往闸门加规则时；或再抓到第二例时 | D-305 |
| 13 | 今日花费可能为负（`SUM(t.amount)` 不约束符号） | 出现第一笔负数流水时；或做数字墙「今日花费」时 | D-307 |
| 14 | 两张卡账面对不上（5501、7402）；先把 `trade_time_raw` 解析进 `occurred_at` | Lemon 定先搁置 | D-307 |
| 15 | 企业邮箱注册的号，价格框默认停在 Business 栏，导航找不到 Plus（D-378，Lemon 定这次不改；这类客户单会安全失败、要人工换号） | 第一张真实客户单因「找不到 Plus 按钮」失败时 | D-378 |
| 16 | 客户页「四态」只做一半（处理中合一 / 复核说正在人工确认 / 环只在处理中显示 / 等卡提示），块 6 已发布但未做（D-386 盘点发现） | 客户页批 A 时 | D-219 / D-250 / D-386 |
| 17 | ~~客户页 P1 正确性~~ 前三项随批 A 上线（D-388）；剩「执行器失败后仍显示处理中」的巡检告警、判未扣款关单的 canRetry 偏保守 | 下一批客户页 / 巡检改动时 | D-386 / D-388 |
| — | 数据库集成测试在生产代码上 14/43 失败，发布门槛实际过不了 | 发布前要真验证时 | D-258 |
| — | 第③④⑤块「只报未改」待定：scheduler 去重桶、「放弃并放卡」按钮、手动用卡影响资格的细做法、`REVIEW_REQUIRED` 老 flag 清理 | Lemon 排 | 旧版 §4.1 |

**已立项未排期**：V2.2 Browser 迁云 Windows（日均单量逼近本机 1 lane 上限时，D-225）；自动销卡（手动删卡成为负担时，D-232）；免费试用号改走 API（D-265）；面五 UI 第二轮（工作台用一周后）。

## 5. 不做（已定，别重开）

接口三段统一（D-248）、早交付（D-240）、卡台解耦到 API（D-253）、自动登录续 token（D-249）、完整财务总账（D-275）、自动开卡总开关（D-306，停一台＝水位设 0）、Plus 结账页套餐核对（D-375）、订单页 D-345～D-350 旧方案（D-351，需新的明确请求才重开）。

## 6. 未验证与约束

- 当前 release 真实付款、取消续费、付款不明恢复、Browser 崩溃补核均无真单样本；旧 403 上游诱因未知。清单：`UNVERIFIED_LEDGER.md`。
- 单 lane、本机执行；不引入跨客户 Session 缓存；不追加住宅出口采购、自动换卡 / 自动重付、新资金账或新订单系统。

## 7. 维护与回滚

- 过程追加 HANDOFF_LOG；决策更新 DECISIONS；收尾重写 HANDOFF_NOW；生产事实只改 CURRENT_STATE（`state-check.sh` 核对其中一部分行，结尾一行写明覆盖多少）。
- 发布只从单一提交 `deploy-release.sh prepare → 校验 → switch`；数据库集成测试串行。回滚点与删表后的回滚前提见 CURRENT_STATE「回滚点」行。
