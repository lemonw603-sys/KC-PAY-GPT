# 系统整改矩阵｜排查收敛候选

状态：用户已批准集中整改，现进入本地实现与回归；不包含部署或真实付款授权。
证据入口：PRODUCT_SIMPLIFICATION_DISCUSSION.md；可复现脚本/结果：artifacts/system-audit-20260906/。

## 根因分组及实施顺序

|优先级/组|已确认问题|整改目标|退出证据|
|---|---|---|---|
|1 执行接口与职责|A01工厂参数不一致；A02填卡前最终报价；G01手工卡派发依赖Provider读；G07预检服务继续领非付款实验任务|统一正式输入合同；一处填表后报价；分离预检与实验领单；Browser不依赖无关Provider开关|实际生产组装隔离调用通过，不只测试构造；填表前无最终金额拒绝；预检不消费正式付款队列|
|1 卡身份与资金恢复|G09内外卡ID混用；G06活动卡身份被快照覆盖；G12同来源重复实体卡；G17余额门槛代替补款到账；G18提交中崩溃无恢复入口|内部ID、ProviderID明确映射；活动卡身份不被替换；实体卡唯一归属；按本次资金证据收口；提交不明只恢复核实不重付|完整迁移库导入及竞争测试、实际Provider适配器配假传输核对目标、崩溃恢复无重复提交；不足余额提示不挡准备|
|2 连续现场与结果|A03返回失败仍关窗；A04保留字段却拒绝恢复；A05上号器忽略目标身份；G08付款后另开页；G04过期运行遗留|以订单/客户/Profile归属统一恢复；人工与自动互斥；实际结果写回；终态才清理|正确客户持续恢复、错误客户不接续、已填字段校对后续行、两种失败出口及人工完成都覆盖|
|2 CDK与后台交付|A06响应丢失仅转查询；A07交付重放；A08多次弹窗；G15刷新假成功；G16写成功后读失败误报；G19旧客户响应覆盖；G20查询耗尽写额度|恢复原订单/原批次；分开历史交付记录与新发放；取消已批准的CDK重复密码；查询/写入分离；界面只报告确定结果|网络故障注入、实际监听函数/接口集成；反复查询不影响生成/切换；未登录仍拒绝；已发码不重发|
|3 规则/展示收敛|G11有效期口径冲突；G13空余额=0；G03缺外部ID掩盖其他对账；A09客户文案与产品不符|统一字段语义；未知不等于0；对账按路线需要判断；展示实际阶段|边界输入与异常映射测试；真实页面检查主按钮和状态反馈|

组1–3是依赖顺序，不表示每组都必须独立生产发布；首批形成一套可验收候选。先用隔离测试证明，再安排发布。

## 明确不是Bug或尚未证明

- 已兑换CDK补记历史交付合法；需防请求重放，不删除历史补记。
- 同attempt并发begin实际只一方成功，锁机制保留。
- 跨来源同PAN并发样本只存1卡，死锁回滚不等于重复入库；反馈可改善。
- Bark已发送记录不因updated_at变化必然重发；没有当前重复发送实证，不盲删通知。
- 空完整快照会标缺失但不删除记录；操作语义待明确，不能误当损坏文件。
- 当前资金定时任务独立运行不等于发生资金动作；停主Worker不等于停所有后台。

## 必要待验证项

1. 已登录线上UI布局/交互：未完成，不能用静态源码代替。随后台修复完成真实页面验收。
2. 导入/分配交错、跨源竞争更多时序、补款外部成功但本地失败：现有窄样本不够，在相关修复前后扩大隔离集成。
3. A11交易读取器缺意图的完整LIVE时序、G10幂等key参数不一致：需补组装调用复现。
4. Session刷新/5X/20X真实资格、周期、报价：未验证；后续产品批次不提前开放。不能保证服务端过期凭证一定可恢复。

## 多产品与减法关系

先修共享执行/资金/交付底座，再加产品CDK与两阶段升级。每阶段允许一笔合法付款，不把整单只能一次的假设套到升级。复用产品、订单、交付、卡源服务，不新增平行系统。

确定减法：CDK重复密码与短时清空；正式链路重复填表/重复最终报价；多个模块自行销毁现场；重复查询与相互矛盾状态反馈。旧API/数据库只有迁移调用者并证明无用才删除；不承诺删表比例。历史订单、付款证据及有效锁不可删。

## 发布与验收

核对发布时实际服务器release、服务、数据库及本机Worker版本；备份与回滚准备；不覆盖活动订单；部署后复验客户端/后台/执行器/API实际行为。故障后不盲目重付。真实付款须另获明确授权；隔离通过不等于真实资金验收。

## 本次交付边界

已完成：已证实问题归并、优先顺序、减法对象、保留项及未验证项记录。
未完成：业务整改、部署、全部真实页面验收、多产品真实付款。
用户未提交的docs/DECISIONS.md不触碰。正式整改后逐行补代码、测试、发布与现场证据，不靠聊天记忆宣布完成。

## 整改执行记录｜第一检查点

- G15本地已修：Browser右上角刷新调用并等待3个loader，不再await未执行函数。
- G16本地已修：写入成功与后续读取失败分开；写响应未知时不宣称“原选择未改变”，读取当前值而非重复写入。
- 新增v1/test/admin-operation-feedback.test.js，实际前端监听函数配模拟DOM/API回归3/3；语法及git diff --check通过。
- 范围：仅以上两项本地修复，未部署/未做真实线上点击；不是整批完成。旧审查复现脚本仍保存故障断言，在修复后预期会失败，使用新增回归作为修复证据。
- 其余执行/资金/生命周期/CDK事项继续待修，不能用3个测试代替全链路验收。用户docs/DECISIONS.md未触碰。

## 整改执行记录｜第二检查点

- A01/A11本地接线修正：shared-live-composition传递平铺runId/orderId/attemptId；实际交易读取器延迟到read/reconcile第一次调用才创建，不在付款前读取尚不存在的付款意图。复用同一reader；首次数据库错误不永久缓存失败，后续核实可恢复。普通恢复器原有平铺row接口不变。
- 定向11/11通过，覆盖真实HNSKJ读取器在intent存在后构造、参数合同及暂时故障恢复；现有组装测试仍仅构造层，完整生产组装端到端未因此视为通过。
- 不涉及卡台写请求、不改数据库/真实订单；未部署。重复填表报价、卡身份、资金恢复及生命周期仍待后续整改。

- 第二检查点全量Browser回归：169项，160通过、9项环境跳过、0失败。日志artifacts/system-audit-20260906/browser-remediation-tests.log；跳过项与生产验证保持未完成。
