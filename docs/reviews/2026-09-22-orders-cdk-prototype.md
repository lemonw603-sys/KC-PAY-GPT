# 订单/CDK入口对比原型（已否定，仅作追溯）

> 2026-09-22 Lemon 反馈：A、B 两版都没有真正理解并保留正式 CDK 的完整业务流程，均不作为后续设计或实施基线。不要继续让用户在 A/B 之间选择。

依据：D-345邮箱优先及详情CDK联动、D-346同码历史展开、D-347双时间/具体阶段且稳定优先。用户同意比较两种方式，未选定版本。

原型：`docs/design/prototypes/step6-orders-cdk-compare.html`；地址 `http://127.0.0.1:8899/step6-orders-cdk-compare.html`。复用现有8899原型服务，未新增worker。全为合成数据；无生产SQL查询或真实订单，不能以此证明后端实现。CSP connect-src none/form-action none，无外部依赖；所有生成与兑换为内存模拟。

## 对比约定

A两个侧栏入口（订单/CDK），B一个入口内两个分区（充值记录/发码与管理）；同样数据、样式和能力。继承候光CDK稿绿白色、紧凑操作控件，不重做全局设计系统。不新增收费操作，也不把本稿单张发码缩减解释成删除正式CDK其他能力。

## 验收及边界

- Playwright真实浏览器实跑：A邮箱→详情CDK→CDK定位→生成测试码→模拟兑换→充值列表对应邮箱；B同一链路，两条均通过。
- 历史失败已点击展开；桌面1440与手机390截图已观察，手机表格局部横滚，桌面双时间同列。
- 独立审查：代码范围ship，无生产接口；存在非阻塞键盘焦点恢复、CDK空态colspan 5/实际4列的轻微问题，正式实现前处理；不称完整无障碍验收。
- 未验证真实剪贴板/真实触屏手势，不接生产支付、Session或CDK。
- 页面合并选择仍待用户，未更改业务代码/数据库/正式页面。该稿不改变D-339统计，不执行历史清理。
