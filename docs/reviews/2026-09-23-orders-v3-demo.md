# 订单页 Demo v3 浏览器验收（D-356）

- 页面：`docs/design/prototypes/step6-orders-v3.html`（本机 `node scripts/proto-server.mjs 8899` → http://localhost:8899/step6-orders-v3.html）
- 数据：`docs/design/prototypes/data/orders-demo.jsonl`，2026-09-23 14:5x UTC 从生产只读导出的 80 单**形态**（单号真实；邮箱与卡密只保留序号 `custNN` / `PLUS-…NNNN`，不含任何客户标识）；Demo 另加 2 条演示单（付款待核实、续费待确认）让「需要我处理」可点。
- 验收脚本：`2026-09-23-orders-v3-demo-check.mjs`（真实 Chromium，1280×860 断言 innerWidth；390px 复测）；结果 `2026-09-23-orders-v3-demo-result.json`：**25 / 25 通过**。字体 `/assets/fonts/*.woff2` 三个 502 是原型服务器反代到未启动的本地后台所致，与 Demo 无关，单独记录。
- 覆盖的裁定：默认近 7 天（6 个客户充值）/ 全部 57 个码 82 次尝试；每码最新一次为主行、「此前 N 次未成功」就地展开收起（12 次那张码展开 11 条）；只有邮箱格打开右侧抽屉，点产品格不开；状态四选 + 产品 + 路线 + 日期范围；「需要我处理」行内直接放此刻唯一可做的动作（取消并放卡 / 去核实 / 已在账号里取消续费 / 标为已手工充值），点击先弹确认框写清会发生什么；抽屉：此刻可做 → 进度（提交时间 + 充值成功时间）→ 客户与卡密（CDK 定位入口、这张码的尝试次数、等 Session 时写明卡已放回）→ 卡与钱三问（用了哪张 / 扣了没 / 还能再充几单，去卡片页链接）→ 技术证据折叠；付款待核实抽屉顶部醒目提示「不会重付、不要让客户重充」；抽屉正文无内部编号。
- 生产形态在 Demo 里的样子：「需要我处理」= 10（1 等 Session + 7 笔成功但续费待确认 + 2 演示），与 FB-04 那 7 笔一致；「未成功」34；「已成功」13（13 张码最新一次成功）。
- 未覆盖（正式实现时补）：分页；「标为已手工充值」的付款痕迹守卫由后端算；抽屉里账本/流水的真实数字；与 CDK 页、卡片页的真实跳转定位；parity 契约与冻结快照（Demo 过了再冻结）。

## 第二轮（2026-09-24 00:5x UTC+8，Lemon 试用五条反馈）

1. 卡片联动：卡尾号可点 → 打开**本单**抽屉并定位到「卡与钱」段（高亮），段内再有「去卡片页看这张卡」；不再是 V1 卡片弹窗（D-351 的问题）。
2. 客户列与产品列间距：列宽改为像素定死（客户 300 / 产品 64 / 路线 150 / 进度自适应 / 时间 112，`table-layout:fixed`）。
3. 行内动作：改为与进度芯片同一行的文字链接式（`.od-rowact`，取消类用 danger 色），不再是实心大按钮、不占额外一行；抽屉里保留 34px 实按钮。
4. 抽屉里「在 CDK 页定位」「去卡片页」不再弹提示，Demo 只把链接文字改成「正式版：跳到…并定位」；确认框「确认」也不再弹 alert。
5. 列表客户格只留邮箱一行；单号移到抽屉头副行（搜索仍可按单号）。
自审（refactoring-ui）另改：产品/路线列文字原被外壳样式染成主色，改为次级灰。
验收脚本增至 **30 项，30/30 通过**（新增：卡尾号打开本单抽屉并高亮卡与钱、行内动作非实心按钮、CDK 定位无弹窗、客户格只有邮箱）。
