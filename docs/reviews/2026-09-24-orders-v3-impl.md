# 订单页 v3 实现验收（2026-09-24 01:1x UTC+8）

对象：`v1/public/admin/assets/orders.js` / `orders.css` + `index.html` 订单视图 + `admin.js` 抽屉重排（D-356/D-357，任务书 `tasks/2026-09-24-impl-orders-page.md`）。
环境：本地后台 `127.0.0.1:8803`（当前工作区代码）+ 隔离库 `step6_demo`（24 单 / 22 码造数，两单绑卡 5590、7726）+ 原型服务器 8899。生产未动。

## 结果（均为本轮实跑，原始输出见同名 `-result.json`、`scratchpad/v1-test.log`）

| 项 | 结果 |
|---|---|
| 真实浏览器验收 `2026-09-24-orders-v3-impl-check.mjs` | 30 / 30（含四态：有数据 / 空 / 接口 500 / 切桶）|
| 几何契约 `scripts/visual-parity.mjs docs/design/parity/orders-page.json` | ✓ 与原型一致 @1440px（18 探针，容差 1px）|
| `npm --prefix v1 test` | tests 1104 · pass 1032 · fail 0 · skipped 72 |
| `scripts/css-drift-check.mjs` | ✓ 没有倒退（orders.css：字面色 0 / 重复选择器 0 / 离标高度 5 基线）|
| `scripts/ui-copy-check.mjs` | ✓ 干净 |

## 30 项
- ✓ 登录后进入订单页，无 JS 错误
- ✓ 视口 1280
- ✓ 「全部」有主行且每码一行（LOAD-0020/0021 并进 0022 的码）（22 行；默认计数「22 个客户充值 · 本页 24 次尝试」）
- ✓ 状态角标全部 = 主行数（22）
- ✓ 「需要我处理」角标 ≥ 3（等 Session ×2 + 付款不明 + 续费待确认）（5）
- ✓ LOAD-0022 主行带历史小标「2」
- ✓ 展开后 2 条历史行（LOAD-0021 / LOAD-0020）
- ✓ 再点收起
- ✓ 点产品格不开抽屉
- ✓ 抽屉头是邮箱、副行是单号
- ✓ 抽屉含此刻可做 / 进度 / 客户与卡密 / 卡与钱 / 技术证据
- ✓ 抽屉含提交时间与充值成功时间
- ✓ 这张码的尝试 = 3 次（这张码的尝试3 次）
- ✓ 未成功单抽屉有「标为已手工充值」按钮
- ✓ 抽屉正文无内部编号
- ✓ 点卡尾号打开本单抽屉并高亮「卡与钱」
- ✓ 「需要我处理」每行都有行内动作链接（5 行）
- ✓ 付款不明（LOAD-0019）的动作是「去核实」
- ✓ 「去核实」打开抽屉且顶部有付款未知警示
- ✓ 等 Session（LOAD-0023）的动作是「取消并放卡」
- ✓ 点「取消并放卡」先弹确认框
- ✓ 续费待确认的单动作是「已在账号里取消续费」
- ✓ 按单号搜索只剩那一单
- ✓ 产品筛选 20X 有结果且少于全部（6）
- ✓ 路线筛选 API 有结果或明确空态
- ✓ 无结果时显示明确空态
- ✓ 接口 500 时显示「读取失败」与重试，不冒充空态
- ✓ 重试后恢复
- ✓ 390px 下表格可横向滚动
- ✓ 全程无 JS 错误

## 验收过程中修的
- 「需要我处理」桶里没有一键动作的单（例如付款后失败、卡台调用卡住）原先行内是空的；现在给「看详情 ›」，桶里不留空行。
- 验收脚本两处竞态：切桶后旧表还在时就数行（改为等主行数 = 角标数）；关抽屉后立即点卡尾号（改为等抽屉真关、等抽屉头出现该单号）。
- 契约 `impl.prepare` 先点「全部」（与原型侧一致），否则近 7 天内没有绑卡主行，`.od-cardlink` 量不到。
- 抽屉「提交时间」区块的内联 `style=` 改成 `.od-kv-gap` 类（隔离测试禁内联样式）。
- `public-isolation.test.js` 两条断言从 D-350 表头/摘要卡更新为 v3（五列、四桶分段器、`groupByCdk`、「取消并放卡」、`#manual-fulfilled`）；搜索走 JSON body 的断言同时读 `orders.js`。
- 资源版本：`admin.js?v=91`、`orders.css?v=2`。

## 已知与未验
- 截图 `orders-impl.png` 顶部的「订单读取失败」通知是脚本注入 500 后留下的，不是页面缺陷。
- 后台登录限流 5 次 / 15 分钟：反复跑验收会被挡（本轮重启本地 8803 两次清零），生产不受影响。
- 「标为已手工充值」按钮在真实库上的整链（后台 → `manual-fulfilled` 接口 → 服务）只在单测与服务层集成里走过；真实页验收只验到按钮存在与确认框。
- 生产未发布：线上仍是 D-350 版（`admin.js?v=89`）。

## 第八轮（02:1x UTC+8，D-358）：时间列前移、进度列收尾、内容区封顶 1280

| 项 | 结果 |
|---|---|
| 真实页验收（+「1920 下内容区封顶 1280」「1920 下时间列在进度列前」） | 32 / 32 |
| 几何契约 @1920（+封顶探针 + 时间格 padding/textAlign） | ✓ 一致 |
| `npm --prefix v1 test` | 1032 / 0 / 72 |
| CSS 棘轮、文案检查 | ✓ |

量到的（原型 = 实现）：客户@0 · 产品@171 · 路线@231 · 时间@407 · 进度@519（表格左缘起，1920 视口，卡片宽 1244）。

