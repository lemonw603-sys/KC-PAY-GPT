# 接班一屏（HANDOFF_NOW）

更新：2026-09-16 12:48 UTC（本轮现场复核）。生产事实唯一表：CURRENT_STATE.md。

> **12:55 UTC新发现，优先级高于首单验收**：D-240早交付后的收尾恢复Session入口仍只接受RECHARGE_PROCESSING，RECHARGE_SUCCESS会被拒。已单变量夹具复现且生产文件hash一致；当前新版本订单0、活动run0，未发现受影响生产单。此前“恢复已测试”仅覆盖任务可发现，未覆盖真实Session源。先修并补组合测试、取得发布确认，再建议用新单验收。详见incidents/d240-followup/findings.md。本轮未停单或改业务。

> **13:58 UTC用户最新要求**：先实际量清早交付能省多少，收益不明显就不拆分。历史Plus/取消落库差不是外部动作耗时，当前无法报准确秒数；新版本订单0。不要直接继续扩建收尾架构。测量方案见incidents/d240-followup/early-delivery-benefit.md；线上逻辑未回滚，恢复门控缺陷仍未修。

## 当前状态
- 用户批准D-240发布，单提交b31a88a已上线，release `20260916-d240-b31a88a`。
- Web/服务器Worker已同步新release；本机Browser PID99137，supervisor98863，12:39:21 UTC启动。
- 12:48 UTC新连接复核：接单/付款true，心跳12:48:02.327Z，非终态0、active_runs0；自恢复接单后新订单0，因此新版本尚无真实单样本（操作前仍须重查）。
- 公网客户JS/CSS v39，文件哈希一致，服务健康200；未代客户充值。
- D-240四项：减少重复核验、确认Plus立即交付且内部取消/对账不展示、有界只读重试、圆环实际步骤与付款阶段2秒轮询。
- 全量本地测试与27项真MySQL集成记录见docs/tasks/2026-09-16-D240-delivery.md；生产部署证据docs/incidents/d240-deployment/。

## 下一步
1. 优先处理上述收尾恢复门控；修复验证并确认上线后，新版本首个客户订单到来时，现场按订单/运行/事件核对实际耗时、早交付及后台收尾；不能拿旧版成功单冒充新版本验收。
2. 如有异常，优先看结构化HTTP诊断和payment-outcome-diagnostic；付款未知不重付。
3. D-240运行验证后，再按PROJECT_MAP/V2.0_EXECUTION恢复V2其余工作。

## 已知边界
- 原h9RKl的403上游诱因仍未知；新有限重试不等于根治。
- 旧版重提x-tIs最终自动成功；新版本暂无真实付款样本，不能承诺固定节省秒数。
- D-217保守资格规则随发布生效，现场可分配1张5371；1657同步余额与账本有差异，不能按单字段放行。见CURRENT_STATE。
- 客户成功后run可能仍收尾；同一lane不得换客户破坏Session。人工待处理会占用该lane，这是安全代价。
- 回滚前必须清完/保留新版处理早交付待收尾run，再切旧release；旧版不能直接处理这类状态组合。
- 主工作区原有两处诊断已在发布前留存并纳入同一release，不再有悬空未提交改动。隔离工作区保留作为测试证据，不运行业务worker。

## 接手时避免踩坑
- 从本仓库main继续；已发布业务提交是b31a88a，之后main的交接/状态提交不代表再次发版。不要把旧隔离候选worktree当成待上线版本。
- 原CDK完整值不写交接；定位使用订单publicNo与证据文件即可。
- 后台run未完不等于客户未开通；先同时查orders和browser_runs，不能看到SUCCESS就杀收尾进程，也不能看到CANCELLATION_PENDING就让客户重兑。
- 真实验收建议记录：首次Plus证据/客户成功写入/客户状态响应/内部取消和对账完成/付款提交次数。不能用旧版本回填的finished_at计算性能。
- 本轮只更新文档并做只读核验；没有后台监控承诺，也没有安排自动真实测试。
