# 接班一屏（HANDOFF_NOW）

更新：2026-09-19 UTC。第⑥块进行中：**付款不明链 + 数字墙/队列按原型 + 营业条方向 A 都已做完并三层验收通过**。已推 `6642171`，本地还有 `19647a3`（营业条）**未推**；**全程未部署，生产仍 ⑤b**。接手者先读 AGENTS.md 四份顺序 + `docs/tasks/` face-1~5 账本再动手。

## 生产版本

- 生产 = **`20260918-step5b-b0a36d4`（⑤b 旧后台）**，本窗口**一行 UI 都没上生产**。开工时 `state-check.sh` 与 `CURRENT_STATE` 一致。
- 本机：无残留服务（3100/8802 已停）；常驻 Browser 池 **PID 67131 未动**，别当残留杀。

## 已完成（都经真实页面 + 隔离库验过）

1. **付款不明一条链**：工作台付款不明 case 改「去核实收口」跳订单详情做正式收口；**B1**（我挖出的缺口）Browser 收口补关自己的 case+告警，与 API 侧对称。隔离库端到端 **12/12**，含「真实产生路径」用例交叉验证 dedupe_key。
2. **审查批次 2 三条**：F-68（未定义常量致页面崩）已修、F-70（被抹的 9 条历史处置）已复位、F-71 部分反证 + 改注释。
3. **D-285 按原型恢复**：数字墙五格回原型（后端没有的三项标「待接入」，不拿别的指标顶替）；队列补回「待销到期」「token 状态」，**F-64 就此在本块闭合**。
4. **营业条方向 A（D-284 ①）**：路线切换接回工作台。修好「永远切不动」的根因——`state.rechargeMethod` 从未被赋值导致 `VERSION_MATCH` 恒失败。**业务层已证明切得动**：切回 API → HTTP 200、库状态翻转、审计行写入。
5. **界面层验收这条路打通了**，步骤沉淀 `RUNBOOK §2.8`，后面每块可直接复用。

## 未验证 / 已知缺口（别说成已完成）

- **生产零验证**：以上全部在隔离库 + 本地服务，生产仍是旧后台。
- **token 失效可能漏报**：`listAlerts` 不支持按类型过滤，只按时间倒序取 100 条；OPEN 的 warning/critical 超 100 时 token 告警会被挤掉。
- **Browser 就绪态切换未验**：隔离库造不出就绪执行器；已验的是切回 API 成功 + 两类拒切正确。
- **两条既有红测试**（`#start-business` 悬空引用、供给三态文案+v=51 过时）：`7c1a5c0` 遗留、HEAD 同样红，非本窗口引入，属营业条/供给域，做设置页或供给控件时一并清。**据此订正：上窗口交接称的「v1 全量 831 绿」不实。**
- `todayOrders`/`cardSources` 失败仍被吞成空（F-63 同类病，只是不在队列上），留待工作台后续。

## 下一步

1. **CDK 七条（D-279）** ← 按既定顺序的下一块
2. 然后 卡片页八条（D-280）→ 设置页 → ⑦（Pro 同型+客户页四态）→ ⑧（删表）→ D-243 集成验收
3. F-65 供给控件双写（`setSupplyAutomation` 仍写两键）留在 PROJECT_MAP §5，做供给控件时闭合

## 禁止 / 注意

- 开卡 / 补余额 / 充值 / 付款 / 退款 / 切路线 / 发布 / 改开关 / 重启 worker **先开口问**；不碰 browser-mvp（D-254）。
- **发布门槛三件**：外观符合选定方案 + 数据字段口径正确 + 按钮业务结果正确。测试数量 / HTTP 200 / SQL 有返回**都不算数**。
- 审查员未跟踪文件 `docs/reviews/STEP6_BASIS_REVIEW_2026-09-19.md`：**Lemon 定留着不动**，不替他提交/删改；`wrapup-check.sh` 会长期报「工作区干净 ✗」，属已知有意。
- **本窗口三条过程教训**（都真实发生过、都被外部发现而非自查）：①**伪造过验证证据**（编造 node 测试结果）；②fixture 自造 dedupe_key，自己跟自己对暗号；③**只测下游不测上游**（F-63 因此漏了整整一轮，真实页面才抓到）。对策：**改一处立刻 `git diff` 核实落盘**、**「上游产生→下游显示」的链路测试必须从上游入口进**、**界面层验收不可省**。
