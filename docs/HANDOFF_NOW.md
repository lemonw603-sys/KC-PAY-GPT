# 接班一屏（HANDOFF_NOW）

更新：2026-09-26 14:1x（UTC+8）＝ 06:1x UTC。执行顺序以 **D-352** 为准，「可离开」第一条以 **D-366** 为准；本窗口 D-376～D-396。

## 现在的状态（06:10 UTC 现查；明细见 CURRENT_STATE.md，`state-check.sh` 20 项一致）

- 生产 release **`20260926-d396-7a7998b`**（`7a7998b`，D-396；无新迁移）。回滚点 `20260926-d395-8b61c71`（无迁移，直接回滚）。发布准备会先自动跑真数据库测试（`v1/scripts/mysql-tests.sh`，需本机 `pojia-stage1-mysql` 容器）。
- **本机常驻池 PID 56510**（supervisor 84622），跑固定目录 `~/pojia-pool/releases/20260926-d389-1ca9a42`（`1ca9a422`；到服务器那版之间池子引用的代码没变，不用重启）；进程环境查新单 1 秒、核实窗口 300000、租约 900。池回滚：`scripts/pool-release.sh switch 20260925-block6-pro5x-f748bb6` 后按 RUNBOOK §5 停 / 开。
- 付款开关 true、下单查心跳 true、心跳新鲜。可分配 Plus 卡 1 张（8718）；**非终态订单 0、「需要我处理」0**（D-396 清掉自用期旧单）。供卡、水位、路线 305/306 见 CURRENT_STATE（本轮未动）。

## 这一轮做完的（D-386～D-396）

- 客户页 / 提速盘点（D-386）→ P0 核实窗口止血（D-387）→ 客户页批 A（D-388）→ **D-389**：失败 / 换号屏收起进度环；卡密已退回的失败单显示可重新兑换（生产 4 单已生效）；批 B：核实窗口上限对齐（填错池启动即报错）、点击后只读计时、查新单 1 秒。演练通过（`PJV1-Iynl…`，PHP 982.14 / 税 0 / 点击 0）后池已换新版本。
- **D-390**：付款前卡住保持「当场判失败 + 退卡密 + 推手机」（Lemon 定）；巡检新增「停在付款前没人处理」推手机、「客户卡住了」告警在订单结束后自动收掉；等卡说明「可能需要几分钟」。欠账 18 Lemon 定不做。
- **D-391**：客户页复核归「处理中」（绿色、「正在人工确认，完成后这里会更新」，Lemon 选 A，覆盖 09-12「不说已转人工」）；欠账 16 清零。**何时对外发新卡密由 Lemon 自己定**。
- **D-392 / D-393**：付款前页面类失败自动重跑讨论后 Lemon 定先不做（建议 2、3 放着）；诊断页新增「失败原因统计」（默认近 7 天、演练单不计入、每种原因的原话与单号）。更正过一次错误说法：导航 / 付款段失败原话其实 09-25 / 09-16 起已入库。
- **D-394**：后台订单抽屉「放弃并放卡」（付款前挂住的单 Lemon 自己能收，不用 SSH；与 `close-rehearsal-order` 共用一份守卫）；真数据库测试 69/0/1 全绿，`deploy-release.sh prepare` 先跑它、不绿即停；事实表「已知未修」逐条复核（4 条关闭）。21 张自用期老码已按 Lemon 指示由我作废（04:4x UTC，正式路径，独立核实 21/21）。
- **D-395**：「演练单」统一按运行方判定，首页成功率与失败统计共用一份规则。
- **D-396**：清「需要我处理」：7 张续费未确认按 Lemon 确认登记、3 张资金已 CLEARED 的失败单改规则移出、09-23 测试单取消。残留 1 个 D-158 前的 BROWSER_PREFLIGHT 任务（`PJV1-Liuc`，已关单）放着，每周自检会提醒。
- 导航失败证据包第一次在真实页面上派上用场：企业邮箱号演练失败，截图直接显示价格框停在 Business 栏（欠账 15）。

## 下一可执行项

1. **等首张真实客户 Plus 单**（可离开第一条，D-366）：Bark 来单 → 按 RUNBOOK §1 盯。跑完读 `~/Library/Application Support/pojia-browser-live/pool/post-click-timing.jsonl` 里该 run 的四行（点击后各段耗时、当场确认为什么没成），据此挑欠账 19 的提速。导航失败先看 `evidence/` 目录（`evidenceRef` 在 fail-closed 事件里）。
2. 何时对外发新卡密：Lemon 自己定（D-391），系统侧无待办。现可用卡密 7 张，均为 09-23 起带 30 天有效期的新码（Lemon 自用测试码）。
3. 放着（D-392 / D-393，欠账 20）：付款前页面类失败自动重跑、重新兑换卡密预填、企业邮箱号改走换号。看失败原因统计积累到数据后再议。
3. 重开 305（5x）：Lemon「以后再说」，先定 5x 卡从哪来（欠账 1、2）。
4. 块 7 空表两张：暂不删，下次本来要改付款链路时顺手（D-385）。

## 已定 / 禁区

- 付款前三件不改（D-254）；browser-mvp 改动走白名单 + 全量测试 + 演练；常驻池重启、发布、开关、LaunchAgent 先问。
- Plus 不加结账页套餐核对（D-375）；企业邮箱号 Business 栏不改（D-378，欠账 15）。
- 演练：不跑 preflight；租约 900；开跑前关 3 号窗口残留 chatgpt.com 标签；**用普通邮箱测试号（账号哈希 `32eac1d1…`，`PJV1-v3ti` / `PJV1-Iynl` 用过），别用企业邮箱号（`f1fed7c9…`）**；演练失败时单子占着卡，锁过期（900 秒）才能收口，收口前别让 Lemon 建新单（会等卡、可能自动开卡）；Session 不经 AI（Lemon 在客户页提交）。
- 回复 Lemon：**回答和思考都用中文**；开头给定位、话要短、大白话，结尾一节「要你决定的」列全。

## 未验证边界

- 失败原因统计：生产上 09-16 / 09-25 之后常驻池还没有真实失败单，所以「执行时原话」这一路还没在生产数据上出现过（隔离库已验）。
- 当前 release 真实付款、取消续费、付款不明恢复、Browser 崩溃补核、点击后计时均无真单样本；5x 专属逻辑无样本（无 5x 卡）。
- 09-25 两次「付款表单加载失败」原因未知；证据包上线后再现会有现场。
- 事实表「已知未修」行是 09-10 旧清单，未逐项复核（state-check 标陈旧）。

## 每块收尾

跑 `scripts/wrapup-check.sh`；向 Lemon 交「我理解的决定」清单；重写本文件。
