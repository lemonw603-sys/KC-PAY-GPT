# 接班一屏（HANDOFF_NOW）

更新：2026-09-21（UTC+8）。**第⑥步主体已发布；使用反馈整改与新版Bark尚未发布，不混为一件事。**

## 当前位置

- 正式release仍是 `20260921-step6-9b9f181`；生产动态事实唯一看CURRENT_STATE，不据本机HEAD判生产版本。
- D-329总门槛：FB-01～09全部处理/明确裁定并验收 → 完整使用一天 → 用户确认后进⑦。A/B均未整体结束，C客户链/D订单需求/E整体试用尚未完成，⑦⑧及各产品真钱集成验收仍在后面。
- 当前规划唯一入口：`tasks/2026-09-21-post-step6-feedback-plan.md`。用户已确认D-338钱包提醒覆盖，不重开已定交互，不自动发布。

## 必须保留：23:20一次性闲置复测

- 应用heartbeat id `highvcc`，2026-09-21 **23:20北京时间**，一次执行（原22:20已顺延）；先用automation view核实当前状态，不能新建重复任务。
- 用户20:13:30自行登录官方IAB tab7，返回expireTime声明09-28 20:13:30到期，无refreshToken。已完成观察，Network已disable，不再要求重复登录。约7天声明不等于保证有效；网页2小时闲置与服务端TTL待区分。
- 基线与指纹在 `reviews/2026-09-21-feedback-a/login-observation.md`。原cua绑定authTab/authCdp可能仍在；若不可用按浏览器工具文档找原tab，不reload/重登/续期/保活。
- 复测先确认同一会话，再至多一次原有wallet GET，凭据只在浏览器内沿用，不导出/打印完整token或写生产。不具备条件就报告限制，不能另登录补测；完成/需用户配合后停止该任务并通知。
- 在等待期间不访问卡台标签，不发卡台请求。只做markHandoff保留标签，不为等待启动脚本。

## 本地已验、未发布的反馈改动

- A / D-332、333已获用户试用认可：工作台钱包原位查询USD+时间、刷新/登录入口，失败留旧标旧；仅本页面最近成功值，整页reload后未查询，不冒充旧库数据为新值。
- token待办直达表单；手机表单单列；写成功/读失败与结果不明分开。撤回“卡台未保存”拦截、去API/Browser原生confirm、操作提示顶部醒目；不取消收费动作确认，不改后台卡源/资金规则。
- A证据：`reviews/2026-09-21-feedback-a/third-batch.md`；25项隔离检查通过，不是生产真付款/全部页面几何验收。
- Bark / D-336：现阶段余额继续逐笔；普通来单仅后台，资金/人工异常保留，手机文案短而不丢关键值。D-337优先关联充值邮箱，缺失/失败/矛盾才回单号；后台原文保留。
- D-338：仅同账户硬钱包不足覆盖软低余额预警，须当前事故正常投递且不早于软预警；不同账户、失败/旧轮次/旧观察或未知key不压掉。后台两条原事实保留，余额变化和拒付不受影响。用户已认可该组规则。
- Bark证据：`reviews/2026-09-21-bark-simplification/report.md`、`email-update.md`、`wallet-coverage.md`。最新默认1077 tests/1008 pass/0 fail/69 skipped；18项真实隔离MySQL含并发通过。真实Bark请求0，未做真机验收，不称手机已经降噪。
- 其他没有同因证据的提醒不强行合并；不新增通用时间静音、服务或未来自动关闭余额通知开关。

## 已完成的生产维护：不要重复执行

- D-335批准的2旧case＋10付款不明alert已RESOLVED，原记录保留，审计647～656；报告 `reviews/2026-09-21-feedback-b-close/report.md`。
- 8类关联业务完整行哈希不变。通知服务将对应10投递转CANCELLED，旧发送时间/次数保留，无重推；不是业务数据变化。最初通知hash校验失败已按实际生命周期补证，不能再当未解决故障。
- 固定cc10b30维护脚本和受限12行备份在 `/opt/pojia/maintenance/20260921-D335-cc10b30/`；不改release、不重启服务。不得重复判款、重开旧提醒或整体倒库。

## 当前待共同裁定与未验证

1. B工作台成功率：默认近7天提交且已完结的订单、明确演练排除、显示成功/样本数、无样本显示“—”、历史保留可查，是**待确认建议**，未改算法。普通未成功关闭先沿现口径计入，进行中不算失败；自动完成率不混在一起。14:51 UTC只读仍20成功/37失败/21关闭，近7日5单（1成功/1失败/3关闭），详见B报告/规划。
2. B剩余：2条RECONCILIATION账本、7单续费待核（其中1缺CONSUMED）、16旧CDK归属，数量有重叠；不能批量关闭/退码。明确演练4单保留记录，不删历史。原分类 `reviews/2026-09-21-feedback-b/report.md` / preview.md 是D-335前快照。
3. A数据/登录：两页库存与HNSKJ钱包内部对齐已验；highvcc上游新鲜度和真实服务端有效期未闭合。费用/拒付10行事件时间缺失，今日费用按首次入库有跨日限制，不能偷偷换算法或清流水。
4. C客户链路、D订单页需求与可点击稿、E统一回归及完整一天试用还需完成；订单页不能直接大重构。Pro每卡次数/归属/PoC在⑦前补，Pro路线不得擅开。

## 保留现场与旧发布证据

- 8805 `scripts/feedback-demo.mjs`：用户可交互Demo，真实前端+内存假API，无DB/Provider/worker；故障工具已折叠默认正常。仅demo-token，不录真实凭据。用户试用产物保留，不当残留worker清。
- 8804 CDK隔离Demo、8803原后台演示、8899原型服务按原用途保留；不凭旧PID判当前存活。测试MySQL是127.0.0.1:54186，禁止将13306生产隧道用于测试。
- 第⑥步发布与恢复证据 `reviews/2026-09-21-step6-release-v2/report.md`；055～058、日对账摘要保存、63表隔离恢复已验证，不等于完整灾备或真钱验收。旧66bfe98发布在迁移前中止，不要重新执行其维护步骤。
- CDK D-314～319、诊断D-325/326已发布裁定不重开；支付开关/钱包输入标签不改。原型权威：工作台C、营业条乙-3、卡片A、CDK A、诊断确认稿。

## 硬边界

- 任何新生产发布、凭据写入、资金或清理动作先确认范围；不把Demo认可当发布许可。
- 未知付款不重付、不换卡/执行器、不释放栅栏；不改D-254受限Browser付款实现或重启常驻池。
- 每批先列跨模块影响与验收，局部测试不冒充全链路；生产修改须新连接独立复验。
- 原有异地备份/密钥可获取性、真实客户各产品链路、历史账目等未验证项继续看UNVERIFIED_LEDGER。旧日志的“待发布”须按最新事实覆盖，不机械复做。
