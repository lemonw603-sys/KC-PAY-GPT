# 接班一屏（HANDOFF_NOW）

更新：2026-09-19 UTC。第⑥块工作台**做偏、已回滚、正重做**；本窗口按 Lemon 指令停止扩展、换窗口交接、不部署。接手者从 main 继续，**先读 AGENTS.md 四份顺序 + `docs/tasks/` 的 face-1~5 五面账本，再动手**（本窗口最大教训：没读账本就做 UI，把业务决策当 UI 元素）。

## 生产版本（现场核实一致）
- 生产 = **`20260918-step5b-b0a36d4`（⑤b 旧后台）**。step6 于 2026-09-19 发布后**当天回滚**。`state-check.sh` 现场核对 CURRENT_STATE **一致**（release / 三服务 active / 可分配 2 / 非终态 0）。三服务 web 1699695 / worker 1699700 / bark 1699751 均在 step5b、active、NRestarts=0。**无数据库迁移**（step6/回滚均无）。

## 已提交实现（都在 git，均**未业务验收**）
- `8cd6d7e` step6 工作台第一版（**已回滚、做偏**）：卡与钱按台聚合（`getOverview.cardStockByProvider`，生产只读验证过 hnskj 0 / backup-a 2）、队列资金核对、营业条五决定候光皮版、v1 全量单测 831 绿。
- `7c1a5c0` 在途重做（**未部署、未验收**）：候光外壳（sidebar 6 导航 / 顶栏去开始营业·刷新 / 全局搜索移顶栏 / 引候光字体）方向对；数字墙去「待接入」换真实指标；48 告警折叠。**营业条改 toggle 时方向错误**——擅自砍掉「走哪条路线」「开卡补钱」塞设置页占位，业务理解错误。

## 审查与处置
- 审查：`docs/reviews/STEP6_REVIEW_2026-09-19.md`（F-61~F-65，全 P1，独立审查）。
- 处置：`docs/reviews/DISPOSITIONS.md`（F-61~F-65 全**接受、待重做闭合**）。要点：F-61 解决案例不收口 / F-62 字段接错(`unverifiableCount` 应 `unverifiableAmountCount`，30 显示 0) / F-63 失败吞成空态 / F-64 队列跳转无落点 / F-65 供给开关连补余额一起写。

## 业务理解（重做的地基，本窗口读 face-1/face-2 后校正）
- **营业条＝运营决定的镜子，接近旧后台五决定**（接不接单 / 走哪条路 / 用哪个卡台 / 能不能付钱 / 能不能自动开卡）；砍成三 toggle 是错的。
- **走哪条路线**＝面一 C1：「产品×执行器→卡台」选择表 + 切换前四项校验（路线唯一 / 可分配>0 / provider 健康 / 版本对）+ 拒切显示逐项原因（任务书 A 项明确要它在工作台）。当前 Plus 走 API 冻结、5X/20X 路线关。
- **「开卡补钱」**＝面二供给：**补余额线已弃删（D-218）**，实质是「能不能自动开卡」（水位驱动、hnskj 故障转 highvcc）；**参数**（水位 Plus 每台 2 / 开卡金额 50·100·150 / 每卡单数 Plus3 / 钱包底线 hnskj30·highvcc20 / 每日上限 20 / 告警线 50）进**设置页**（任务书 B 项）。

## 未验证 / 未做（别说成已完成）
- step6 与在途工作台**只本地 mock 渲染验过外观，未做数据字段口径、按钮业务结果的验收**（F-61~F-65 就是没验出来的）。
- 三个「待接入」数（自动完成率 / 今日花费按台 / highvcc 钱包）未实现；在途版把数字墙换成可分配卡 / 告警数，属改展示内容，**待与 Lemon 再对齐**。
- 自动完成率口径任务书仍写待定，重做前先冻结。

## 接下来做什么（PROJECT_MAP 既有执行顺序，不改范围与验收标准）
1. **重做前先冻结基准**：与 Lemon 确认一份有效原型 + 允许差异（D-283 授权 sidebar 暂留旧皮 与 回滚要求换皮 冲突，需裁）+ 自动完成率口径。
2. **重做工作台**：先读透 face-1~5 + DECISIONS；营业条按五决定（含路线四项校验、供给正确单键语义）重做；数据字段对真实响应验真、按钮真收口、失败态与空态分开（闭 F-61~F-65）。
3. 继续完成⑥（工作台达标 → CDK D-279 / 卡片 D-280 / 设置页），随后⑦、⑧，再 D-243 集成验收。

## 分支、运行与禁止事项
- main 是接手入口；本窗口提交见收尾（`7c1a5c0` 及 docs 收尾提交）。隧道 13306 保留。
- 常驻 Browser 池 **PID 67131** 不要当残留杀掉；本窗口临时 http.server 8799 已停、无残留。
- 开卡 / 补余额 / 充值 / 付款 / 退款 / 切路线 / 发布 / 改开关 / 重启 worker **先开口问**；范围外只报不改（D-254）。
- **发布门槛三件缺一不可**（STEP6_REVIEW 教训）：外观符合选定方案 + 数据字段口径正确 + 按钮业务结果正确；测试数量 / HTTP 200 / SQL 有返回**不替代**任何一件。

## 尚未解决（交给新窗口 + 待 Lemon 裁）
- 有效原型 / 允许差异未冻结（D-283 sidebar 暂留旧皮 vs 回滚要求换皮 冲突）— 待 Lemon 裁。
- 营业条业务模型未重做落地（路线四项校验、供给正确语义、接近五决定）。
- F-61~F-65 五个 P1 全待重做时闭合。
- 后端 `setSupplyAutomation` 仍写补余额键（F-65 既有风险）— 已登记 PROJECT_MAP §5，重做时核。
- 自动完成率口径待冻结。
