# 接班一屏（HANDOFF_NOW）

更新：2026-09-20 UTC。**第⑥块的卡片页已按 A 版重做完（D-305，B1~B4 全做完）**；**自动开卡总开关裁定不做、F-65 结案（D-306）**；**「可分配」拆成库存口径/分配口径两个数（D-307，Lemon 追问「明明有两张卡」查出来的真 bug）**；**全后台状态术语统一（D-308）**；**待销去掉「取消续费未确认」、搬进工作台队列（D-309）**；**卡片页按钮精简 + 高级区 4 件 + 卡台管理表并入台账栏（D-310）**，连同此前的工作台、CDK、设置页——**第⑥块四页代码层面全部完成，一行都没上生产**。生产仍是 **`20260918-step5b-b0a36d4`（⑤b 旧后台）**。**下一步是 CDK 页重做**（Lemon 判「完全不合格，没有任何整体设计」，与卡片页同因；卡片页这一轮的做法可以照搬：先出/定原型 → 冻 `_frozen/` 快照 → 写 `docs/design/parity/` 契约 → **契约先于实现写，第一次跑必然红** → 实现到绿 → 变异测试确认契约有牙）。三页做对之后**才**发布，发布必须连同迁移 055 一起做。接手者先读 `AGENTS.md` 的四份顺序，再看下面「仍开的洞」。

## 生产版本

- 生产 = **`20260918-step5b-b0a36d4`**，第⑥块**一行 UI 都没上生产**。
- **迁移 055 未应用生产** → CDK 新端点在生产会报字段不存在。**发布必须连同迁移一起做**，迁移前单独确认。
- 本机：**8803 验收服务在跑**（`step6_demo` 隔离库，供比对与页面验收）；**8899 原型服务在跑**（上一个窗口起的，`proto-server.mjs`）；常驻 Browser 池 **PID 67131 未动，别当残留杀**。
- 演示库 admin 密码本轮重设过，存在本窗口 scratchpad 的 `parity-pw`，**不入库**。`.claude/launch.json` 已指向本窗口的 env 文件（`.claude/` 在 .gitignore 里）。

## 本轮做完的（卡片页 B 部分，D-305）

**A 版三块 + 一个高级入口**，顺序＝两台卡台 → 在役卡表 → 待销清单 → 高级（此前待销在卡表前面，是偏离原型的，已改回并用位置选择器钉住）。

- **B1 删块 2**（「卡片概况 + 卡台管理」，675px）。七样内容逐样查过去向：四个合计数删（块 1 按台已有，生产实测同值 2=2）；每卡单数、最低余额搬设置页（那里本来就有，块 2 是重复入口）；浏览器当前卡台删（工作台有）；**API 固定卡台、卡台管理整表、新增备用卡台**降进「高级」（这三样全后台只有这一个入口）；**开卡被禁的三条原因 + 剩余开卡额度搬进 HNSKJ 开卡区**——任务书那张表漏了这一整块，Lemon 当次同意搬。
- **B2 token 认告警**：改读 `PROVIDER_TOKEN_EXPIRED`，不再读 `supply_fault_state`。无告警时只报「上次贴 <时间>」，**一个字都不写「有效」**。
- **B3 去确认词 + 补回头路**：新建 `POST /card-retirement/undo`；「我手动用了」的撤销复用早就存在的 `DELETE /card-operational-overrides`（前端此前零调用，**差点又造第二份**）。
- **B4 五条折叠条收进一个「高级」**（六件）。

**三层验收**：业务层隔离库真实点一遍全链路、每步新连接独立复核（审计链 `CARD_RETIRED_CONFIRMED → CARD_RETIREMENT_UNDONE → CARD_OVERRIDE_CLEARED` 齐全）；界面层 18 条探针全绿 + **5 个变异全被抓**；工程层全量 **1010 / 943 pass / 0 fail**，新增 19 条测试 + **5 个变异全被抓**，三条闸门全绿。

> **全绿是本轮才有的**：那条挂着「F-65 有意留红」名头的测试，随 D-306「总开关不做」改写了。**不是为了绿而改**——是它守的那个控件被裁定不建，断言随决定作废，剩下的改成钉住新决定，三个变异全被抓。

## 接班要知道的洞（都仍开）

1. **日对账逐卡差异明细无处可看**（§4.1 欠账 11）。
2. **`todayOrders` / `cardSources` 的 `__error` 没有消费方**（F-71 / F-72 的另一半）。
3. **界面文案闸门只扫前端**（§4.1 欠账 12，本轮新登记）：`ui-copy-check.mjs` 只看 `admin.js` 和两个 `index.html`，**后端产出的文案照样显示到页面上**。本轮实例是待销原因里的枚举名（已就地改掉三条，闸门没扩）。
4. **审查员角色决定至今未落 `DECISIONS.md`**（F-69）。这是治理决定，留给 Lemon 定；本轮取号 D-305/D-306 是常规取号，不是占它的号。

**本轮结掉的**：~~自动开卡总开关~~ **裁定不做（D-306）**。停自动开卡的正式做法是**设置页把水位设成 0**——调度器需求 = `max(水位, 等卡单数)`，按台按产品，比全局一刀切更贴合「只停坏掉那一台」。查清了两件事：**「不接单」代替不了它**（调度器 `grep accept_new_orders` → 0 次，它压根不读），**而水位可以**。前端孤儿处理器已删、设置页已写明、测试已随决定改写（三个变异全被抓）。后端 `setSupplyAutomation` 一次写两键的毛病没修，但前端零引用且有测试钉着不许调；将来真要接界面，**先拆成单键**。

**D-307 要接班知道的一句**：卡片页/工作台的「还能服务」= 库存口径；「此刻可立即绑」= 分配口径（多一条 15 分钟同步时效），后者只在小于前者时出现在脚注里。**别再把这两件事合成一个数** —— 合起来的那个版本让 hnskj 的好卡在 91.7% 的时间里显示成 0。

## 未验证 / 别说成已完成

- **生产零验证**：第⑥块四页全部只在隔离库与演示服务验过。
- **生产现在正有一条 OPEN 的 token 告警**（`provider-token-expired:…0103`，2026-09-18 12:52 起）。上生产后卡片页会立刻显示「已失效」——**那是预期，不是新 bug**。
- **退役撤销对「本轮之前就已退役的卡」会走「原值不详」分支**（老事件里没记 override 原值），生产现有退役卡全部属于这种，**这条分支没在生产数据上跑过**。
- **`CARD_OVERRIDE_CLEARED` 是本轮新增的事件类型**，生产库里一条都没有。
- **开卡闸门只验到「阻断态」**，「可以开卡」那一支只有单元测试。
- **比对契约只覆盖 1440px 和高级区折起态**，窄屏与展开态没量过。
- 其余见 `docs/UNVERIFIED_LEDGER.md` 末尾一节。

## 下一步

1. **CDK 页重做**（做法照搬本轮：原型 → 冻快照 → 写契约 → 契约先红 → 实现到绿 → 变异测试）。
2. **诊断页维持现状**，不动（Lemon 定）。
3. 三页做对后**才**发布：四页 + 迁移 055，回滚点 `20260918-step5b-b0a36d4`；**发布与迁移各需当次点头**。
4. 然后 ⑦（Pro 同型 + 客户页四态）→ ⑧（删表）→ D-243 集成验收。**⑦ 的前置**：8 步表 ① 的 `pro5/pro20` 映射验证。

## 欠账在哪看

**`PROJECT_MAP.md` §4.1 的欠账表现在有 12 条**，每条写明触发条件。**别靠记忆，开工前扫一眼那张表。**

## 禁止 / 注意

- 开卡 / 补余额 / 充值 / 付款 / 退款 / 切路线 / **发布 / 应用迁移** / 改开关 / 重启常驻 worker **先开口问**；不碰 browser-mvp（D-254）。
- **发布门槛三件**：外观符合选定方案 + 数据字段口径正确 + 按钮业务结果正确。测试数量 / HTTP 200 / SQL 有返回**都不算数**。
- **原型必须 link `_frozen/` 里的定稿快照，不是实时样式**（D-293）。**定稿原型还必须带全外壳**——本轮漏了侧栏，比对报出 26 处假差异，根因是 `.admin-shell` 的 230px 那一列（D-305）。
- **比对契约写完必须做变异测试**，否则不算数。**契约先于实现写**更好：第一次跑必然红，这本身就证明它不是空的。
- **CSS 只许降不许升**（D-293）：`node scripts/css-drift-check.mjs`。新写的规则用裸 `var(--wb-*)`，不加字面色回退（cards.css 的 65 处字面色是欠账 7）。
- **界面「像不像设计稿」不许用眼睛判**（D-292）：跑 `scripts/visual-parity.mjs`。测量/截图前先断言 `innerWidth`；放大只用 `transform: scale()`，绝不用 CSS `zoom`。
- **删代码按语法边界，不按字符串距离；UI 改完必须真点一遍**（D-289）。本轮的变异测试又抓到同型：删掉一个块 → `loadStock` 在中途抛 → 卡片列表整块不渲染，而测试全绿。**凡是往 DOM 直接赋值的地方都要守卫。**
- **说「不存在」之前至少查三种形态**。本轮两次靠这条避坑：`DELETE /card-operational-overrides` 早就有（换个形态才找到，差点造第二份）；token 告警的 key 在隔离库有第二种形态（是上个窗口的手写夹具，生产只有一种）。

## 三条闸门与验收环境怎么跑

```bash
# 演示/验收服务（step6_demo 隔离库）与原型服务
#   .claude/launch.json 的 admin-ui-verify 起 8803；proto-server.mjs 起 8899
PARITY_PROTO_BASE=http://localhost:8899 \
PARITY_ADMIN_BASE=http://localhost:8803 \
PARITY_ADMIN_PASSWORD=<本窗口 scratchpad 的 parity-pw> \
  node scripts/visual-parity.mjs
node scripts/css-drift-check.mjs
node scripts/ui-copy-check.mjs
```
