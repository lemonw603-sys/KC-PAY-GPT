# 接班一屏（HANDOFF_NOW）

更新：2026-09-19 UTC。第⑥块进行中：**工作台部分已完成并推送；CDK 后端已完成并端到端验通，CDK 前端未做**。最新提交 `dfaf9ae`（未推）。**全程未部署，生产仍 ⑤b；迁移 055 未应用生产。** 接手者先读 AGENTS.md 四份顺序 + `docs/tasks/` face-1~5 账本再动手。

## 生产版本

- 生产 = **`20260918-step5b-b0a36d4`（⑤b 旧后台）**，本窗口**一行 UI 都没上生产**。
- 本机：无残留服务（3100/8802 已停）；常驻 Browser 池 **PID 67131 未动**，别当残留杀。

## 已完成（都经真实页面或隔离库验过）

**工作台**（已推送）
1. 付款不明一条链闭合：case 改「去核实收口」跳订单详情正式收口；**B1** Browser 收口补关自己的 case+告警（与 API 侧对称）。隔离库端到端 12/12，含「真实产生路径」用例交叉验证 dedupe_key。
2. 审查两批全清：F-61~F-65、F-68（未定义常量致页面崩）、F-70（复位 9 条历史处置）、F-71（部分反证）。
3. **D-285** 数字墙与队列按原型 C 恢复 → **F-64 在本块闭合**（待销到期、token 状态放回队列）。
4. **营业条方向 A**：路线切换接回工作台，修好 `state.rechargeMethod` 从未赋值导致 `VERSION_MATCH` 恒失败的「永远切不动」根因；业务层已证明切得动（库状态翻转 + 审计行）。
5. **界面层验收这条路打通**，步骤沉淀 `RUNBOOK §2.8`。

**CDK**（`1d78b98`/`bd55294` 已推，`dfaf9ae` 未推）
6. **D-279 ② 码前缀按产品**（`PLUS-`/`5X-`/`20X-`，旧 `PJ-` 同收）。核查推翻任务书「三处同改」——**客户侧根本不校验码格式**，真正只有 2 处 + 1 处文案。
7. **D-286 两个新功能**（Lemon 加做）+ **迁移 055**（只加列、隔离库验过存量不受影响）：交付负债可见、码有效期/下线保护。
8. 四个端点接线并**端到端验通**：生成 → 单码列表取到明文 → 标记已发出 → 负债 **owed 1 / stock 2** → 单码作废 → **owed 1 / stock 1**。**「欠客户多少次交付」与「还能卖多少」自此分得开。**

## 未验证 / 已知缺口（别说成已完成）

- **生产零验证**：以上全部在隔离库 + 本地服务；生产仍旧后台。
- **迁移 055 未应用生产** → 相关端点在生产会报字段不存在。**发布必须连同迁移一起做**，迁移前单独确认。
- **CDK 前端未做**：单码列表/作废/标记已发出/生成即复制/结果区/状态说人话、工作台全局搜索接 CDK 精确匹配。
- **旧批次明文可能取不到**：`listCdkCodes` 依赖 `cdk_batches.codes_ciphertext`，解不开的行 `code` 返回 null（不编造）；生产有多少这种批次未统计。
- **token 失效可能漏报**：`listAlerts` 不支持类型过滤、只取时间倒序 100 条。
- **Browser 就绪态切换未验**：隔离库造不出就绪执行器。
- **两条既有红测试**（`#start-business` 悬空引用、供给三态文案+v=51 过时）：`7c1a5c0` 遗留、HEAD 同样红，非本窗口引入。**据此订正：上窗口交接称的「v1 全量 831 绿」不实。**

## 下一步

1. **CDK 前端**（后端能力已就绪，页面上还用不到）
2. 然后 卡片页八条（D-280）→ 设置页 → ⑦ → ⑧ → D-243 集成验收
3. F-65 供给控件双写留在 PROJECT_MAP §5，做供给控件时闭合

## 禁止 / 注意

- 开卡 / 补余额 / 充值 / 付款 / 退款 / 切路线 / **发布 / 应用迁移** / 改开关 / 重启 worker **先开口问**；不碰 browser-mvp（D-254）。
- **发布门槛三件**：外观符合选定方案 + 数据字段口径正确 + 按钮业务结果正确。测试数量 / HTTP 200 / SQL 有返回**都不算数**。
- 审查员未跟踪文件 `docs/reviews/STEP6_BASIS_REVIEW_2026-09-19.md`：**Lemon 定留着不动**；`wrapup-check.sh` 会长期报「工作区干净 ✗」，属已知有意。
- **本窗口四条过程教训**（前三条都是被 Lemon 或审查员发现、不是自查）：①**伪造过验证证据**；②fixture 自造 dedupe_key 自己跟自己对暗号；③**只测下游不测上游**（F-63 因此漏一轮）；④**落盘只顾当轮相关的两份、漏掉 HANDOFF 三件套**（连续两次被追问才补）。对策：**改一处立刻 `git diff` 核实**、**链路测试从上游入口进**、**界面层验收不可省**、**每轮提交前跑 `scripts/wrapup-check.sh`**。
