# 接班一屏（HANDOFF_NOW）

更新：2026-09-13 13:50 UTC。写者：大脑窗口。**本文只由大脑窗口写，且每次重要事实变化后整篇复核，不止改暂停原因那一行。**

## 分工

- **大脑**：本窗口。全项目理解、排序、验收、四份事实源（本文、CURRENT_STATE、DECISIONS、PROJECT_MAP）、生产动作与浏览器侧实施。
- **Codex 已退出**（D-152）。其阶段 1 已合并保留，`BRAIN_TO_CODEX.md` 停更。
- 任务书只写目标/验收/边界，不写实现路径（D-151）。大脑不替系统操作页面（D-160）。
- **大脑的三类惯犯错误与硬规则见 D-172，接班先读。**

## 里程碑

**2026-09-11 11:15 UTC 全链路首次跑通**（`PJV1-ztS9FZ3QcwHopTmZRfDY`，git 标签 `e2e-first-success-20260911`）。

**2026-09-13 02:56 与 03:39 UTC 连续两单全自动成功**（`PJV1-KLZokl…` 2 分 31 秒、`PJV1-L_fKJY…` 3 分 40 秒），
同卡段 `53211304`、同执行器版本、各实扣 $15.72，全程无人工操作。第二单的账号**自带历史绑卡且未删**，
照样填入我们的卡付款 —— 回答了"客户账号已有绑卡时系统能不能填我们的卡"这个悬着的问题。
两单的全部细节与可复现前置检查固化在 `docs/E2E_CHAIN_TEST_SAMPLE.md` 第 5、6 节。

## 现在状态（2026-09-13 13:45 UTC 当场核实，`state-check.sh` 除 5371 余额行外全一致，该行已改）

- 生产 release **`20260913-orderno-6dcb458`**；web / worker 均 active；迁移 `052_cards_bin`。回滚点 `20260913-expyear-c3f664f`。
- 付款开关 **true**、接单开关 **true**；非终态订单 **0**、active_runs **0**。
- **可分配卡 1 张**：`5371`（卡段 `54317796`，**库内 $34.28**，从未同步过卡台实时值——`last_transaction_synced_at` 为空；账本用量 0/3）。
  **今天它跑了 12 单全败**（同一客户 `chenxing66623@gmail.com`、同一 Session，10:21–13:15 UTC），随后 **Lemon 手动付款一次
  （13:29–13:34 UTC，D-206）——付款成功尚未独立核实**，核实后账本记 1 笔、CDK 锁回 `REDEEMED`。
  `3159` 已 `DEPLETED`（$2.84），**账本计数 2、实际用 3，补钱前人工核对**。5 张幽灵卡 `source_present=0`，不要手动放出来。
- **本机 worker PID 10503，起于 13:12:59 UTC**，带当天全部代码（最后一处 adapter 改于 13:11:11）。
  重启只需 ~20 秒（D-204 修了 SSH 连接风暴之前是 18 分钟）。
- **`highvcc-card.mjs` 的 token 已过期**，卡台流水/实时余额查不了；刷新要 Lemon 在登录的 Chrome 里做，我不碰密码。

## 下午 10:21–13:35：同一客户 12 单全败，然后 Lemon 手动 3.5 分钟走通（D-203 → D-206）

**读 D-206 的轨迹表，比读下面这段有用。** 压缩版：

- **12 次失败不是 12 个原因**：前 11 次进了结账页死在结账阶段（`CHECKOUT_DRIFT` ×5 等），第 12 次连账号都探不到
  （`ACCOUNT_STATUS_UNKNOWN`，注入的 Session 探账号 403）。情况在恶化。
- **代码修了三层，每层都是"同一教训又漏一处"**：
  - D-203：填完卡后 requote 观察的 catch 没判瞬时故障——`isTransientPageError` 的**第四处**漏网；
    断连现在退回 `CARD_READY` 等重试，不再退 CDK。
  - D-205：Lemon 要"失败别清空卡资料，停住让我接手"。清空的 finally 有**两处**，D-203 只改了预检那处，
    真正出事的是付款适配器那处。现已都改，且分两类：**故障类保留现场，规则拒绝类（税不为零等）照样清空**——
    后者若保留，运营看到亮着的订阅按钮会去付含税价 ₱1,100，违反零税硬约束。`CHECKOUT_DRIFT` 现带 `stage`。
  - D-204：worker 重启 18 分钟起不来——supervisor 每分钟开 7 条 SSH 撞 sshd 并发上限，自己把自己挡在门外；
    加 `ControlMaster` 复用后一轮 6/6。顺带修了 ready-check 三处"查不到被播报成坏了"（可分配卡 0 张、生产服务空）。
- **Lemon 手动走通的四条差别**（D-206，只说记录里看得见的）：①他走 `/auth/login` 重新登录，执行器只注入 cookie；
  ②hCaptcha `Please try again` 全程挂着，不拦路（第二次证实）；③他不断连，执行器 D-203 那次死在 CDP 断连；
  ④节奏：进页→卡框 23s、填卡 50s、填账单 63s、提交→成功 37s。
  **前 11 次执行器在结账页的确切 stage 不知道**（D-205 之前不记），下次跑才能逐步对照。
- **我今天犯的、已收回的判断**：hCaptcha 拦路（实测 y=-8645 屏幕外）；"卡一个字没填进去"（填了，被清的）；
  "SSH 被我查太密限流了"（是工具自己的连接模式）；"可分配卡 0 张"（查询失败被当 0）。**每一条都是拿观察补了原因。**

## 今日（09-13）上线

`captcha-alert-6797fdc` **人机验证在检测到的那一刻就推手机**：原先 notify 钩子只弹本机 macOS 桌面通知
（`local-operator-notify.js` 走 `osascript`，人不在电脑前收不到），现在同时写 `operator_alerts` 走 Bark；
并把 `BROWSER_HUMAN_VERIFICATION` 补进 `BROWSER_ALERT_TYPES`（此前只有调用点没有类型，会抛
`unknown browser alert type`，等于告警根本写不进去）。测试加了一条断言守住"人机验证不得进
`PHONE_SILENT_TYPES`"。**写告警失败不影响付款流程。**

09-12 的 8 次发布见 CURRENT_STATE 与 `docs/HANDOFF_LOG.md`。

## 今日第二次上线（06:37 UTC）

`customer-ux-4ccb8c7` 客户页四处返工（Lemon 提的）+ 一个他没提、我查出来的真 bug：

- **付款成功的那几秒，客户看到的是橙色「遇到点问题」** —— `SUBMIT_UNKNOWN` 原本和
  `RECONCILIATION_REQUIRED` 一起映射成 `REVIEWING`（warn / 30 秒轮询），而它是**每一单
  必经的一步**（今天两单各停 8~9 秒）。改映射到新客户态 `VERIFYING`（正常色、3 秒轮询、
  「支付已提交,正在和 ChatGPT 核对开通结果」）；超过 3 分钟仍在确认才按停留时长降级成
  warn + 30 秒，真卡住照样告诉客户。`CONFIRMING` 轮询 6→3 秒。
- **进度环不跳**：跳点只有两处（阶段 4 实际 18 秒、阶段 7 实际 8.8 秒，都远短于段长常数
  90 秒；阶段 8 三个操作同事务提交、零停留，所以 77% 直接跳 100%）。没去猜每段该多长，
  改成换段逐帧追差距 12%、跑完 700ms 滑到 100，以后耗时怎么变都吸收得住。
- **去掉查询码，只留卡密**：查询码行、等待屏订单号行、「复制查询码」按钮全去掉。
  **限制**：状态接口不回传 CDK（凭证不该回传），所以成功页那行只能删不能换成 CDK。
  客服不受影响，后台搜索与 `find-order-by-cdk.mjs` 按 CDK 都能查到单。
- **订阅方案显示 `Plus`**：三处改走已有的 `productShortName()`；后端 label 仍是全名。

**还剩约 105 秒延迟是真实的**（点付款 → 标记成功之间确认付款结果 97 秒 + 核实 Plus 与
取消续费 8 秒）。不能靠提前标成功消除——那会破坏「付款结果不明确必须锁定」。见 D-192。

## 今日第三次上线（07:16 UTC）

`steady-ring-5825d1c` 进度环改匀速（Lemon：「一段快一段慢，我希望尽可能是匀速从零到 100」）。

根因不在曲线在分配：九阶段上限原本按**阶段数**平均分（每段 9~14 点），而真实耗时差 600 倍 ——
阶段 5+6 吃掉全程 80% 时间只分到 28 点，阶段 1+2 几乎不花时间却占 22 点。改法三条：
上限按真实耗时占比重分（2/6/9/20/62/94/98/99/100）、段内线性且每阶段用自己的 `typicalMs`、
超过典型耗时后剩 6% 指数逼近上限**永不停住**（停住的环和卡死的环长得一样）。
阶段 4~7 占全程 99% 的时间，速度落在 0.45~0.51 %/秒。

实测每 15 秒走的点数（单2 220 秒）：改前 `36.5 11.1 4.3 … 0.4 27.0` → 改后 `14.2 7.9 7.0×5 6.6×4`。

**分母只有两单**（2026-09-13 仅有的两次全自动无干预成功单），阶段 6 波动已经很大
（36.7s vs 96.5s）。攒够更多无干预成功单要回来重算 `CUSTOMER_STAGES` 那张表 ——
`typicalMs` 随状态接口下发，前端不存副本，改表即可。

给 Lemon 看的演示页（跑真实代码、可切真实速度）：
`https://claude.ai/code/artifact/26fbedd2-c763-475d-9ef1-0b878fc0af31`

## 第一个真实客户单：成了，但先被我的 bug 卡住 11 分钟（D-194）

`PJV1-XrDZCfpakRwaoRQLrEbS` 07:21 提交 → 07:32 成功，实扣 $15.72。

**前 10 轮全失败**，根因是我当天开卡时把 highvcc 返回的**两位年份** `28` 原样入库
（Excel 导入那条路径有 `2000 +` 转换，API 开卡这条没有，而它今天才第一次真用）。
执行器的过期校验算 `28×12+7=343 < 24321`，判卡已过期、拒绝付款。
**10 轮全是 `BEGIN_RUN → PRE_PAYMENT_ABORT`，没有一条 `PAYMENT_SUBMIT`，钱一分没动**
——执行器拒绝用"看起来过期"的卡付款，这条保护完全正确。

修了两处：**读取侧**容错两位年（本机 browser-mvp，已生效，对所有已入库的卡立即有效，
不用改数据）；**写入侧**开卡时存四位年（`v1/src/services/highvcc-card-service.js`，
**在服务器上，要发布才生效，下次开卡前必须发**）。修复后那一轮 **152 秒**跑完。

**教训（比"验外部字段"更具体）**：一条写入路径没有被真实跑过，就等于没写过。
以后新增或首次启用写入路径，必须拿它写出来的数据跑一遍**下游的真实校验**——
不是看写入成功，是看下游认不认。今天代价是一个真实客户等了 11 分钟。

## 流程提速：73% 的时间在两步，而这两步没有埋点（D-196，**未实施，等 Lemon 定**）

三单实测（`BEGIN_RUN` 起算，平均 167 秒）：填卡并提交 **56.3s（36%）**、等付款结果 **57.3s（37%）**，
两者合计 73%。**这两段内部一个事件埋点都没有**——11 个 action 里 `checkout-navigation` 是最后一个，
`browser_operations` 的 `prepared_at`/`completed_at` 又是同一时刻写的。**现在说优化哪里都是猜。**

代码里读出的线索（非实测占比）：填卡那段串着三个 30 秒超时（填地址／填邮箱／等零税重算）；
等付款那段 `watchPostSubmit` **轮询间隔 5 秒**——付款真成功后最多晚 5 秒才被发现，
这一条不用埋点也能确认是浪费。

建议顺序：①先给 adapter 那一趟加 4~5 个 checkpoint，**单独做、单独发、单独验**
（它改的是付款路径，全系统最敏感处）；②有数据后再收紧超时；③前段那 23 秒暂不动
（碰 Session 注入链路不划算，D-187 刚修好）。**不做**：不为省时间去掉零税校验或付款后 Plus 确认。

## 09:15–09:52 这四单发生了什么（D-198、D-198 续）

- `PJV1-4J5Oq9I…` **成功**，156 秒，卡 3159，实扣 $15.72。`card-balance-refresh` 首次被真单
  触发（`updated: 7`）——**但结果被付款确认覆盖**：卡余额被置回 NULL，D-195 想解决的问题
  又出现一次。手动跑一次同步才救回来（竞态，**未修**）。
- `PJV1-CsswKRT9…` **失败** `CHECKOUT_DRIFT`，卡 5371（新卡段 `54317796` 第一次用），
  钱没动、CDK 已自动退回。**样本 1 次，不下结论说卡段有问题。**
- `PJV1-pom5NfWi…` **卡死两次**：Playwright 的 `Execution context was destroyed` 被兜底成
  「客户 Session 无效」，Lemon 换了两次 Session 都是同一个错（D-198）。
  **第一次修复只改了一半**——改的是 `session-bootstrap` 的兜底，实际失败在
  `account-readonly-probe` 那处的另一份兜底。两处现已都先认瞬时故障，并加守卫断言
  （executor.js 里任何一行把未知错误归成 Session 问题的地方，必须出现 `isTransientPageError`）。
  **这个修复还没被真单验证过**——最后是 Lemon 手动付款收的口。
- 手动付款那单用 `close-manually-fulfilled-order.mjs --card-used` 收口，已独立核实：
  订单 `RECHARGE_SUCCESS`、卡释放并 `DEPLETED`、CDK 保持 `REDEEMED`、重复扣款口子堵死。
  **两件还欠着**：①**账本状态错标**——`card_consumption_ledger` **有**此单行（`PJV1-pom5NfWi…`），
  但状态是 `RELEASED` 而非 `CONSUMED`，而资格 SQL 只数 `RESERVED/CONSUMED/RECONCILIATION`，
  所以 3159 用量显示 2、实际 3。**2026-09-13 14:15 UTC 更正：先前写的「无此单行」是错的，
  行一直在，问题是状态**；②取消续费要运营去账号里做，然后在后台点「已在账号里取消续费」。

## 已发现、还没修的缺口

1. **`BROWSER_ORDER_STALLED` 是死类型** —— 在 `BROWSER_ALERT_TYPES` 里有定义（`critical`），
   但全项目没有任何地方发出它。D-175 要的第二个节点"客户卡在队列里没人处理"实际没接上。
   排队超时那条通知走的是服务器侧 timer `pojia-operator-watch`，与这个类型无关。
2. **没单的时候没卡，不会有任何通知** —— 缺卡只写进本机 supervisor 日志（而且同一原因只写一次）。
   所有 `upsertBrowserAlert*` 都强制要 `orderId`（`browser-alert-repository.js:25`），
   告警体系是订单驱动的，所以"库存空了"必须等客户来撞一次才有人知道。客户那次会白等。
3. **本机 worker 直接读工作区文件** —— 2026-09-12 17:58 的 6 次 `LANE_FAILURE
   (humanVerification is not defined)` 就是正在跑的 worker 加载了我改到一半的文件。
   当前代码已无此引用问题（`payment-executor.js:221` 用可选链、repository 侧是参数解构），
   但**改 browser-mvp/v1 代码前要先确认 worker 不在跑**，或接受它会即时加载中间态。
4. **同一 Session 连续失败没有熔断**——今天同一客户第 12 次和第 1 次一样照跑，客户前台干等 3 小时，
   Lemon 后台看它一次次失败。该有"同一 Session 连败 N 次就停、推手机、让客户换 Session"。**未做，等 Lemon 点头。**
5. **卡余额从未真正同步过**——`cards.last_transaction_synced_at` 全空，库内余额是开卡时的静态值。D-195 的
   `card-balance-refresh` 跑过但结果被付款确认覆盖（见上节 09:15 那单）。**判断卡够不够用时不要信库内余额。**

## 下一可执行项

1. **5371 那单已收口**（CDK `REDEEMED`、订单 `RECHARGE_SUCCESS`，13:58 UTC 独立核实）。**还欠**：取消续费（Lemon 去账号关 → 后台点确认）、
   卡台流水核对（token 在刷）、账本 1 笔状态错标（D-207）。
   **最重要的一条线索：Lemon 上号器一键登录 3.5 分钟付成，执行器 Cookie 注入探账号 403——同账号同窗口同出口。**
   EXTENSION 模式现成（`BROWSER_SESSION_PROVIDER=EXTENSION`，两个单测，无真单）。切不切 Lemon 定；切了下一单就是它的真单验证。
   **执行器下次跑任何一单，拿它的 stage 序列去对 D-206 那张轨迹表**——这是"为什么人能过机器不能"唯一靠得住的对照。
2. **免费试用 offer 的业务口径**（D-190 第五节，**在 Lemon 手上**）：客户账号有免费试用资格时算怎么回事 ——
   客户自己能白领一个月、我们收钱只能替他点免费按钮算不算交付、要不要在兑换入口就检测。
   它阻塞客户页与执行器的设计，未实施任何改动。
3. **报错单自动处置第二步**（第一步已完成）：把卡台交易证据接进验证 lane + 补判定矩阵 + 节流。
   见 ROADMAP。样本够了之后由大脑主动找 Lemon 拍板。
4. 上面三条缺口按 1 → 2 → 3 的价值排序修；缺卡提前通知（缺口 2）最直接影响客户体验。
5. 操作入口统一（`docs/UX_PUNCHLIST_2026-09-10.md` 第 6 节）、飞书机器人远程处理后台按钮
   （`docs/ROADMAP.md`）—— 均已记录，未排期。
6. 待办（不阻塞）：Dqcn 单取消续费；本机会话记录里出现过 `DATABASE_URL`（含密码），建议经 Lemon 确认后轮换。

## 已定不做

- **不实施任何绕过或自动完成人机验证的方案**（D-153/D-154），不做设备身份轮换（D-165）。此条不因重复要求而改变。
- 成单后自动登出客户账号：Lemon 决定暂不做（D-165）。
- Pro 5X/20X 全部搁置（D-146）；不买住宅出口（D-142）；不调研商用/分销/礼品码渠道（D-154）；不做大而全（D-147）。
- 卡段拒付标注中，尝试次数 < 3 只标「样本少」，不下结论（D-169）。

## 运营自助（无需大脑在场）

```bash
bash browser-mvp/scripts/ready-check.sh pay         # 现在能不能接单，一屏看完
bash browser-mvp/scripts/run-stats.sh               # 真实成功率
node v1/scripts/verify-and-return-cdk.mjs <CDK>     # 核实没扣款后退回卡密；加 --apply 才真退
node v1/scripts/check-card-charge.mjs <订单号>       # 单独查卡台有没有扣款
```
执行器是常驻的，客户任意时间兑换都会自动走到真实付款。两级停止：后台关付款开关（订单停在付款前）；
或 `launchctl unload -w ~/Library/LaunchAgents/com.pojia.browser-pool.plist`（整个常驻停掉）。
手机收到「充值需要人工验证」→ 去 Pilot 窗口点掉那个验证，自动化自己继续。

## 收尾自检（说"做完了"之前必须跑）

```bash
scripts/wrapup-check.sh                      # 工作区/推送/现场一致/接班一屏是否过期
browser-mvp/scripts/contract-probe.mjs       # 卡台字段契约（改动涉及卡台时跑）
```

## 暂停 / 恢复

```text
暂停原因：无阻断。5371 那单**已收口**（D-206：Lemon 上号器手动付款，客户确认 Plus，CDK 已锁 `REDEEMED`）；**账本 2 笔状态错标为 `RELEASED`（3159、5371），用量因此少算，修法见 D-207**。
         可分配卡 1 张（5371，库内 $34.28，卡台实时值待 token 刷新）。执行器在跑（PID 10503），现场干净（非终态 0、槽 0）。
当前 release `20260913-orderno-6dcb458`。**9 个文件未提交**（D-203~D-205 代码 + 测试 + 三个脚本修复 + DECISIONS + evidence/），
         Lemon 没说提交就不提交。
**四个决定在 Lemon 手上**：①**执行器切不切 `BROWSER_SESSION_PROVIDER=EXTENSION`**（他一键登录能过，注入不能，同账号同窗口同出口的对照，D-206）；
②同一 Session 连败要不要熔断（缺口 4）；③账本 2 笔错标怎么补（D-207 建议：新增 RECONCILIATION 行，不动 RELEASED 行）；④免费试用 offer 口径（D-190 第五节）。
允许继续：只读核对；browser-mvp/v1 代码与测试；文档落盘；发布
禁止操作：不实施人机验证绕过；开卡/补余额/换卡/提现等资金动作仍需 Lemon 当次确认；**worker 在跑时不碰 Pilot 窗口**
恢复第一步：读本文 → 读 D-206 轨迹表 → 读 D-172 → `ready-check.sh pay`（现在会诚实报"查不到"而不是"0 张"）
下一步：收口 5371 那单 → 等执行器下一单拿 stage 对照 D-206 → 定熔断口径
```
