# 接班一屏（HANDOFF_NOW）

更新：2026-09-21（UTC+8）。**第⑥步仍在发布前收口；四页本地实现、CDK用户试用、J-01～04修复及两项恢复修复已完成本地验证，未发布。**

## 当前下一步

- **D-328：用户同意最小容量修复、真实落库/恢复验收后继续发布。** 058与迁移恢复保护已本地实现；10项真实MySQL检查通过，默认980 pass/0 fail/69 skipped。更正：生产实际存摘要544字符，展示报告31348字符不是存储内容；保持只存摘要。新候选待固定后继续D-327发布，原66bfe98准备包不复用。证据`docs/reviews/2026-09-21-report-capacity/report.md`。
- **D-327发布尝试已在迁移前中止，原⑤b已恢复营业。** 候选66bfe98已推送/prepare，1249项manifest通过，两份加密备份通过；但现场发现日对账任务ER_DATA_TOO_LONG：生产app_settings.setting_value=VARCHAR(255)，只读报告31348字符，完整JSON落库不下。此前只读验收没有证明定时保存。需要先确认新增容量修复，不能继续switch旧候选。
- **07:00 UTC独立核实恢复**：release仍20260918-step5b-b0a36d4，web/worker/bark active、ready200；9timer恢复active/enabled，accept=true，付款/派单原值未变；schema054，迁移账号Super_priv=N。**055～057/临时授权/switch/新ops安装全未执行。** 接单暂停06:52:41→06:59:38 UTC，正式服务审计齐全；无在途/活动账号槽/未知资金。生产日对账旧故障仍未修，别宣称全部健康。
- 执行记录`docs/reviews/2026-09-21-step6-release/report.md`及prepare.log/maintenance-snapshot.json。服务器检查点`/opt/pojia/maintenance/20260921-step6-66bfe98/phase.json`为restored-before-migration；准备好的release和bundle已存在，不能重复prepare到同目录。备份与旧ops副本保留。`migrate-with-revoke.sh`是未执行的受控wrapper草案，不是授权/迁移证据。
- 两项UI建议已由用户明确不改：成功率口径知道即可、金额框标签暂不调，不作阻塞。D-294数字墙布局延期、D-323付款开关不改继续有效；不要重开改版。

- **最新D-326：用户“没问题”确认诊断交互稿后，正式接线已本地完成，未发布。** 新diagnostics.js/css按运行状态→待核对/逐卡→按单排查→低频工具组织；保留原Browser控制、配置/CSV、订单/卡片详情。付款不明通用关闭后端409，UI统一去订单正式收口；普通记录仍可关闭并留结论。逐卡报告消费既有只读数据，不增加判定规则或自动补账。
- **证据**：`docs/reviews/2026-09-21-diagnostics-implementation/report.md`。24项隔离检查通过，含4次伪造通用关闭409且DB不变、诊断出发4条Plus正式收口、原码重提幂等、卡资格、报告/详情/错误重试/配置保存/CSV。默认1048 tests / 979 pass / 0 fail / 69 skipped；既有未知付款MySQL12/12。9项1440几何契约通过，gap变异被拒；1440/390无整页溢出，手机标题已分行。不是生产或整个V2验收。
- 原型`step6-diagnostics-interaction.html`外部CSS已冻结，仍为8899演示；原用户CDK演示8804保留不重启。没有新长期服务。历史2条OPEN case关联CLOSED订单不自动清理；Pro/付款开关/browser-mvp/迁移SQL均未动。发布候选必须纳入诊断修正，旧5e7e717不含它，固定值以发布方案为准。

- **D-324恢复检查修正已本地实现并验过，生产未安装。** `pojia-ops restore-test`必须通过新验证器的业务读取、代表性Session/CDK解密、057合成告警轮次检查才报OK。只在准确的无网络恢复容器内补已知DEFINER，ACCOUNT LOCK+仅告警表SELECT/UPDATE/TRIGGER；未知身份/规则不符/已有账号权限不足拒绝，不扩权。正式shell真实恢复+负例14项通过，默认961 pass/0 fail/69 skipped。证据 `docs/reviews/2026-09-21-restore-fix/report.md`。**D-323付款开关不改。**
- **付款不明四条页面→真实MySQL链已通过**：API/Browser × 已扣/未扣。工作台→详情→确认框真实提交，核对订单/资金/账本/卡/case/告警；未知不退码、不复用卡，已扣不退码，未扣原码可重提且重复提交只建一单。卡复用跑正式资格SQL，不靠inventory_status猜。12项隔离检查、既有MySQL12/12、定向单测37/37，无跳过；不是全项目/生产真钱验收。
- **合成备份功能恢复已验，不能宣传生产灾备整体完成**：此前负例1449与合成解密/getOverview已证实；D-324将检查接入正式恢复脚本，权限进一步收窄到告警表。生产备份代表性解密、异地副本与独立密钥仍待验。新统一发布候选必须包含D-324，不再直接使用不含该修正的2d41192；固定值见发布方案。
- highvcc与客户密文保留策略仍按CURRENT_STATE及上轮观察处理，不因隔离测试而认定已恢复或已清理；D-291延期项不重开。原型依据D-283/284工作台C、营业条乙-3，CDK不重做。

- **发布方案等待执行确认**：`docs/tasks/2026-09-21-step6-release-plan.md`，须包含新恢复检查及运维入口安装。拟对专用迁移身份临时授SUPER，正式迁移后撤回，不改应用权限/全局变量。本机已实证撤权后触发器正常、重复迁移通过；生产未授权/DDL/prepare/switch。下一次确认须覆盖推送/备份/维护/授权撤回/迁移/切换及运维入口安装，不能把本地修复当已发布。

1. 固定含058的新候选，先prepare及维护前旧备份功能恢复，再重查空闲条件进入维护；当前仍旧版营业，不能从“已经停单/授SUPER”接续。
2. 按D-328/D-327执行055～058、撤临时权限、正式日对账一次并读回、备份恢复/switch/独立复核后恢复原营业状态。两项UI不改。
3. 上线后才按既定计划用页面处理自用老码；数量重查，不沿用历史21。
4. 第⑥步收口后才到⑦产品与客户页、⑧清理和集成验收。真实客户/卡网发货及真实付款仍不能用本地测试代替。

## 最新修复（D-321）

证据：`docs/reviews/2026-09-21-prepublish-rehearsal/report.md` 末尾，修前/修后JSON同目录。

- **迁移恢复已修**：原055～057 SQL不改。正式migrate.js先拿数据库级迁移锁，检查整批权限与现有结构；权限不足零DDL退出。受校验逐项计划核对字段/索引/触发器，同定义跳过、缺失补做、异定义拒绝。SHA256绑定原SQL，未审变更拒绝。
- **迁移28场景符合预期**：完整/重复成功；原7个DDL断点+新15个逐项断点全部可续；错列/错索引/错触发器提前拒绝；并发执行拒绝；受限账号不足时零schema变化。仅055～057支持该恢复适配，不承诺旧迁移/未来迁移同样可续。
- **无卡Session恢复已修**：资金ACTIVE/UNKNOWN/SETTLED检查保留；有效任务租约不抢占。无卡只恢复/补建唯一ASSIGN_CARD，下游准备/提交以标记暂停；实际分卡成功才恢复规范去重键的下游任务，不复活无关DEAD任务。
- **真实MySQL6类恢复场景通过**：缺任务/已完成/过期租约→实际分到卡→CARD_READY→准备任务可领取；有效租约/UNKNOWN资金拒绝且不改订单任务；已绑卡保持原路径；重复提交不复制任务。
- 默认套件1027 tests / 958 pass / 0 fail / 69 skipped。定向单测19/19；不能把69条跳过或先前11条旧夹具失败当全MySQL套件已通过。
- 无browser-mvp/界面布局改动，未连接生产、未推送、未重启服务。29份专用测试库与一次性账号已清理，独立查剩余0/0，数据库全局配置未变。

## 已闭合的前一阶段

- D-319：用户在8804实际试用后确认“同意，没问题”，CDK已定页面/规则不重开讨论。
- D-320：J-01告警重开漏推→057轮次/迟到回调隔离；J-02今日订单失败态+重试；J-03移除退休72小时提示；J-04完整北京时间批次日期。均本地已修验，详见`docs/reviews/2026-09-21-step6-joint/report.md`末尾。
- 四页基础联动已验：设置水位保存→工作台与卡片同步、工作台生成→CDK出现；三份1440几何契约（卡片/CDK/营业条）通过。不是所有资金按钮/全部场景重验。
- 12条旧MySQL失败已归因：11条为旧夹具/退休预期，1条Bark真缺陷已修。正式大套件其余夹具未统一整理。

## 唯一事实源与生产边界

- 生产事实只看`docs/CURRENT_STATE.md`；当前state-check读取release仍为`20260918-step5b-b0a36d4`。此前发布就绪证据在`docs/reviews/2026-09-21-production-readiness.md`，03:27 UTC highvcc观察见CURRENT_STATE。
- 该次读取：线上仍⑤b、迁移054、没有非终态订单或活动Browser账号槽；迁移身份库级ALL、binlog开启/trust_creators关闭。057需要额外创建触发器权限，仍未授予/执行。
- 新预检会在整个055～057之前拒绝权限不足，避免半迁移；生产执行仍需批准，代码防护不等于生产权限已经解决。
- 生产备份只验过校验和/解密/gzip；本轮是合成数据功能恢复，不能混同。发布前新备份需含触发器；恢复必须重建其准确DEFINER/权限，不能机械删除账号。
- 回滚旧应用不能保证保留新CDK有效期/用途/通知轮次语义。失败优先停在维护状态，不自动删列/触发器或整库倒回。
- 实施代码与发布前文档已推送origin/main；生产未切新版本。当前生产以恢复后的CURRENT_STATE为准，不用旧PID。无新增本机worker，保留演示与原常驻进程；生产未启动恢复临时MySQL容器（迁移前已停止发布）。

## 保留的用户演示

- `http://127.0.0.1:8804/admin/`，`scripts/cdk-demo.mjs`，PID30087（exec会话29657），库step6_cdk_demo，本机容器端口54186。按用户要求保留，不当残留清理；下次使用先重查进程/端口。
- 只注册CDK操作与订单只读，不能真实下单/开卡/付款，合成码不可发真实客户。原8803/8899及常驻worker不动。
- 本机隔离密钥配置路径见`.claude/launch.json`的ui.env，同目录parity-pw仅用于演示登录；凭据不入库。已有码恢复需同一套密钥。

## 已定业务与其他遗留

- 最终CDK任务书：`docs/tasks/2026-09-20-impl-step6-cdk-page.md`；D-314～318：普通发码零必填备注，应急另标并事后补登记，卡网可不过期；默认生成起30天，可自设/延期；不撤销发出回库存、不做复杂拆批结算。
- 四格：待兑换/应急备码/已成功交付/要看一眼。前两格排除过期/在途/作废；待兑换不等于已售、备码以登记为准，历史异常不按updated_at判断。
- 其他遗留维持：cardSources的__error消费；后端文案闸门；今日花费可能负数；5501/7402账目搁置；“余额已用尽”措辞暂不处理；每卡次数按产品仍待⑦前补齐。日对账逐卡明细本地已由D-326闭合，不再列为未实现。不要借检查扩范围。
- 方向和资金/生产动作需当次确认；不能凭“技术上能执行”改生产，也不要为了全绿删保护断言。
