# 接班一屏（HANDOFF_NOW）

更新：2026-09-18 16:3x UTC+8（08:3x UTC，第⑤步全部完成：发布 + 装 timer + 生产只读复验）。按 CLAUDE.md 约定维护，接手者从 main 继续，不另起项目。

## 现在是什么

- **V2 落实 8 步：①②③④⑤已上生产并验完**（第⑤步 release `20260918-step5-740bc1d`，08:29:00 UTC 切换，**无迁移**），⑥任务书已写（`docs/tasks/2026-09-18-impl-step6-workbench-and-settings.md`）。
- **第⑤步做了什么（D-271/D-272）**：
  - **坐实了 D-176 静音为什么没生效**——代码没错，是 `pojia-bark-notifications` 这个常驻进程**不在发布重启名单里**，五天一直跑旧 release（发现时落后 current 四个版本）。同一个洞 D-220 修过一次，当时只修了 worker。`deploy-release.sh` 已加上一起重启并打印 `bark cwd=`，**本次发布已闭合**。
  - 推送**排除法 → 白名单四类**（`v1/src/domain/alert-push-policy.js`），不推的 9 种逐条写了理由；`enqueueOpenAlerts` 里那条没有类型过滤的「复活」路径也补上了。
  - **三个新告警产生点**（以前都只改状态、不叫人）：token 失效、卡台故障、**拒付**（生产 4 笔拒付、0 条告警）。
  - **日对账**（次数 / 金额分开）+ 判据重写 + timer + 后台只读端点；发布前只读实跑两次，**11 条差异里 9 条是判据自身问题、当场修掉**，剩 2 条原因未知。
  - 钱包预检扣掉 highvcc 押金（Lemon 定 `usdDeposit` 不能花）。
- **测试**：v1 全量 **885 项 819 通过 0 失败 66 跳过**（基线 842/776/0/66，+43）；`sql-probe` 609 条 0 失败 + 插值 SQL 单独 PREPARE 通过。`git diff -- browser-mvp/` **为空**。
- **发布后三个常驻服务全部换到新 release**（新连接独立核实）：web 1202546 / worker 1202551 / **bark 1202626**——**bark 第一次跟着发布换代码，D-271 那个洞闭合**。
- **日对账 timer 已装**（Lemon 批）：下次 2026-09-19 04:01:19 UTC；首跑 08:29:39 成功，心跳已写，推了一条汇总。
- 生产：接单 / 派单 / 付款开关 true；Plus 走 API；可分配 2 张（hnskj 那两张出了 15 分钟同步窗口，库存口径仍 2+2）；非终态 0；常驻 Browser 池 **PID 67131**。具体值只看 `CURRENT_STATE`。

## 证据从哪里看

1. `docs/V2.0_EXECUTION.md` **§6 第⑤步那节**：静音根因的完整证据链、A/B/C 逐块证据、日对账两次生产实跑的原始输出、前置核查五项、范围外发现 12 条。
2. `docs/DECISIONS.md` **D-271（静音根因）/ D-272（Lemon 定的三件 + 判据）**。
3. `docs/RUNBOOK.md` **§2.7**（推送白名单怎么看、日对账怎么跑怎么读）与 **§5**（发布后必须核对 `worker cwd=` 与 `bark cwd=` 两行）。
4. 契约表三 `docs/contracts/2026-09-18_human-intervention-points-contract.md` 末尾新增的「第⑤块落地」段。

## 接下来做什么（顺序）

1. **开第⑥步窗口**：任务书已写（`docs/tasks/2026-09-18-impl-step6-workbench-and-settings.md`，工作台 + 设置页，UI 先出 2~3 版给 Lemon 挑）。它带着前五块归过来的 7 条发现。
2. **明天（09-19 04:01 UTC）看一眼日对账第二跑**：1657 / 3159 那两条差异如果还在，会变成 `persistent` 并把汇总升成 critical——那是设计如此，不是新问题。
3. 真单来时顺手看两件（都还没有样本）：分卡当场同步在 `provider_calls` 留下 `order-demand-sync:` 前缀的两条读；付款后备用卡台真证据路径被 worker 自动调用。

## 已定不做 / 别再重开的

- 待销到期**不单推**，并进每日对账那一条汇总（D-272 ①）。
- highvcc `usdDeposit` 是押金、**不能花**，预检要扣掉（D-272 ②）。
- 运营手动用卡**要做登记入口，归第⑥块**；在它之前日对账把这类扣款列「待登记」、不算差异、不猜原因（D-272 ③）。
- 成功（`BROWSER_ORDER_COMPLETED`）**不再推手机**——D-249 的「其余只进后台」覆盖 D-176 的「一头一尾」，成功数改进每日汇总。
- 资格规则不改（D-266 a）；水位口径库存口径（D-259）；卡台↔支付方式解耦已定（D-253）；待销清单不建表；自动销卡推 V3（D-232）。
- ZZSHU 零原因失败「停单不退码」本块仍没做，与队列一起归第⑥块。

## 未验证边界（别说成已完成）

- **白名单的生产复验有限度**：只证明了「白名单类型能推（那条对账汇总已 SENT）、白名单外 6 种同期新增 0 行」。**「白名单外的遗留 PENDING 行不会被 claimNext 领走」没有生产样本**（生产当前根本没有 PENDING 行），只有单测。
- **三个新产生点（token 失效 / 卡台故障 / 拒付）代码在生产但都还没触发过**——要等真的 token 过期、卡台故障或拒付才知道文案与去重对不对。
- **押金预检没有真实样本**：要等 highvcc 钱包够开卡时才会走到那条分支。
- **日对账剩的 2 条差异原因未知**：1657 差 -1.00、3159 差 -1.70，两张都是 AVAILABLE 的 highvcc 活卡、次数对得上。不猜。
- **`funded_amount` 对 highvcc 卡多数不可信**（5501 funded 2.00 / 扣款 159.16 等），原因未知 → highvcc 侧金额对账目前只对新卡成立。
- **highvcc 卡实查 16 张，`CURRENT_STATE` 原写 17 张，差 1 未查清**（已按 16 更正并标注）。
- 第④步那四件仍无真单样本；`plausiblePlusAmount` 只认 Plus 价位（第⑦块）；rehearsal 已跑通但备用卡台真证据路径演练走不到；集成测试既有失败（D-258）。

## 分支、运行与禁止事项

- main 是接手入口；本窗口改动已提交。隧道 13306 保留。
- 常驻 Browser 池 **PID 67131** 不要当残留杀掉（上一版 HANDOFF 里写的 74272 是过期值，已更正）。
- 付款未知禁止重付/换卡；开卡 / 补余额 / 切路线 / 发布 / apply / 改开关 / 装 timer / 重启 worker 或推送进程 **先开口问**；范围外发现只报不改（D-254）。
- **每次发布后核对 switch 打印的 `worker cwd=` 与 `bark cwd=` 两行都指向新 release**（D-220 / D-271 同一个洞踩过两次）。

## 暂停/恢复记录

- 本窗口「先问再动」停顿：①发布与装 timer 都先开口问、Lemon 当次批准后才做（在那之前日对账两次实跑都是 `--dry-run`，无任何生产写操作）；②任务书 C.2（`CHECKOUT_NAVIGATION_FAILED` 诊断）落点实查在 browser-mvp 内 → 本块不碰，归第⑦块，只报。
- **中途停下来查清的两次**：①发布复核时 grep 结果看着像「新代码没进包」→ 查清是我 grep 错了关键词，SHA256 逐字节一致才继续；②switch 打印 `bark cwd=/` → 新连接独立核实发现 bark 其实换对了，是**我自己刚加的那行**在 restart 后立刻取 MainPID 拿到 0（已修成等到有真 PID，账本发现 13）。
