# 接班一屏（HANDOFF_NOW）

更新：2026-09-17 09:50 UTC+8（2026-09-17 01:47 UTC 现场核对）。按CLAUDE.md原约定维护，接手者从main继续执行，不另起项目。

## 现在是什么
- **2026-09-17 救火（本轮）**：Plus 已从 Browser 切回 **API**（`LEGACY_HNSKJ_ZZSHU_V1` 接单、Browser 停，事件 55e62934）；hnskj 开卡恢复——默认卡段 `default_card_type_id` 18→23（卡台 09-14 后换新段致旧段失效，D-241）、开出 1 张卡 5276（16 刀、可分配 1）、账户余额 106.06→89.48；worker 开卡写开关曾临时开→**已回滚 false**（手动开卡靠 `card-stock-job-runner.js` 按需跑，不靠 worker）。详见 D-241、CURRENT_STATE。
- 当前上线业务提交4334dc2，release `20260916-unified-4334dc2`；具体生产值只以CURRENT_STATE为准。
- **用户最终决定：Plus确认→取消自动续费并完成现有内部核对→客户成功。取消细节不展示客户。**
- 曾上线提前成功b31a88a，因收益未量清且增加恢复复杂度已撤回；不要照旧方案重新实现。
- 保留：核验去重复、付款后结果只读检查修复、有界只读重试、结构化诊断、真实付款子阶段及2秒查询、正确完成时间戳。
- 本次只做文档整理/只读核对：当前release与服务正常；接单/付款true，活动run0、非终态0；统一成功发布后新订单0。没有真实新版本验收样本。

## 证据从哪里看
1. `incidents/unified-success/README.md`：最终版本实现、测试及prepare/switch/SQL证据入口。
2. `tasks/2026-09-16-D240-delivery.md`：此前改造记录，顶部已标早交付方案撤回；其旧测试不替代最终版本测试。
3. `incidents/d240-followup/findings.md`：遗留问题；早交付专属状态冲突因撤销入口处置，旧凭证期限等仍待处理。
4. `incidents/d240-followup/early-delivery-benefit.md`：为什么历史记录无法算取消耗时，不许把落库间隔当外部动作时长。
5. `DECISIONS.md` D-240末尾是最终决定，前面条目为历史；过程在HANDOFF_LOG末尾。

## 接下来做什么
0. **V2 现方向：阶段 2.5 业务实际全审（2026-09-17 Lemon 定，D-242）**——文档层面 0/1/2 过审但没按业务实际终审；阶段 3 落实暂缓，先把六面方案（A/B/C，从 A 组起）拉到真实业务重审，"两卡台同等重要（hnskj≈highvcc、经常切）"这类真实逐条吃进方案。
1. 正常客户来单后核验实际全链路；需要测时就分别记录首次Plus、取消结束、订单成功和客户响应，不擅自付费造单。
2. 对旧Session期限阻断付款后恢复、恢复诊断缺失、备用卡独立交易证据不足按真实证据修；不要继续扩建已撤销的早交付状态机。
3. 本轮不等于整个V2完成。继续V2前读V2.0_EXECUTION §6，并对照本轮已改代码避免重复；其余优先级依最新决策。

## 分支、运行与禁止事项
- 主工作区main为接手入口。4334dc2之后的文档提交不是再次发布。
- ai-recharge-d240/release/unified/unified-release四个隔离worktree仅作追溯；所有最终变更已入main，不能从旧worktree直接发版；没有在这些目录启动业务worker。
- 常驻Browser是正常生产服务，不要按“残留worker”杀掉。发布前仍须重新查在途付款和实际进程，不能用本文旧PID猜。
- 当前没有额外自动监控/付费验收任务。普通诊断不等于授权新充值、重付、开卡、补余额或再次发布。
- 付款未知禁止重付/换卡；不改写历史证据；备用卡现有内部标记核对不等于卡台独立交易对账。
- 403上游原因仍未知；只读重试不等于根治。统一成功只删除早交付触发条件，不代表所有恢复问题已修复。

## 暂停/恢复记录
- 统一成功发布曾断在prepare结束，09-16 15:36 UTC已续完switch/执行器同步/恢复接单。
- 现在没有未完成发布、未恢复的维护开关或待合并的本轮业务修改；剩余是上述真实验证与遗留事项。
