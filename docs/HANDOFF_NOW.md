# 接班一屏（HANDOFF_NOW）

更新：2026-09-18 00:2x UTC+8（2026-09-17 16:2x UTC，落实第②步窗口收尾）。按 CLAUDE.md 约定维护，接手者从 main 继续，不另起项目。

## 现在是什么

- **V2 方向工作已全部完成**（D-243 步①②③）。**落实（步④）按账本 §3.A「做的顺序」8 步走，第①②步已完成。**
- **第①步 C2 已出结论**（D-253）：ZZSHU 按 BIN 白名单放行、拒 highvcc 卡 40020 → Lemon 定「API 固定 hnskj、不加 BIN」。解耦结论 = Browser 任意卡台 / API 固定 hnskj。
- **第②步「数据源三件」代码已上生产**（release `20260917-datasources-c1a026a`，2026-09-17 15:28:44 UTC）：
  - **T1** highvcc 授权流水入 `card_transactions` + 钱包入 `provider_balance_snapshots` — ✅ **生产已验证**（2026-09-17 15:52:25 UTC，22 笔 / 9 张卡 / 0 归不到，钱包 $23.68）。
  - **T2** 开卡真实成本记账（D-255 Lemon 选 B「观察余额差」而非按费率表推算）— 代码已上线，**真实开卡时才执行，至今没跑过一次**，第③步两台各开一张时才算验收。
  - **T3** 账本补记 — ✅ **已 apply**（Lemon 同意）：7 单补记、Dqcn 按证据规则拒绝。账本 CONSUMED 12→19。
- **browser-mvp 一个字没碰**（D-254 边界）：`git diff --stat 4334dc2..HEAD -- browser-mvp/` 为空。
- 生产：接单/派单/付款 true；Plus 默认路线 API（301=1）；305/306（Pro）接单位仍关（D-245）；非终态订单 0；本机 Browser 常驻 PID 47905 未动。具体值只看 `CURRENT_STATE`。

## 证据从哪里看

1. `docs/V2.0_EXECUTION.md` **§6 第②步那节**：发布/测试/对真实响应的核实/T1 生产实证/T3 apply 复核/10 条范围外发现，全在那里。
2. `docs/V2.0_EXECUTION.md` §3.A：五面定稿 + 补丁 + 落实顺序（唯一有效的方案）。**顶部有阅读指引：§2/§4/§5 是 09-15 过程证据，与定稿冲突以定稿为准，不凭旧行号改代码。**
3. `docs/DECISIONS.md` **D-255 ~ D-258**：T2 选 B；补记 8→7 单及 apply 复核；对账判据的实测推翻；集成测试立项。
4. 提交：`4f5e158`(T1) · `50384e2`(T3 脚本) · `c1a026a`(T2) · `69a67fe`/`e07227b`(落盘)。

## 接下来做什么

1. **第②步已完成，直接开第③步窗口**：任务书 `docs/tasks/2026-09-17-impl-step3-card-source-and-supply.md`（卡台选择表 + 供卡调度）。它会碰 browser-mvp 两处，白名单已在任务书里列死。
   开卡时**顺带验 T2**：两台各真开一张后查 `card_transactions` 里有没有 `CARD_ISSUE_FEE` 行且金额合理（预期 hnskj $16 卡约 $0.58）。
2. 之后按账本「做的顺序」④～⑧逐块开窗口；每块做完放回主线验。
3. 运营侧待 Lemon：每天固定时段贴 highvcc token（D-251）；两批旧卡标终态在第③步一并做。
4. 正常来单仍按 RUNBOOK；当前 Plus 走 API + hnskj。
5. **已立项、未排期**：数据库集成测试 14/43 失败（D-258）。不挡 V2 落实，但挡「发布前能不能真验证」，越晚查越亏。

## 已定不做 / 别再重开的

- 卡台↔支付方式解耦：**已有结论**（D-253 补记），别再当开放问题讨论。
- T2 不按费率表算（D-255）：费率只作对照，代码只信余额差。
- `card-consumption-audit.js` 的判据：**排到第⑤步**。注意 D-257 已实测推翻「放宽白名单即可」的说法，那块要按真实口径重新设计。
- 305/306 在 Pro 落实（顺序第⑦步）前不重开。

## 未验证边界（别说成已完成）

- **T2 尚无真实样本**：代码在生产，但只有真开一张卡才会执行，**至今一次没跑过**。第③步开卡时才算验收。
- **T1 的「抽一张卡与卡台后台一致」还没让 Lemon 看**：库内数字已与手工比对的 22 笔逐卡吻合，但任务书要求的「Lemon 看一眼卡台后台」没做。
- **Dqcn 那单为什么在 7402 上找不到流水，原因未知**（D-256），Lemon 定先记着不查。
- **数据库集成测试在生产代码上就有 14/43 失败**（D-258，已立项未修）：发布门槛实际过不了。第②步已确认没引入新失败（当前版独有失败 0 条），但这个既有问题还在。
- **对账判据要认真设计、不是放宽白名单**（D-257，推翻我先前的建议）：符号不统一、PENDING 也是真实扣款、有系统外人工用卡的无主流水、还有反向缺口。归第⑤步。

## 分支、运行与禁止事项

- main 是接手入口；本窗口提交已全部推送 origin。本窗口起的两个临时 docker 容器与 `/tmp/pojia-baseline` worktree **已清理**（本机另有两个 `pojia-stage1-mysql` / `pojia-funding-candidate-*` 容器不是本窗口起的，未动）。
- 常驻 Browser（本机 PID 47905）不要当残留杀掉。
- 付款未知禁止重付/换卡；开卡/补余额/切路线/发布/账本补记 **先开口问**。
- 范围外发现只报不改（D-254）：写进收尾「发现」段 + 登记账本/DECISIONS，修不修 Lemon 定。

## 暂停/恢复记录

- 本窗口无暂停。发布后 timer 按小时正常跑。
