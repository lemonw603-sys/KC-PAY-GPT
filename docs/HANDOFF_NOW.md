# 接班一屏（HANDOFF_NOW）

更新：2026-09-20 UTC。第⑥块进行中：**工作台 / CDK / 卡片页 D-280 ①③④⑤⑥⑧ 已完成**，全部**未上生产**。**D-280 八条全部完成**（D-288/D-289）；**设置页已完成**（D-290，B「一张大表」）。**第⑥块代码层面做完，全部未上生产。** **下一步已变（2026-09-20 晚）**：Lemon 看过真实页面后判定**卡片页、CDK 页「完全不合格，没有任何整体设计」，要重做**；工作台整体认可；**营业条已定稿为乙-3「一条工具栏」**（D-293，条高 117→56px）。**工作台还有一轮**：数字墙重做（Lemon 判定排版最扎眼，且里面写了 `口径待定（D-284 ③）` 这类内部编号，违反「界面不写解释旁白」）、「卡与钱」扫不出重点、标题结构重复五遍。**三页做对之后再上生产**（D-291 的「先上生产用一天」顺延到重做之后）。**本轮建立了「视觉一致性比对」机制（D-292）**：界面像不像设计稿改由 `scripts/visual-parity.mjs` 比数字，不再靠肉眼看截图——做法见 `docs/design/VISUAL_PARITY_PROTOCOL.md`，**重做卡片页/CDK 页时各补一份 `docs/design/parity/` 契约**。发布必须连同迁移 055 一起做。接手者先读 AGENTS.md 四份顺序 + `docs/tasks/` face-1~5 账本再动手。

## 生产版本

- 生产 = **`20260918-step5b-b0a36d4`（⑤b 旧后台）**，第⑥块**一行 UI 都没上生产**。
- **迁移 055 未应用生产** → CDK 新端点在生产会报字段不存在。**发布必须连同迁移一起做**，迁移前单独确认。
- 本机：**8803 验收服务在跑**（step6_demo 隔离库，供视觉比对与页面验收用；不用了就停）；常驻 Browser 池 **PID 67131 未动**，别当残留杀。

## 已完成（都经真实页面 + 隔离库验过）

**工作台**（已推送）：付款不明一条链闭合（含自己挖出的 **B1**：Browser 收口原本不关自己的 case/告警）；审查两批全清；**D-285** 数字墙与队列按原型 C 恢复 → F-64 闭合；**营业条方向 A**（修好 `state.rechargeMethod` 从未赋值导致 `VERSION_MATCH` 恒失败的「永远切不动」根因）。界面层验收路径沉淀在 `RUNBOOK §2.8`。

**CDK**（已推送）：D-279 ② 码前缀按产品；**D-286** 交付负债可见 + 码有效期/下线保护（迁移 055，只加列）；四端点端到端验通（owed / stock 真的分开）。

**卡片页 D-280 ①③⑤⑥**（本轮，A「台账优先」+ 候光皮，D-287）：
1. **① 两台并列四个数**：可分配/水位、钱包/底线、今日已开/日限、token。**可分配复用 `providerCardStockSql()`**（＝第③④块资格规则，与工作台同口径）；**底线直接读 `provider_accounts.wallet_floor`**——就是开卡预检挡开卡的那条硬底线（生产 hnskj 30 / backup-a 20），只读显示不给编辑入口（改它是资金动作，按 D-284 归设置页）。
2. **③ 在役列表 + 历史折叠**：状态复用 `classifyStockCardOperationalState`；补了 `createdAt` / `issueFee` / **`externalCardId`**（overrides 按 external_card_id 匹配，生产当前两列同值但不让页面依赖这个巧合）。
3. **⑤ 待销清单**：复用 `classifyRetirementRow` 的 due/notYetDue；到期才给按钮，确认词闸门保留。
4. **⑥ 手动用卡登记**：走 overrides（F-57 定的正确端点）——卡不再分配，但**仍留在待销清单里**。

**三层验收都做了**（不是只看 HTTP 200）：
- **业务层**（隔离库实跑 + 独立查库复核）：退役登记 → `inventory_status` DEPLETED→RETIRED + `CARD_RETIRED_CONFIRMED` 审计事件；手动用卡 → override RETIRED 写入且**卡状态未被改动**；设底线 → `app_settings` 落库；查余额失败 → 报 `highvcc_token_missing`、**不显示假余额**。
- **界面层**：真实浏览器 1280 宽与比稿 A 版逐项比对，一处差异（两台栏被 `.stock-grid` 挤扁）已修成整宽置顶。
- **工程层**：全量 **942 / pass 874 / fail 1**；新增 `test/admin-cards-page.test.js` 13 条（重点是诚实性断言：没设的底线不许显示成 $0、读不到的待销不许显示成「没有待销的卡」、拿不到的可销时间不许显示成「—」）；vm harness 抽到 `test/helpers/admin-dom-harness.js` 两处共用。

**审查批次6 已处置**（`DISPOSITIONS.md` 批次6）：#1b 采纳已修——「今天(UTC+8)」原来还有两处手写（`admin-read-service.js` 今日订单计数与 TODAY 过滤），已收编进 `todayCst8WindowSql`，加了扫描 `src` 全树的防回潮守门测试并**反向验证过它会抓人**；#2 部分反证——highvcc 余额字段有生产实读依据（`highvcc-transaction-wallet-sync.test.js:184`），代码不改，真正没验的是成功分支端到端外网跑通；#6 确认属实、留观察。

**第二轮（D-288）**：② 每台「开卡…」+「刷新这台」（开卡只展开既有折叠区，不另造花钱入口；新增 `POST /backup-cards/highvcc/refresh`）；④ 点尾号看流水，订单只认账本记了交易号的（生产 76 笔中仅 7 笔能关联）；⑧ 导入备用卡降为折叠高级入口。**highvcc 余额改全部按需查**——原先随页面加载自动打外网，已验证改后「加载 0 次 / 展开开卡区 1 次」。**⑦ 全部完成**：卡台切换已搬工作台（先补齐「同时接管」三态与三条安全行为，再把卡片页改只读）；补余额区块已退休（连同工作台那条会跳过来的待办，Lemon 定）。**⑦ 只剩「卡台切换搬工作台」待裁**——卡片页的切换支持「同时接管 N 单」（卡台断供时把排队单改指新卡台，只接管完全没碰过钱的单），而工作台写死不接管；要搬得先把这个能力补过去。生产当前 0 单符合接管条件。

> **2026-09-20 事故必读**：本轮删代码时用「下一个 `async function`」当边界，误删了 **14 个顶层事件绑定**（侧边栏导航、订单筛选分页、导出、对账表、document 委托），**951 条测试全绿而后台点不动**——`node --check` 只查语法，测试走 snippet/harness 不碰顶层绑定。已按顶层块边界精确恢复并加守门测试（D-289）。**删代码按语法边界，不按字符串距离；UI 改完必须真点一遍。**

**设置页（D-290）**：策略表（台×产品：水位/开卡金额/每日上限）与钱包底线/告警线**第一次有了写路径**，此前只能改库；字段走白名单、值参数化、每改一项写 `admin_setting_events`；改完先变色出「保存」，点了才写（不做失焦即存——改错直接影响资金）。

> **两件必须记着的事**：① **每卡单数仍是全局值**，D-221 要的按产品（Plus 3 / 5X 1 / 20X 1）本轮没做，**5X/20X 自动化接入前必须补**；② **5X 最低余额应为 $95**（Lemon 定），生产现值 16.00 而 5X 开卡要 $100 —— **尚未改**，等设置页上生产后用页面改，改前单独确认。

> **D-291 对照规划核查**：第⑥块范围我原先理解偏了——任务书还含「D 归入本块的 7 条发现」，其中 **4 条仍未做**（放弃并放卡按钮 / 建演练单脚本 / bark 生产哑 / ZZSHU 停单不退码 / state-check 偶发失败，Lemon 定排到 ⑥ 之后）。**页面遗漏**：数字墙「今日花费」「异常支出」仍是聚合待写；卡用量只做了 Plus 未按产品；Session 门槛只读。**⑦ 前置**：8 步表 ① 的 `pro5/pro20` 映射验证并入 ⑦。

## 未验证 / 已知缺口（别说成已完成）

- **生产零验证**：以上全部在隔离库 + 本地服务；生产仍旧后台。
- **迁移 055 未应用生产**（见上）。
- **唯一一条有意留红的测试**＝`admin overview does not describe disabled automatic card opening as enabled`。查清后比原记录严重：**不是文案过时，是供给开关在后台根本没有入口**——`data-supply-toggle` 只有 handler、全项目无渲染处。这是 **F-65** 的真实表现，属供给控件范围，做供给控件时闭合。**不要为了全绿去改这条测试。**
- **旧批次明文可能取不到**：`listCdkCodes` 依赖 `cdk_batches.codes_ciphertext`，解不开的行 `code` 返回 null（不编造）；生产有多少这种批次未统计。
- **Browser 就绪态切换未验**：隔离库造不出就绪执行器。

## 下一步

0. **工作台数字墙重做**：先定「哪些指标真有数据」，没数据的撤下来，而不是摆「待接入」占位；清掉界面上的内部编号与开发备忘。
1. **卡片页重做**（Lemon：「完全就是在原来的基础上加了一点东西，完全不合格，没有任何整体设计」）。功能八条（D-280）已全部实现且验过，**重做的是整体设计，不是重写功能**。按 D-292 的做法：先出原型给 Lemon 挑 → 定稿后当轮补 `docs/design/parity/` 契约 → 实现 → 脚本比数字。
2. **CDK 页重做**（同上判定。此前「只修 bug、皮肤照旧」的选择已被 Lemon 推翻）。
3. **诊断页维持现状**，本轮不动（Lemon 定）。
4. 三页做对后**才**发布：四页 + 迁移 055，回滚点 `20260918-step5b-b0a36d4`；**发布与迁移各需当次点头**。
5. 上生产后用设置页把 **5X 最低余额改成 $95**（Lemon 定；生产现值 16.00，改前再确认）。
6. 然后 ⑦（Pro 同型 + 客户页四态）→ ⑧（删表）→ D-243 集成验收。**⑦ 的前置**：8 步表 ① 的 `pro5/pro20` 映射验证并入第⑦步。

## 禁止 / 注意

- 开卡 / 补余额 / 充值 / 付款 / 退款 / 切路线 / **发布 / 应用迁移** / 改开关 / 重启 worker **先开口问**；不碰 browser-mvp（D-254）。
- **发布门槛三件**：外观符合选定方案 + 数据字段口径正确 + 按钮业务结果正确。测试数量 / HTTP 200 / SQL 有返回**都不算数**。
- **原型必须 link `_frozen/` 里的定稿快照，不是实时样式**（D-293）：反代实时 CSS 会让原型跟着实现一起变，比对永远通过 —— 变异测试拆穿过一次。改完比对相关的东西，**一定要跑变异测试确认闸门还有效**。
- **CSS 只许降不许升**（D-293）：`node scripts/css-drift-check.mjs`，规范见 `docs/design/DESIGN_SYSTEM.md`。
- **界面「像不像设计稿」不许用眼睛判**（D-292）：跑 `PARITY_ADMIN_BASE=... PARITY_ADMIN_PASSWORD=... node scripts/visual-parity.mjs`。非要看截图时：**测量/截图前先断言 `innerWidth`**（视口 emulation 会被静默清掉，当轮两次因此看错）；放大只用 `transform: scale()`，**绝不用 CSS `zoom`**（它参与布局，会造出假缺陷）。
- 审查员未跟踪文件 `docs/reviews/STEP6_BASIS_REVIEW_2026-09-19.md`：**Lemon 定留着不动**；`wrapup-check.sh` 会长期报「工作区干净 ✗」，属已知有意。
- **本窗口过程教训**（前几条都是被 Lemon 或审查员发现、不是自查）：①**伪造过验证证据**；②fixture 自造 dedupe_key 自己跟自己对暗号；③**只测下游不测上游**；④落盘漏 HANDOFF 三件套；⑤**把「已知两条红测试」当结论沿用，实为六条**——接班交代的数字也要自己重查一遍。对策：**改一处立刻 `git diff` 核实**、**链路测试从上游入口进**、**界面层验收不可省**、**每轮提交前跑 `scripts/wrapup-check.sh`**。
