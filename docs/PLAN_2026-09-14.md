# 三天执行计划（2026-09-14，v3，按 Lemon 的三天时间盒 + 去时间化验收重写）

> v1/v2 用"两天冲刺 + 两周一阶段 + 连续 N 天不求助验收"，Lemon 2026-09-14 晚推翻（D-222）：
> 给硬时间盒——**09-15 两天冲刺 · 09-16 V2.0 · 09-17 V2.1 · 09-18 起 V2.2**——并明确**验收不许用时间**。
> 本文按此重写。决定权在 Lemon；DoD 与外部依赖据实标注，不用时间讨价还价。

## 1. 验收口径（D-222，本项目自此生效）

一个交付物**完成** = **它的行为可当场核实为正确**，由三类客观证据回答，**不由时间、不由 AI 自述**：

1. **针对性测试通过**——含对抗性夹具，不是只测 happy path（D-172 惯犯 C：274 测试全绿却对真 bug 零覆盖）。
2. **现场脚本一致**——`state-check.sh` / `wrapup-check.sh` / `ready-check.sh` 相关项全绿。
3. **生产只读复验**——新连接、新命令核对该行为。

**稳定性（跑久了没出事）是运营期观察，不作交付门槛。** 混淆二者，就是用时间掩盖没法证明做对了。

**「生产跑通」与「做对了」分开**：下面每天右列列出我交付不了"生产实跑"的部分（依赖卡台服务器 / BitBrowser Linux / 真实 5X 单）。那几处的交付验收是「代码对 + 测试绿 + 能当场验的行为」；「生产跑通」作为运营期里程碑，由 Lemon 判定何时算数。

## 2. 三天计划（每项带 DoD 与外部依赖）

### Day 09-15：两天冲刺压成一天——Browser 主线能跑真单且失败可诊断

> 今日 API 卡台服务器故障、开不了卡（Lemon 报），所以今天主线只能是 **Browser**（可分配卡 `1657` 一张，Plus 门槛够 1 单）。

| # | 任务 | DoD（可当场核实） | 涉及文件 | 外部依赖 / 前提 |
|---|---|---|---|---|
| T1 | A3 账本推算口径发布 + `funded_amount` 手动开卡入库记真实金额 | 生产 `card-inventory-eligibility.js` md5 = 工作区；补钱入库记真实开卡金额的单测；发布含 `restart pojia-worker`（D-220）；`state-check` 一致 | `v1/src/services/card-inventory-eligibility.js`、`card-stock-service.js:260`、`scripts/deploy-release.sh` | **前提：Lemon 关掉 `card_balance_recharge_enabled`（D-223）**；发布前问 Lemon |
| T2 | 诊断存库（D-214）：`fill-billing-email` 三种死因落 DB | 制造一次失败后，DB（`browser_run_events`）里查得到确切原因，不靠日志缓冲 | `browser-mvp/src/*`、`shared-runtime-integration.js` | 需一次真实失败样本；日常单量低，可能用 rehearsal 兜 |
| T3 | 切换校验（A2）+ 切路线解耦卡台（D-219 发现 5） | 单测：目标路线无卡→拒切并给因；切路线不再连带写 `browser_card_source_switch_events` | `v1/src/services/provider-route-admin-service.js` | 无 |
| T4 | 免费试用入口识别（决定①） | 单测对着 D-216 那单的页面文本；执行器认 "Start your free trial" 类入口 | `browser-mvp/src` adapter | 无（时间不够则挪 09-16） |

### Day 09-16：V2.0——执行面接口三段 + 切换解耦 + 资源面按产品 + 控制面 4 页

| # | 任务 | DoD | 涉及文件 | 外部依赖 |
|---|---|---|---|---|
| V0-1 | 执行面接口 `submit / verify / cancelRenewal`；APIExecutor 包 ZZSHU、BrowserExecutor 收现有 | 接口测试：同一订单分别走 API/Browser 都返回三段状态；旧单段 `execute()` 入口已删（`grep` 证明） | `browser-mvp/src/executor.js`、`workers/workflow-handlers.js` | 无 |
| V0-2 | 资源面按产品：`card_max_successful_payments`（Plus 3 / 5X 1 / 20X 1）+ `default_open_card_amount` 按产品，后者加后台端点 | 端点带审计；单测三产品上限；`default_open_card_amount` 可从后台改（现无任何入口） | `card-inventory-eligibility.js`、`create-app.js`、`card-stock-service.js` | 自动开卡"生产跑通"依赖 **hnskj 卡台恢复** |
| V0-3 | 控制面 4 页骨架：工作台 / CDK / 卡片 / 设置 | 4 页能加载、关键操作可点；旧 18 视图函数收敛 | `v1/public/admin/*`（2366 行前端） | 无（最重的一项，可能溢出到 09-17，见 §5 弱点） |

### Day 09-17：V2.1——5X/20X + 展示面四态 + 通知面 4 类 + 对账面 + 精简删表

| # | 任务 | DoD | 涉及文件 | 外部依赖 |
|---|---|---|---|---|
| V1-1 | 通知面 4 类 + 推送由类别定（D-219 发现 3） | 分类函数只输出 4 类；测试：缺卡必推、信息类不推 | `browser-alert-repository.js`、告警分类 | 无 |
| V1-2 | 展示面四态（加 `WAITING_FOR_SESSION`，D-219 发现 4） | 客户页测试覆盖四态 | `v1/public/assets/customer.js`、`domain/customer-stage.js` | 无 |
| V1-3 | 对账面：每日账本 vs 卡台自动比对，差异进队列（D-219 发现 7） | job 能跑；差异写进"需要我处理" | `scripts/card-consumption-audit.js`（现只读未接流程） | 无 |
| V1-4 | 精简：删 0 行表 / 死端点（`order_tags`/`refund_cases` 等，见 V2 §4.1） | `grep` 证明无残留引用；迁移删表 | 见 V2_ARCHITECTURE §4.1 | 无 |
| V1-5 | 5X/20X 进接口（`product` + `fromPlan` 两参数） | 代码 + 单测（Free→5X 直充、Plus→5X 升级两路径） | 执行面接口 | **真实跑通需真实 5X 客户单**（D-138 之前因量小不做，无样本） |

### Day 09-18 起：V2.2——Browser 迁服务器

**第一步必是 BitBrowser Linux 调研**（官方有无服务器版）——这是走"路 1（装服务器几乎不改）"还是"路 2（重做 runtime 层）"的唯一分岔，也是 V2.2 能不能按期的唯一前提。调研前不写迁移代码。

## 3. 200 单/天的算术（不变，按 D-221 每卡单数校验过）

```
Plus 单笔 $15.72 · 每卡 3 单（D-221）· 开卡 $50/张 + $0.5 手续费/张
200 单/天 → 67 张卡/天 → 开卡 $3,350 + 手续费 $33.5/天 → 约 $3,144 实扣/天
高峰 4 小时承载 140 单 → 35 单/小时 → 1 lane 最多 17 单/小时 → 至少 2 条 lane
```
5X/20X 是 **1 单/卡**，开卡金额按其单价（V2.1 定）。**67 张/天靠手点不可能**——卡供给自动化是达标后第一件事，
但它的"生产跑通"卡在 hnskj 卡台（今天就故障），代码解决不了，需 Lemon 和卡台谈配额与资金流。

## 4. 巡检清单归位（Lemon 问过：那些问题排在哪）

已按"阻不阻塞当前阶段"归入 §2：诊断存库/切换校验/账本口径/免费试用 → Day 09-15；F-42 EXTENSION 开关、卡供给自动化、账本 3 笔错标（RECONCILIATION 行）、D-215 重试计数与告警噪音、连败熔断（V2 以"同一 CDK 连败 N 次"实现）、缺卡通知 → 分入 09-16/09-17；夜间 rehearsal、多 lane、同步 1h→5min → V2.2/放量期。**明确不做**（本计划期间）：hCaptcha 绕过、设备身份轮换（D-153/154/165）、住宅出口（D-142）、多卡台自动回退。

## 5. 这份计划自己的弱点（据实，不藏）

- **控制面 4 页（V0-3）是 2366 行前端重写，一天做完 4 页最可能溢出。** 缓解：09-16 先做"工作台"一页（日常只看它），其余三页沿用 V1 到 09-17；若仍不够，工作台是唯一硬指标，其余降级。
- **多处"生产跑通"不在我控制内**：hnskj 卡台（今天故障）、BitBrowser Linux（未调研）、真实 5X 单（无样本）。这三处三天内我只能交付"代码对 + 测试绿 + 能当场验"，不能交付"生产实跑"。
- **Day 09-15 依赖一次真实失败样本**才能验 T2；日常单量低（4 单/天），可能攒不到，退路是 rehearsal，但 rehearsal 账号状态与真实客户不同，诊断可能不完全等价。
- **一个窗口三天做完 V2.0+V2.1，强度极高。** 见 §6 多窗口——建议开，但四份事实源仍只由大脑写。

## 6. 接班与多窗口（Lemon 问：后面模型能不能接手）

- **能接"在哪 / 下一步 / 什么不能碰"**：`AGENTS.md` 四份顺序 → `HANDOFF_NOW` → `PROJECT_MAP` → `CURRENT_STATE`，每份可用脚本对现场。
- **能直接开工的前提是任务书**：§2 每项的 DoD 已是机器可验形式；逐项拆成 `docs/tasks/<日期>-<任务>.md`（目标/验收/边界/涉及文件）。Day 09-15 的任务书本轮先建，后两天在前一天收尾时建（方案还可能微调，不提前铺开）。
- **验收不信自述、只信脚本与测试**——这是新模型大概率重犯 D-211 六类错误时唯一有效的兜底。
- **多窗口分工**（D-151/152）：大脑排序/验收/四份事实源/生产动作；执行 A 只碰 `browser-mvp/`；执行 B 只碰 `v1/` 且不碰付款路径。两执行窗口不同时改同一文件。**开不开由 Lemon 定；不开则控制面 4 页几乎肯定溢出。**

## 7. 需要 Lemon 现在拍板的

1. **多窗口开不开**（§6）——直接决定 V2.0 那天能不能装下控制面 4 页。
2. **补余额开关**（D-223）：Lemon 去后台真关，还是授权大脑走正式端点关。
3. **V2 六个面**（D-219 修正版）方向是否全认——若对某个面仍有异议，趁拆任务书前说。
