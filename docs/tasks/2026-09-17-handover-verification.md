# 任务书：新模型接手核对（2026-09-17）

> 这是 V2 后续推进的**第一步**（Lemon 2026-09-17 定，D-243）。目的：接手模型**真正接手**，不靠上一模型的转述。做完、Lemon 核对过，才算接手、才动账本。**开专门新窗口做，第一句读本任务书 + AGENTS.md。**

## 为什么要这一步
- 2026-09-17 审 A 组 5 项，发现账本 `V2.0_EXECUTION §3` 多处照文档推、与真实脱节（2 项地基错判、1 项大幅低估）——账本不可信。
- Lemon 担心新模型对项目了解片面、行为准则不清晰、不知道怎么正确接手，最后项目烂掉。
- 接手模型是从会话摘要接过来的：没自己读过代码全貌、没查过生产、没读过全部决策、B/C 组方案没看过。

## 要做什么（三件事，按顺序）

**① 自己摸一遍，不信转述**
- 按 `AGENTS.md` 顺序读事实源：`CLAUDE.md` → `docs/HANDOFF_NOW.md` → `docs/PROJECT_MAP.md` → `docs/CURRENT_STATE.md` → `PRODUCT_SIMPLIFICATION_DISCUSSION.md` 末尾基线 → `V2_ARCHITECTURE.md` / `V2.0_EXECUTION.md` → `DECISIONS.md` 近期条目（至少 D-219 起）。读到能复述"项目在哪、为什么"。
- **对着生产核一遍**：跑 `browser-mvp/scripts/prod-query.sh`/`state-check.sh`，CURRENT_STATE 与现场不一致的逐条列出。只读，不改生产。
- **自己走一遍代码主线**：一单从 CDK 兑换到交付的全链路（分卡 → 付款 → 核实 → 取消续费 → 交付 → 对账），六个面各管哪段、API/Browser 两条路线在哪分叉、两个卡台（hnskj 101 / highvcc 103）各在哪接入。落一张主线图（文字即可）。

**② 写一份"我的接手理解"给 Lemon 核对**（落 `docs/tasks/2026-09-XX-handover-understanding.md`）
- 项目在哪、为什么这么做、一单全链路主线、硬约束、当前生产状态、V2 六个面各是什么、A 组已审出的结论、**我不确定 / 看不懂的点（必须诚实列，不许装懂）**。
- Lemon 核对纠正。**不是模型自己觉得懂了算，是 Lemon 确认才算。**

**③ Lemon 核对通过后**，才进入下一步：清账本、定主线（D-243 五步之①）。

## 已定的方向（D-243，别重问）
- Lemon 是贯穿所有窗口的人，方向和连贯的最终判断在他。
- 先清账本再审 B/C。
- "一单全链路"是连贯基准。
- **卡台↔支付方式解耦：Lemon 还在考虑做不做，不是已定，是接下来第一个要一起讨论的方向节点。** 任何文档里写"Lemon 的解耦设计"当已定的，都按"待讨论"理解（技术可行已坐实——ZZSHU 不认卡台、卡凭证齐——但做不做没定）。

## 边界
- **只读**：不改生产、不开卡、不切路线、不发布、不改账本方案（清账本是下一步）。
- 拿不准的问 Lemon，不猜（2026-09-17 上一模型错的几次全是猜的）。
- 每次回答先给三点定位（AGENTS.md 要求）。
- 收尾：`scripts/wrapup-check.sh` 全绿；HANDOFF_NOW 覆盖重写。

## 可调度资源
- Codex 可随时配合，但只派边界清晰、能独立验收、不需要跟 Lemon 讨论方向的活。**接手核对本身不派**（必须接手模型亲自做）。
