# D-240｜改造记录（含已撤回的提前成功方案）

> **当前结论（2026-09-17整理）**：本文件的早交付设计/测试为历史过程，不是当前待办。最终4334dc2已经撤回提前SUCCESS与恢复回调；保留去冗余/有限重试/真实进度/2秒查询。当前验收证据以 `../incidents/unified-success/README.md` 为准。下方27项测试属于早交付候选，不可冒充统一成功版本测试。


更新：2026-09-16 12:41 UTC。生产已发布 `20260916-d240-b31a88a`；下方工作区/测试记录保留实现阶段上下文。隔离分支 `codex/plus-delivery-d240`；工作区 `/Users/lemon/.codex/worktrees/ai-recharge-d240`。

## 验收矩阵

|用户确认项|实现|验证|状态|
|---|---|---|---|
|核验请求做减法|删除无消费的snapshot；一次已核对身份的账号快照同时判断Plus；向后传已确认结果，避免再次confirmPlus|真实浏览器本地HTTP夹具：立即Plus+立即取消的正常链20→8请求；跨账号切换被拒|本地通过；生产耗时收益待测|
|确认Plus立即客户成功|recordPlusActivation事务写Plus订单SUCCESS；run仍CANCELLATION_PENDING，已有verification排程保留|独立MySQL连接读取客户status服务即SUCCESS/stage9；新repository发现待收尾；重复操作不重复交付；Pro不提前成功|本地通过|
|取消续费/对账后台完成且不展示给客户|正常worker在交付后继续；崩溃由既有核实lane续做；未收尾/人工待处理时禁止该lane换客户；内部告警与人工确认可收口|真实MySQL演练失败升级人工、客户仍成功、CDK不退；人工收口不改变交付时间；UI测试不显示取消续费|本地通过；真实账号异常恢复待生产验证|
|有界只读重试|初次+最多2次、等待2s/5s、30s读预算；403/429/5xx/网络失败，遵守Retry-After；401/错账号不重试；租约停止优先|403→200、持续403最多3次、429长Retry-After不提前请求、401不重试、租约丢失不发后续请求|本地通过；403上游诱因仍未知|
|圆环接真实阶段|读取payment-stage子阶段；填卡/邮箱/零税阶段推进，不冒充付款成功；付款阶段客户轮询≤2s|真实MySQL阶段SQL+原状态服务；Chromium客户页准备→成功、成功停止轮询|本地通过|

重试预算外不发请求，沿既有安全失败路径收口，不增加无限回队列。未引入新数据库表、新服务、消息队列或跨客户Token缓存。后台收尾与客户交付解耦不等于免费增加吞吐：同一浏览器仍占用到收尾完成/人工处理，避免换号破坏收尾。

## 同时修复的直接阻塞

- 结果读取曾调用只允许RESUMABLE的付款前检查，但提交后getRecoveryState必为RECONCILE_ONLY。新增只允许付款后结果读取的租约校验，普通提交路径仍拒绝二次写；单测证明读可以、重付不可以、租约丢失都不可以。是代码层确定性问题；旧真单原异常未保存，不能声称其UNKNOWN仅由此导致。
- 核实服务写结果时重新取时钟，不再把开始时间当完成时间。
- 人工升级分支引用未定义humanVerification变量，本地MySQL测试实际报错，修复后升级可落库。
- 内部取消确认同时更新orders的subscription_cancelled等投影；已交付订单人工收尾要求CHARGED+renewalCancelled=true，不允许降级或退码。

## 测试证据

`docs/incidents/d240-validation/`：
- browser-tests.txt：304总计，295通过，9跳过，0失败。
- v1-tests.txt：741总计，675通过，66跳过，0失败（含真实浏览器客户页测试）。
- mysql-tests.txt：27通过，0跳过，0失败；使用本地Docker 127.0.0.1:54186 独立数据库pojia_d240_test，所有数据库套件串行。

这些数量有重叠，不能相加称为不重复用例数。跳过项没有冒充通过。

重放（先安装root/v1/browser-mvp的lockfile依赖；需要本地Chromium和隔离MySQL）：

```bash
npm ci
npm --prefix v1 ci
npm --prefix browser-mvp ci
(cd browser-mvp && node --test)
(cd v1 && node --test)
# TEST_DATABASE_URL 只能指向独立测试库，不能使用生产runtime.env。
node --test --test-concurrency=1 v1/test/plus-early-delivery-mysql-integration.test.js v1/test/browser-execution-mysql-integration.test.js v1/test/browser-recovery-mysql-integration.test.js v1/test/browser-manual-payment-mysql-integration.test.js v1/test/browser-resolve-unknown-payment-mysql-integration.test.js browser-mvp/test/payment-executor-mysql-integration.test.js
```

## 当前工作区与部署边界

- 已合并主工作区、推送并上线单提交b31a88a；当前生产值见CURRENT_STATE，发布证据见d240-deployment/report.md。没有为发布创建新充值单。
- 先前两个在途诊断已提交留存并包含在发布包，不再是未提交改动。隔离候选/发布worktree仅保留追溯，不是另一套生产，不要从旧分支重新发布。
- 自检脚本变量插值错误已修；state-check与wrapup-check已实际通过。库存值随生产资格规则变化，以CURRENT_STATE最新核对为准，不使用实现阶段“2张”的旧数字。
- 用户已授权的这次发布已结束；再次部署、开卡、补钱、付款仍须按项目规则取得当次确认。
- **回滚不是无条件切旧版**：新版本可能留下“客户SUCCESS但run收尾未完”的合法组合，旧recordCancellationConfirmed不接受。回滚前须确认这类run已收尾；否则保留兼容的收尾执行器处理完再回滚，不能清订单或重付。

## 尚未证明

没有生产新版本请求耗时、真实新故障重试成功率、客户最终端到端时延。首次403上游诱因仍未知；不能承诺不再失败或固定节省一分钟。

## 发布验收（2026-09-16 12:41 UTC）

已按用户明确发布授权，单提交b31a88a prepare→校验→switch，更新Web/Worker/本机Browser。备份与1060文件清单通过；592条SQL PREPARE零失败，动态72条未计入，其中关键新查询另用正式候选代码只读运行通过。公网customer.js v39与发布文件同哈希。接单已恢复true，付款开关未变，未代客户充值。真实耗时收益仍待新版本订单；详见docs/incidents/d240-deployment/report.md。
