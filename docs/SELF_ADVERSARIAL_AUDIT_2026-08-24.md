# 本窗口自我对抗式审查（2026-08-24）

## 审查结论
本窗口前几轮存在事实/建议/用户确认混淆和历史快照未显式标过时的问题，已修正。没有把这些问题继续当成业务事实。

## 发现的问题

1. **把建议写成用户确认**：曾将 Session 风险、接单/派发组合和“执行窗口负责开关”写成用户确认。已改为安全事实、测试建议或协作偏好。
2. **把用户陈述当现场事实**：用户说我方服务器正常、卡台恢复，不等于本窗口现场核验。后续已通过 SSH/readiness/HNSKJ read-check 核验服务器和卡台只读；Provider/卡台写能力仍未验证。
3. **测试数量存在 408/409 两个数字**：408 是早期工作树快照，409 是增加独立派发测试后的当前工作树结果。文档需按时间/commit理解，不能并列当作同一次运行。
4. **Session 的 279 字节与 7430 字节冲突**：279 字节是早期剪贴板内容/读取轮次；后续核实为 7430 字节整体、尾部 19 字符，清理后 7409 字节 JSON 前缀。旧记录已标为历史快照。
5. **后台未核验结论过时**：第一轮没有登录 ops 标签页，第二轮已完成逐页只读核验；旧结论已标为历史快照。
6. **“卡台正常”范围过宽**：现场只读正常，不代表开卡/卡余额充值写入或真实付款正常；已在当前状态中收窄措辞。
7. **工作区边界风险**：存在其他窗口未提交代码；本窗口不应把未提交代码当作已部署，也不能混提。

## 尚未解决的事实边界

- 生产写开关目前关闭，未做真实资金写验证；
- 真实成功订单、取消续费、UNKNOWN 人工对账未验证；
- 生产遗留 PENDING 任务、VALIDATING intake batch、quarantine/review 卡和历史 UNCERTAIN 尚未处置；
- Browser 真实付款未验证。

## 修正后的书写规则

- 用户确认 ≠ 运行事实；
- 代码测试 ≠ 生产部署；
- 只读 Provider 成功 ≠ 写入/付款成功；
- 建议 ≠ 决策；
- 历史快照必须标注时间和是否已被后续证据覆盖；
- 任何无法由当前证据证明的内容写入“未验证”，不写入“已完成”。
