# 破甲 MVP 验收与证据边界（2026-08-19）

## 已确认事实

### 2026-08-18 独立真实 Plus PoC

- 原始证据：生产机 `/opt/pojia/private/provider-poc-state.json`，权限 `0600`；敏感卡资料、Session、`card_key` 不进入本文。
- 参数：HNSKJ 卡型 ID `1`（`Z-43612081`），开卡 `$16`，最低卡余额 `$16`，`planType=plus`。
- 卡：Provider ID `493`，尾号 `8590`。
- ZZSHU 最终状态：`success`；实际支付 `982.14 PHP`；`is_subscription_cancelled=1`。
- HNSKJ 交易：`$16` 入金成功；`$15.97`（原币 `982.14 PHP`）支付成功并结算。
- 2026-08-19 只读复核时卡余额 `$0.02`。
- 2026-08-19 13:43:31 另有一笔 `OPENAI *CHATGPT SUBSCR`、`$78.24` 的失败支付尝试，没有实际扣款。用户已确认这是主动点击升级套餐后产生的失败支付，不是系统自动续费；原因归属来自用户确认。

结论：双 Provider 的真实开卡、Plus 创建、最终成功和取消标记已经验证；该 PoC 独立于正式订单数据库，不能替代正式订单状态机验收。

### 2026-08-19 正式订单付款前闭环

- 正式订单：`PJV1-m2wZr2ETzmiI8fTGfAT9`。
- 仅调用一次 HNSKJ 开卡，绑定 Provider 卡 `612`，状态 `active`，余额 `$16`，尾号 `1666`。
- 开卡任务执行 1 次；卡片就绪轮询 6 次。
- `PREPARE_RECHARGE` 完成，审计事件为 `submitted=false`。
- `SUBMIT_RECHARGE` 保持 `PENDING / attempts=0`；没有 ZZSHU `create_direct` 调用。
- 实测结束后 `accept_new_orders=false`、`dispatch_new_recharges=false`、三个 Provider 写开关均为 `false`。

结论：正式数据库、任务租约、开卡、缺失 ID 恢复基础、卡就绪和付款前请求构造已验证；没有重复花钱验证真实 Plus。

### 2026-08-19 自有后台运营入口

- 后台已提供开始/停止接单、人工补卡和单订单“确认充值/撤销放行”。
- 开始接单只让新订单进入自动付款前流程；真实直充仍必须逐单二次确认。
- 本次验收只检查按钮、确认窗、权限和隔离数据库语义；没有点击生产“确认充值”，没有新的 Provider 写请求。

### 2026-08-20 库存对账与付款前复核

- 发布：`/opt/pojia/releases/20260820-card-reconcile-1`，源码提交 `8154c12`；131 个发布文件的 SHA-256 清单已校验。
- 卡台只读全量对账：历史总卡数 16，active 5，未解析 0；本地可分配 3、已分配 1、已耗尽 1、核对中 0。
- Provider 卡 `493` 余额 `$0.02`，已标记为 `DEPLETED`；Provider 卡 `444` 余额 `$17`，已纳入可分配库存。
- 总览现在单独显示“待确认充值 1”，指标可跳转至对应订单列表；`CARD_READY` 不再误算为自动处理中。
- 真实后台订单详情已确认：Session 有效、Access Token 有效、卡片余额/凭据有效、卡片 15 分钟内已核对；服务端在签发 Permit 时会再次执行同样的复核。
- 未点击生产“确认充值”或“创建开卡任务”；新增开卡任务 0，有效充值 Permit 0，ZZSHU `create_direct` 历史调用仍为 0。

### 2026-08-20 CDK 批次恢复与管理

- 发布：`/opt/pojia/releases/20260820-cdk-batches-2`，运行代码提交 `37044a3`。
- CDK 生成增加唯一幂等键；隔离 MySQL 实测两个并发同键请求仅创建一个批次，另一个返回相同 CDK。
- 单个 CDK 仍只保存 SHA-256 哈希；批次恢复副本使用 AES-256-GCM 加密，测试确认数据库密文不包含 CDK 明文。
- 生产后台已显示批次总数、未使用、已兑换和已作废数；新版批次可重新下载，旧批次明确标记为“无明文”。
- 作废操作只修改 `AVAILABLE` CDK，不会修改已兑换订单；超过 10 个 CDK 要求额外确认。
- 所有已认证后台写接口已加入同源 `Origin` 校验和独立限流。
- 本次生产验收没有生成或作废任何 CDK；新批次表记录 0，开卡任务 0，有效充值 Permit 0，`create_direct` 历史调用 0。

### 2026-08-20 失败订单 CDK 补偿

- 发布：`/opt/pojia/releases/20260820-order-compensation-2`，运行代码提交 `9c7dde9`。
- 只有 `CREATED`、至少一个任务已 `DEAD`、没有活动任务、没有卡片且没有供应商调用的订单可人工补发；已进入开卡、充值或结果未知链路的订单均禁止补发。
- 补发会在同一事务中关闭原订单、创建一张同套餐 CDK、写入加密恢复副本和审计事件；重复请求返回同一张 CDK，不会重复生成。
- 补发后的原订单在客户查询页显示失败，不会继续显示排队中。
- 生产后台已只读验证：现有已进入开卡链路的订单显示“禁止补发”，页面不提供补发按钮。
- 本次生产验收没有点击补发；补发记录保持 0，开卡/直充供应商调用计数部署前后不变，没有产生费用。

### 2026-08-20 未充值订单取消与卡片释放

- 发布：`/opt/pojia/releases/20260820-order-cancellation-1`，运行代码提交 `00b66d5`。
- 后台订单详情新增“取消未充值订单”；只有直充任务仍为 `PENDING / attempts=0`、没有 `create_direct` 调用或外部充值单号、卡片状态与余额合格且 15 分钟内已同步时显示执行按钮。
- 服务端在事务内锁定订单、卡片和提交任务，再次核验后关闭订单、停止等待任务、释放卡片回可用库存并写入时间线。
- 已提交、结果不明、卡片状态异常或数据过期时禁止取消；客户查询取消后的原订单显示失败。
- 生产页面已只读验证当前历史测试订单显示“可以安全取消”，同时仍显示“确认充值”；本次没有点击任一按钮。
- 部署前后均为可用卡 3、已分配卡 1、待确认订单 1、`create_direct` 调用 0，没有改变真实业务数据或产生费用。

## 安全执行规则

1. 直充任务没有针对该订单的有效 `rechargePermit` 时不可被 Worker 领取。
2. Permit 只允许 `CARD_READY + PENDING + attempts=0 + 从未调用 create_direct` 的订单。
3. 同一时间最多一个有效 Permit；有效期 1–30 分钟。
4. Worker 在外部请求前原子消费 Permit；消费后无论成功、明确拒绝或结果未知，都禁止自动第二次创建。
5. 正式 Worker 的卡台开卡权限恒为关闭；直充调用能力可用，但任务领取 SQL 和提交 handler 都必须校验指定订单的有效 Permit。
6. 后台“确认充值”只签发一个 10 分钟 Permit；可在未消费前撤销，消费后禁止重复放行。

## 明确未完成

- 没有在正式订单 `PJV1-m2wZr2ETzmiI8fTGfAT9` 上执行真实 Plus；这是有意的零成本边界。
- 真实退款样本、大批量并发和 Telegram 仍属于后续阶段。卡池、人工补卡和大于 10 张的额外确认已实现。
- 取消标记后的 `$78.24` 失败授权已由用户确认为手动升级操作；后续仍通过交易记录只读观察实际扣款，不把用户说明误写成供应商接口直接返回的字段。

## 生产固化

- 当前发布在每次生产验收后更新；发布目录使用不可变名称，`/opt/pojia/current` 原子指向当前版本。
- 每次保留上一个不可变发布作为回滚基线；迁移 001–017 均为向后兼容的新增表/列/索引。
- 发布清单：生产发布根目录 `RELEASE_MANIFEST.sha256`，包含关键运行文件的 SHA-256。
- 最新本地无数据库验证：160/174 通过，14 项 MySQL 用例跳过；生产机隔离临时 MySQL 8.4 完整验证：174/174 通过。
- 生产健康检查：Web、Worker、`/health/live`、`/health/ready` 均通过。
- 生产后台数据确认：付款前检查“已就绪”，直充“已锁定”，任务 `PENDING / attempts=0`，ZZSHU 创建调用 0 次。
