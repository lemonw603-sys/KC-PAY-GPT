# 当前状态快照（2026-08-24）

## 已验证事实
- 本地 v1 测试：408 项，374 通过，0 失败，34 跳过；跳过项需要隔离 MySQL/外部条件。
- 公网 ops/plus live 与 ready 检查均返回 HTTP 200；这不等于生产业务和资金链路可用。
- 单笔真实 API 全链路测试方案已落盘：`docs/REAL_E2E_SINGLE_ORDER_TEST_PLAN_2026-08-24.md`。
- 最新本窗口测试准备提交：`072a227`。
- 2026-08-24 完成一次独立全项目对抗式审查（文档+代码+隔离测试交叉核验，无生产证据）：报告 `docs/FULL_PROJECT_ADVERSARIAL_AUDIT_2026-08-24.md`。核心资金安全机制代码级已验证（单订单唯一资金栅栏、create_direct 唯一约束、开关默认全关、一卡一单卡资格、HNSKJ 幂等键、UNKNOWN 锁定、客户侧敏感隔离、Browser 不伪造 Provider 调用）。

## 用户确认结论
- 2026-08-24 用户澄清：我方生产服务器正常；阻塞在**卡台（HNSKJ）正在升级服务器**，当前开不了新卡、也无法往新卡充值。此前"服务器尚未修好"应理解为卡台侧写能力不可用，非我方服务器故障。
- 因卡台写能力不可用，暂不执行真实开卡、卡余额充值或 Plus 充值。
- 可先由其他模型接手做审查、准备和只读验证。

## 当前实现/规划
- API 是当前可验证的正式执行器；Browser 是独立工作线，真实付款尚未达到生产闸门。
- 真实单笔测试必须并发 1，任何 UNKNOWN、重复写入或资金差异立即停单。

## 未完成
- 卡台升级完成后，开卡/卡余额充值写能力恢复与只读合同核验。
- 我方生产现场只读体检（release/迁移/服务/五类开关现场值）。
- 单笔真实 API 全链路测试（依赖卡台写能力恢复）。
- Browser 真实付款验收。

## 未验证边界
- 当前生产接单、派发和 Provider 写开关状态必须在测试前重新现场核验，不能沿用旧快照。
- 时间线已澄清（2026-08-24）：`SINGLE_SOURCE`(08-22 称生产正常) 与旧措辞"服务器尚未修好"不冲突——我方服务器正常，阻塞在卡台升级；仍需现场只读体检确认我方 release/开关现场值。
- `DECISIONS.md` 决策 ID 重复：D-069 是已提交老问题；D-089、D-109 的重复出现在其他窗口未提交改动（D-090~D-133 那批 +59 行）中，本窗口未改动该文件，待其提交后统一重编号（映射见审查报告补充节）。

## 当前阻塞
- 卡台（HNSKJ）升级中：开卡与卡余额充值写能力不可用；我方生产服务器无故障。

## 下一步唯一动作
- 其他模型先读取项目事实源和本文件；可在不做资金写入前提下进行我方生产只读体检和真实单笔测试前置闸门核对，等卡台写能力恢复后再按方案执行；不得执行资金写入。
