# 当前生产状态｜事实表

每行一个事实，带核对时间与证据方式；变化时改行，不追加段落。旧版快照原文：`docs/archive/2026-09/CURRENT_STATE_snapshot_2026-09-07.md`。

| 项目 | 当前值 | 核对时间（UTC） | 证据方式 |
|---|---|---|---|
| 生产 release | `/opt/pojia/releases/20260918-step5b-b0a36d4`（提交 b0a36d4，2026-09-18 13:07 UTC 切换）。**⚠️ 第⑥步 step6（`20260919-step6-8cd6d7e`）2026-09-19 发布后当天回滚**——工作台做偏设计图（营业条被改成五决定按钮组而非原型的 toggle 开关、sidebar/顶栏留旧皮未照候光原型、顶栏残留「开始营业/刷新」），Lemon 不满意，**已回滚重做中**（代码仍在 commit `8cd6d7e`，对照原型重做后重发）。**⑤b「第⑤块收窄」+ Codex 复审补修（D-275 / F-47~F-60）**：**无数据库迁移**。金额对账降级 `UNVERIFIABLE`（无可验证期初基准不判异常/一致，D-274）+ 余额有符号；未知扣款分 `UNEXPLAINED_CHARGE`（无主进差异）vs `PENDING_MANUAL_REGISTRATION`（已登记 manual-used）；「连续两次」只认正式批次 + 输入存疑（同步失败/highvcc 无水位）不自动升级；日报按天 key + 前缀收历史；删 DAILY_DIGEST 空开关 / `countPushesByType` 改名 / 猜测拒付类型 CHARGE_BACK·DISPUTE；**F-47 押金重复扣减修复**（`walletPreflight` 回到余额−金额−手续费≥floor）。前一版第⑤步的白名单四类/三个新产生点/次数对账保留不动。 | 2026-09-18 13:08 UTC | 回滚后新 SSH 独立核实：web 1699695 / worker 1699700 / bark 1699751 三者 cwd 均在 step5b、NRestarts=0、active；index.html 引用 admin.js?v=50（旧后台）；/health 200、登录页 200 |
| 回滚点 | **`20260918-step5-740bc1d`**：⑤b 的上一版（第⑤步）。当前生产已是 ⑤b（step6 回滚后）。命令：`ln -sfn /opt/pojia/releases/20260918-step5-740bc1d /opt/pojia/current && systemctl restart pojia-web.service pojia-worker.service pojia-bark-notifications.service`。回滚仍须独立评估在途 run。 | 2026-09-18 13:08 UTC | `deploy-release.sh switch` 打印的 ROLLBACK 行 |
| 最新数据库备份 | `/var/backups/pojia/pojia-20260918T130636Z.sql.gz.enc`（⑤b prepare 前自动备份；step6/回滚均无迁移）。**完整性已验**（`backup_integrity=OK`）。日常仍由 `pojia-backup.timer` 每日 03:2x UTC 出一份。 | 2026-09-18 13:07 UTC | `deploy-release.sh prepare` 的 backup 段输出 |
| **服务器重启** | **2026-09-16 23:36:10 UTC 服务器重启过**（`uptime -s`；`journalctl --list-boots` 仅此一个 boot）。web 起来时 MySQL 未就绪崩一次（`PROTOCOL_CONNECTION_LOST`），systemd 6 秒后自动拉起。原因：**Lemon 判断是服务器欠费到期**（9 月 8 日到期、可延约一周），已续费（D-244）；日志无直接证据。 | 2026-09-17 07:20 UTC | ssh `uptime -s` / `journalctl -u pojia-web --since "2026-09-16 23:34"` |
| pojia-web | active；PID 1699695（2026-09-19 UTC 起，step6 回滚重启），NRestarts=0，cwd /opt/pojia/releases/20260918-step5b-b0a36d4/v1。 | 2026-09-18 13:08 UTC | 新 SSH 连接 `systemctl show -p MainPID -p ActiveEnterTimestamp` + `/proc/<pid>/cwd` |
| highvcc 备用卡台 A token | 已配置进生产（`app_settings.highvcc_access_token_ciphertext`，加密存储，09-10 09:17 UTC 写入） | 2026-09-10 09:52 UTC | `v1/scripts/set-highvcc-token.mjs` 输出 |
| highvcc 备用卡台 A 已开卡片（本窗口） | 3 张：尾号 9839（$50，08:xx）、9354（$5，09:19）、3241（$3，09:35，开卡时因 detail() 竞态未即时入库，09:53 用 `reconcile-highvcc-card.mjs` 补记）；账户另有 $20 押金要从钱包余额里先扣，才是真实可开卡余额（Lemon 提供） | 2026-09-10 09:52 UTC | 平台卡片列表 + `cards` 表独立核对 |
| pojia-worker（v1 任务 Worker） | active；PID 1699700（2026-09-19 UTC 起，step6 回滚重启），NRestarts=0，cwd /opt/pojia/releases/20260918-step5b-b0a36d4/v1。分卡前当场同步过期候选（`provider_calls` request_key 前缀 `order-demand-sync:`）；付款不明排有界 POLL_RECHARGE。 | 2026-09-18 13:08 UTC | 新 SSH 连接 `systemctl show -p MainPID -p ActiveEnterTimestamp` + `/proc/<pid>/cwd` |
| **pojia-bark-notifications** | active；PID **1699751**（2026-09-19 UTC 起，step6 回滚重启），NRestarts=0，cwd = `/opt/pojia/releases/20260918-step5b-b0a36d4/v1`（新连接 readlink 独立核实；switch 打印 `bark cwd=/` 仍是假值）。**switch 打印的 `bark cwd=/` 仍是打印时机假值**（旧进程未退净、MainPID=0 → `readlink /proc//cwd` 打 `/`），新连接独立核实真实 cwd 在新 release（账本发现 13 的老毛病，用独立核实兜住）。D-271 修复保持：`deploy-release.sh` 发布重启 bark，每次核对该行、别信打印值。 | 2026-09-18 13:08 UTC | 新 SSH 连接 `systemctl show -p MainPID` + `readlink -f /proc/1376490/cwd` |
| pojia-daily-reconciliation.timer | active / enabled。`OnCalendar=*-*-* 04:00:00 UTC` + 随机 10 分钟，下次 **2026-09-19 04:0x UTC**。⑤b 发布后跑**新代码**：金额降级、无主扣款不升级、输入存疑（同步失败/highvcc 无水位）不升级、日报按天 key + 前缀收历史。**13:0x UTC 只读 dry-run 复验**（新代码，不写）：30 张卡 / 差异 **6**（全 `UNEXPLAINED_CHARGE`）/ persistent **0** / 待登记 **1**（3336）/ 无法核对 **30** / 输入存疑 0 / 待销到期 4；**1657/3159 落 `UNVERIFIABLE` 不再进差异**——04:01 第二跑不会再误升 critical（旧 `daily-reconciliation:2026-09-18` key 由新代码前缀收掉，F-59）。第⑤步 08:29 首跑的旧指纹与旧格式 last_report 会被 04:01 新跑覆盖。 | 2026-09-18 13:08 UTC | ssh `daily-reconciliation-runner.js --dry-run`（只读，current=新 release） |
| pojia-browser-worker | inactive / disabled（Browser 执行在本机，来单人工拉） | 2026-09-09 11:46 UTC | systemctl |
| pojia-card-funding.timer | active / enabled，`OnUnitActiveSec=5s` 跑 `card-funding-runner.js`（带 `PROVIDER_CARD_WRITES_ENABLED=true`），但 `card_balance_recharge_enabled=false` → 每次立即退出（journal `CARD_BALANCE_RECHARGE_DISABLED`），不做事。另有 `pojia-card-funding-reconcile.timer`（15s）、`pojia-operator-watch.timer`（1min）、`pojia-backup.timer`（每日）在跑。 | 2026-09-17 05:40 UTC | ssh `systemctl cat` / `list-timers` / `journalctl -u pojia-card-funding` |
| pojia-card-read-sync.timer | active / enabled，15s 一轮。第④步起：AVAILABLE 卡每 3 小时一次（dedupe 桶仍 60 分钟，见 D-267）；只排有只读 API 的卡（highvcc 卡不再被排进 hnskj 同步）；RETIRED 卡不排。历史 REVIEW_REQUIRED 307 条已归档 `ARCHIVED_LEGACY`（06:27 UTC，Lemon 批 D-269 ⑤），现 REVIEW_REQUIRED 0。合格窗口仍 = 同步后 15 分钟，窗口外由分卡当场同步补。 | 2026-09-18 06:31 UTC | card_sync_jobs 独立 SELECT（ARCHIVED_LEGACY 307 / COMPLETED 1729 / REVIEW_REQUIRED 0） |
| pojia-highvcc-snapshot-sync.timer | active / enabled，**`OnUnitActiveSec=1h`**（D-169 改的）跑 `sync-highvcc-snapshot.mjs --commit`。**service 现处 `failed`：08:38、09:38 UTC 两次 `HIGHVCC_TOKEN_EXPIRED（HTTP 200 登录已失效）`**；05:38/06:38/07:38 成功（committed null = 数据未变）；最近成功批次 04:38:23 COMMITTED（0 插入 / 9 更新）。token 06:06 贴、08:38 失效（Lemon：该 token 一般活约 2 小时）；过期后无任何 operator_alert——面四实证「token 过期不叫人」。**Lemon 12:47:21 UTC 已重贴；12:53:15 UTC 经 Lemon 同意手动 `systemctl start` 一次 → 12:53:18 COMMITTED（批次 3fb14bb4，0 插入 / 9 更新），同步恢复**；timer 照常 13:38 再跑。**highvcc 登录 = 账号密码 + 随机位置滑块**（Lemon）→ 自动登录续 token 不做，token 只能人贴（D-249 补记）。 | 2026-09-17 12:50 UTC | ssh `systemctl cat` / `list-timers`；`manual_card_import_batches` 独立 SELECT |
| pojia-card-stock-runner.timer | active / enabled，60s 一轮。**2026-09-18 06:27 UTC highvcc 自动开卡首次生产实跑**：钱包 23.45 → 142.49（Lemon 充值）后，调度器开 **4022**（$50，job deb85d05 COMPLETED）；06:28 第二张 job cdd7d90d 报 `HIGHVCC_RECONCILE_NOT_READY`（卡台详情未就绪）→ REVIEW_REQUIRED、103 FAULT、调度器每轮 `FUNDS_REVIEW_REQUIRED`。**06:49 人工补记那张卡（8718）、06:51 用新的 `resolve-reviewed-stock-job.mjs` 结清 job**（opened_count 写回 1，原 error_code 保留为观察）→ 故障态与告警解除，调度器回到 `NO_DEMAND`（水位 2 已满）。 | 2026-09-18 06:51 UTC | journal 逐轮输出 + card_stock_jobs / operator_alerts / provider_accounts 独立 SELECT |
| 健康 | 服务器本机live/ready 200；admin登录页200；公网客户JS v39的SHA256与单提交发布包一致；历史成功单状态API返回SUCCESS/stage9。 | 2026-09-16 12:41 UTC | 发布日志、独立SSH/DB/API/公网哈希复验（docs/incidents/d240-deployment/） |
| 数据库迁移 | 最新 `054_card_retirement`（2026-09-18 06:26:29 UTC 应用，第④步；只加 `card_min_retire_age_hours=6`；053 于 00:0x UTC） | 2026-09-18 06:26 UTC | schema_migrations 独立 SELECT + app_settings 实查 |
| accept_new_orders | true。**第④步发布起 `setOrderAcceptance` 写 `admin_setting_events`**（D-265 第 3 条已上线）；表里既有 4 行是 09-16 两次发布脚本（codex）写的，不是后台按钮。 | 2026-09-18 06:30 UTC | app_settings + admin_setting_events 独立 SELECT |
| dispatch_new_recharges / 模式 | true / AUTOMATIC | 2026-09-09 11:46 UTC | app_settings |
| 默认路线（Plus） | **API：LEGACY_HNSKJ_ZZSHU_V1 accepts_new_orders=1；CHATGPT_PLUS_BROWSER_V1=0**。第④步 rehearsal 期间 06:53 UTC 切 BROWSER（事件 6df4d9f2）、07:00 UTC 切回 API，两次四项校验全过（`TARGET_POOL_AVAILABLE` 用库存口径：切回时 hnskj 分卡口径 0 张、库存口径 2 张，校验按后者放行——D-259 的改动在切换校验上的生产实证）。仅影响切换后新单。 | 2026-09-18 07:07 UTC | fulfillment_routes 独立 SELECT + provider_route_switch_events |
| Browser 当前卡台 | 备用卡台 A（`manual_excel` / `backup-a`） | 13:31 | browser_card_source_selections |
| browser_dispatch_enabled | true | 2026-09-09 11:46 UTC | app_settings |
| browser_payment_writes_enabled | true；第③步 E 演练期间 01:03 UTC `stop-live.sh` 关（审计行 actor stop-live.sh）、01:45 UTC `setBrowserPaymentWrites` 开回并同步 executor profile（审计行 actor lemon-via-fable:step3-E）。 | 2026-09-18 01:46 UTC | app_settings + executor_profiles + admin_setting_events 独立 SELECT |
| **菲律宾出口对 ChatGPT 的可达性** | **未定论**。裸 curl 经出口访问 chatgpt.com 返回 403 Cloudflare 拦截页，但 **curl 不能用来判断 Cloudflare 是否封禁**（无 TLS 指纹、不执行 JS，会被单独拦）。**Lemon 当场在 BitBrowser 窗口里看到的是 ChatGPT 的退出登录页面，说明页面打得开、出口没被整站封**。真实根因转向「Session 没能登录上」，见 D-187 | 2026-09-12 09:40 UTC | 反例证据来自 Lemon 直接观察窗口；curl 测试已作废 |
| Browser Profile productionWritesEnabled | true（CHATGPT_PLUS_BROWSER_V1；正式启动检查与DB读取一致）。 | 2026-09-16 12:41 UTC | 发布日志、独立SSH/DB/API/公网哈希复验（docs/incidents/d240-deployment/） |
| card_auto_replenishment_enabled | **true**（2026-09-18 00:50:35 UTC 开，第③步 C，`set-supply-scheduler-flag.mjs on --apply`，审计 `admin_setting_events` false→true actor `lemon-via-fable`）。这是供卡调度器总闸。**`card_balance_recharge_enabled` 仍 false 且未被连带打开**（补余额整条线定要删，面二⑪；后台那个「开启」按钮会把两个一起开，别用它，用脚本）。 | 2026-09-18 00:52 UTC | app_settings + admin_setting_events 独立 SELECT |
| card_balance_recharge_enabled | false（此前 09-14 true 已过期；本轮未改开关） | 2026-09-16 10:38 UTC | app_settings 独立只读 SELECT |
| card_max_successful_payments | 3（对 Plus；5X/20X 每卡 1 单的按产品上限**未落地**，第③步只改供卡不改资格规则） | 2026-09-18 00:30 UTC | app_settings |
| 最低所需卡余额 | default 16 / **pro_5x 95**（2026-09-20 06:08:21 UTC 由 16 改为 95，Lemon 确认「5X 最低金额就是 95」，D-301；`set-minimum-card-balance.mjs --apply`，审计行 16.00→95.00，新连接独立复核）/ **pro_20x 150**（09-09 05:33 UTC 调） | 2026-09-20 06:08 UTC | app_settings（`minimum_required_card_balance:*`）+ admin_setting_events 独立 SELECT |
| Worker 进程写权限 | 进程实际（`/proc/75228/environ`）：`PROVIDER_READS_ENABLED=true / WRITES=false / CARD_WRITES=false / RECHARGE_WRITES=true`。来源：三个写开关来自 drop-in `api-recharge-enabled.conf` 的 ExecStart；**READS=true 来自 `/etc/pojia/provider.env`（unit 里 `EnvironmentFile=-`，后加载覆盖 `runtime.env` 里的 `READS=false`）**。funding 单元：`CARD_WRITES=true`。 | 2026-09-17 05:40 UTC | ssh `systemctl show -p EnvironmentFiles`、`cat drop-in`、`grep provider.env`、`/proc/pid/environ` |
| HNSKJ 卡 | AVAILABLE 2 张：`5276`（尾 0577，$16）+ `5622`（尾 6754，$50，开卡成本 $0.75）。**旧卡 12 张已标终态 RETIRED**（2026-09-18 06:27:14 UTC，`retire-legacy-cards.mjs --batch hnskj-voided --apply`，Lemon 批；`sync_tier=ARCHIVED`，定时同步不再碰）。分卡：候选过期时当场同步再判（第④步）；定时同步每卡 3 小时。限流预算：按需每单 ≤2 次 + 开卡每张 ≤3 次 + 定时每卡每 3 小时 2 次。 | 2026-09-18 06:27 UTC | cards / card_state_events 新连接独立 SELECT（RETIRED 12、CARD_RETIRED_CONFIRMED 21 条） |
| 备用卡（备用卡台 A = highvcc → 103 manual_excel，sync_tier=MANUAL_IMPORT） | 库内 **16 张**（`CURRENT_STATE` 原写 17 张，2026-09-18 08:0x 实查为 16，差 1 张未查清——第⑤块发现 9）：**AVAILABLE 7 张**，可分配 2 张（**4022** $50 调度器自动开、**8718** $50 卡台已开出后人工补记）；其余 6 张余额不够门槛。**RETIRED 9 张**（8 张按「已注销」+ 3336 按「Lemon 手动付 20X」，D-269）。**钱包 $41.49**（06:5x 只读核对：Lemon 充值后 142.49，开两张各扣 50.50）。第二张开卡 job 已人工结清（D-270 不适用；见 stock-runner 行），103 `supply_fault_state` 回 OK。 | 2026-09-18 07:05 UTC | cards / provider_balance_snapshots / card_stock_jobs 独立 SELECT；卡台 detail 只读核对 |
| 可分配卡（正式资格 SQL） | **库存口径 4 张**（hnskj `5622` $50 + `5276` $16，highvcc `4022` $50 + `8718` $50）——后台「可分配」显示的就是这个数（D-307/D-312）。**分配口径此刻 2 张**（2026-09-20 13:2x UTC 用生产 release 的 `eligibleInventoryCardSql` 实跑，逐张列出＝highvcc `4022`/`8718`）：hnskj 那两张此刻**不合格**，因为分配口径多一条「15 分钟内同步过」而 hnskj 每 3 小时才同步一次，窗口外由分卡当场同步补（D-259/第④步）。**这个数是瞬时值、会自己来回跳**（09-20 00:0x 查是 4、13:2x 查是 2），`state-check.sh` 抓到 2 或 4 都正常，不算漂移。非终态订单 0。**订正**：本行前一版把库存口径的 4 标成「正式资格 SQL」，且把 highvcc 两张记成 `1eca`/`eb30`——生产查无此二卡（`last4` 与 `id` 前缀都没有），记错来源未查。 | 2026-09-20 13:2x UTC | 生产 release 的 `card-inventory-eligibility.js` 生成 SQL + `prod-query.sh` 逐张列出；`state-check.sh` 复核 |
| HNSKJ 卡台 | 钱包 **$38.73**（2026-09-18 00:27:31 UTC，开 5622 后；开卡前 $89.48）。默认卡段 23（`provider_accounts.default_card_segment`，第③步起策略/账户表是供卡真相，`default_card_type_id` 仍供人工开卡表单用）。**人工开卡不再需要 ssh**：后台建 job 后由 `pojia-card-stock-runner` 执行（timer 启用前需手动 `systemctl start`）。限流：每分钟最多 60 次请求（D-244）。 | 2026-09-18 00:30 UTC | provider_balance_snapshots 独立 SELECT + journal |
| **highvcc 数据源（D-249 T1，2026-09-17 上线）** | `card_transactions` 对 103 账户 **22 笔**（9 张卡，0 笔归不到）；`provider_balance_snapshots` 对 103 **1 条**，钱包可用余额 **$23.68**。由 `sync-highvcc-snapshot.mjs --commit` 每小时随 timer 写入，**与快照的 NO_CHANGE 短路无关**（挂在 runner 层，短路时照样写）。首次成功写入 2026-09-17 15:52:25 UTC。 | 2026-09-17 15:55 UTC | 隧道新连接 SELECT 计数 + 逐卡分布与手工比对 22 笔吻合 |
| **消费账本覆盖（D-256 补记后）** | CONSUMED **19** / RELEASED **50** / RECONCILIATION 2。20 个成功单里 19 单已记 CONSUMED，**只剩 `PJV1-DqcnqHF0tPlxDhygTtAA` 未记**（7402 卡在其时间窗内无任何流水，证据不足未补，原因未知待查）。补记后用量：1657=3（用满）、3159=3（用满）、5371=2、7402=1、6807=1。 | 2026-09-17 16:03 UTC | 隧道新连接 SELECT 状态分布 + 逐单 consumed_at/审计行核实 |
| 订单总况 | RECHARGE_SUCCESS 20 / RECHARGE_FAILED 37 / **CLOSED 21**（+1：第④步 rehearsal 单 `PJV1-8DB4vPHrXotbcNBJHETH`，CANCELLED_PRE_SUBMISSION，付款提交 0 次，卡已放回、CDK 停在 REDEEMED 作死码）/ 非终态 0。 | 2026-09-18 07:05 UTC | orders 独立 SELECT |
| 活动资金与运行 | active_runs 0；非终态订单0；统一成功发布后新建订单0。当前版本真实单时延/重试效果仍未验。 | 2026-09-16 22:50 UTC | 独立SELECT orders/browser_runs |
| 最近 Browser 运行 | 最近付款样本见「最近真实单」行；本次D-240仅重启和只读校验，新版本尚无真实付款验收样本。 | 2026-09-16 12:41 UTC | 发布日志、独立SSH/DB/API/公网哈希复验（docs/incidents/d240-deployment/） |
| Browser 自动化里程碑 | 已有真实自动成功样本（不是0）；09-16重提单由旧版自动补核收口。D-240部署验证已通过，真实提速与异常恢复效果待新样本。 | 2026-09-16 12:41 UTC | 发布日志、独立SSH/DB/API/公网哈希复验（docs/incidents/d240-deployment/） |
| 最近真实单 | PJV1-x-tIsPB5ICHu6R9bzsSO（09-16 11:06:44 UTC提交）：RECHARGE_SUCCESS；Browser COMPLETED/PAYMENT_CONFIRMED/RESOLVED/CANCELLATION_CONFIRMED；PAYMENT_SUBMIT仅1条，CDK REDEEMED，卡分配RELEASED。先UNKNOWN，后自动补核成功。orders.subscription_cancelled仍NULL，Browser取消续费确认记录存在（投影差异）。 | 2026-09-16 11:12 UTC | 独立查询，docs/incidents/2026-09-16-x-tIs-success-evidence.tsv |
| 告警 | **OPEN 123**（第⑤步发布后 +1 = `DAILY_RECONCILIATION_SUMMARY`）：BROWSER_ORDER_SUBMITTED 36、BROWSER_ORDER_FAILED 30、PROVIDER_BALANCE_CHANGED 25、BROWSER_PAYMENT_UNKNOWN 10、BROWSER_ORDER_COMPLETED 6、BROWSER_PAYMENT_CONFIRMED 6、BROWSER_HUMAN_REQUIRED 3、BROWSER_ORDER_STALLED 3、CARD_SUPPLY_WALLET_LOW 1、PROVIDER_WALLET_LOW 1、DAILY_RECONCILIATION_SUMMARY 1。`CARD_STOCK_LOW` 4 条全 RESOLVED。**推送白名单已上生产**（08:29 UTC，D-272）：08:29 之后 `alert_notifications` 只新增 1 行且来自白名单类型；白名单外 6 种类型同期新增 **0 行**（只读复验）。 | 2026-09-18 08:30 UTC | operator_alerts / alert_notifications 独立 SELECT |
| **5X/20X 路线** | **305/306 `accepts_new_orders` 已 1→0（2026-09-17 07:28:51 UTC，Lemon 授权，D-245）**：两阶段方案退休、组合层结账固定 plus，20X 码下单会误买 Plus，方案改完再开。审计 `provider_route_switch_events` a31f2b1c / 9186026b（actor `lemon-via-fable`）。回滚 = 同法把两条改回 1。现在 20X 码下单会 `ORDER_ROUTE_UNAVAILABLE` 503。其余：`products` 三个 ACTIVE；`browser_card_source_selections` 三产品都指 103；`cdks` pro_20x AVAILABLE 2 / REDEEMED 1；后台 CDK 生成端点接受 pro_*（`create-app.js:358`）；intake 无产品闸门。历史 pro_20x 订单 9 单全在 09-08～09-10。**代码显示一张 20X CDK 现在就能下单进 Browser 路线**（付款后 UPGRADE_DIALOG_STOP 交人）。本轮未动。 | 2026-09-17 05:37 UTC | products / fulfillment_routes / browser_card_source_selections / cdks / orders 独立 SELECT；`cdk-service.js:13` |
| 本机 | 常驻 Browser 池 **PID 67131**（2026-09-18 07:06:55 UTC 由 supervisor 拉起，第④步 rehearsal 后），PAY/pool:lane-1，**已带 30 分钟付款后核实窗口**（`ps eww` 实测 `BROWSER_PAYMENT_VERIFICATION_WINDOW_MS=1800000`，D-269 ③）与第④步全部 browser-mvp 改动（含备用卡台真证据读取器）。上一实例 74272 于 06:53 UTC 为演练由 `stop-live.sh` 停掉。本机源码 = origin/main 最新。 | 2026-09-18 07:07 UTC | 本机 `ps` / `ps eww` / supervisor.log；app_settings `browser_worker_heartbeat_at` 07:07:07 |
| 已上线（本次 release） | 统一成功时点修正。删除提前SUCCESS写入与恢复器回调，保留核验减法/结果只读门控/有限重试/真实阶段与2秒查询。证据docs/incidents/unified-success/。 | 2026-09-16 15:36 UTC | 独立SSH/进程/DB查询与发布清单；docs/incidents/unified-success/ |
| 已知未修 | ①**结账页 403：候选修复已落代码（D-140），根因未坐实，真单未验证**：注入的 session cookie 曾按 `{url}` 放成 host-only，与网站在 `.chatgpt.com` 轮换的同名 cookie 并存；对照实验（同号同窗口同出口）复现 + 改 `.chatgpt.com` 域后结账页打开出 ₱ 报价。但本机 WAL 显示 09-07/09-08 有 7 次同样并存却到达结账页、1 次付款成功（审查 F-27），"并存即 403"不成立；真单再 403 按 D-139 转人工。修复在 `browser-mvp/src/session-bootstrap.js`（本机工作区，pool worker 从工作区启动即生效，无需服务器发布）；次要差异未处理：注入路径没有 auth.openai.com 层（付款后刷新依赖，D-134 已知）；②预检租约默认已改 900s（`run-live-pool.sh`），只在 09-09 第 4–5 次预检验证过"不再超时"，未在成功路径验证，A1 演练也验不到（F-33）；③`browser_run_events` 同一任务多次重试只落第 1 次（job_id+sequence 唯一键），后续尝试只在本机 WAL；④本机绕过连接池直连写入（09-07/08）遗留问题已清；⑤后台控制事务并发可能 `ER_LOCK_DEADLOCK`；⑥手动卡付款后无独立卡侧扣款证据；⑦审查批次 1 P1 处置更新：中途关付款开关即判失败（F-25）、点击后 kill 无核实排程（F-26）——已修，browser-mvp 本机代码，pool worker 下次启动即生效，不需要本次 v1 release；打回后重提同码丢 Session（F-34）、换账号重提 409（F-35）——已修且已随本次 release（`20260910-highvcc-open-session-resubmit-0b5639c`）发布到生产，未做真单复验 | 2026-09-10 09:10 UTC | 本表 + REVIEW_RECORD 批次 1 + 本次 release |


## 事实表之外

- 客户页 v2（09-03，`3cef082`）与夜间配色（`eba5331`，`?v=11`）已部署；成功态文案固定为 Plus，产品扩展时需改状态映射。
- 生产使用量为零的功能（三方对账案例、退款观察、灰度许可、CDK 交付记录、订单标签/备注/补发）按基线删除清单处理。
- 备用卡台导入完整快照是供给核心能力，保留；每次导入代表该卡台全部卡片。
