# 当前状态快照（2026-08-24）

## 已验证事实
- 本地 v1 测试：408 项，374 通过，0 失败，34 跳过；跳过项需要隔离 MySQL/外部条件。
- 公网 ops/plus live 与 ready 检查均返回 HTTP 200；这不等于生产业务和资金链路可用。
- 单笔真实 API 全链路测试方案已落盘：`docs/REAL_E2E_SINGLE_ORDER_TEST_PLAN_2026-08-24.md`。
- 最新本窗口测试准备提交：`072a227`。
- 2026-08-24 完成一次独立全项目对抗式审查（文档+代码+隔离测试交叉核验，无生产证据）：报告 `docs/FULL_PROJECT_ADVERSARIAL_AUDIT_2026-08-24.md`。核心资金安全机制代码级已验证（单订单唯一资金栅栏、create_direct 唯一约束、开关默认全关、一卡一单卡资格、HNSKJ 幂等键、UNKNOWN 锁定、客户侧敏感隔离、Browser 不伪造 Provider 调用）。

## 本轮进行中的优化（2026-08-24 后台）
- Lemon 授权放手改任何地方、只需记录清楚供交接（详见 `ADMIN_OPTIMIZATION_LOG_2026-08-24.md`）。
- 已完成并提交（`9f3c2a1`）：运营后台高频文案改名——派发→自动充值、Provider 写→对外扣款、供应商→上游（admin.js + index.html）；`node --test` 408/374/0/34、node --check OK。
- 待 Lemon 定（属运营偏好，未动）：①异常队列降级为订单筛选；②接单/派发拆成独立开关。方案见优化日志任务 B/C。
- 教训：`admin.js` 是超长行文件，Bash grep/sed 会返回损坏内容，本会话 git 输出也串过——核对一律用 `git diff` + `git log -1` 验证。

## 用户确认结论
- 2026-08-24 用户澄清：我方生产服务器正常；阻塞在**卡台（HNSKJ）正在升级服务器**，当前开不了新卡、也无法往新卡充值。此前"服务器尚未修好"应理解为卡台侧写能力不可用，非我方服务器故障。
- 因卡台写能力不可用，暂不执行真实开卡、卡余额充值或 Plus 充值。
- 可先由其他模型接手做审查、准备和只读验证。

## 当前实现/规划
- API 是当前可验证的正式执行器；Browser 是独立工作线，真实付款尚未达到生产闸门。
- 真实单笔测试必须并发 1，任何 UNKNOWN、重复写入或资金差异立即停单。

## 未完成
- 卡台升级完成后，开卡/卡余额充值写能力恢复与只读合同核验。
- 我方生产现场只读体检（release/迁移/服务/五类开关现场值）。
- 单笔真实 API 全链路测试（依赖卡台写能力恢复）。
- Browser 真实付款验收。

## 未验证边界
- 当前生产接单、派发和 Provider 写开关状态必须在测试前重新现场核验，不能沿用旧快照。
- 时间线已澄清（2026-08-24）：`SINGLE_SOURCE`(08-22 称生产正常) 与旧措辞"服务器尚未修好"不冲突——我方服务器正常，阻塞在卡台升级；仍需现场只读体检确认我方 release/开关现场值。
- `DECISIONS.md` 决策 ID 重复：D-069 是已提交老问题；D-089、D-109 的重复出现在其他窗口未提交改动（D-090~D-133 那批 +59 行）中，本窗口未改动该文件，待其提交后统一重编号（映射见审查报告补充节）。

## 当前阻塞
- 卡台（HNSKJ）升级中：开卡与卡余额充值写能力不可用；我方生产服务器无故障。

## 下一步唯一动作
- 其他模型先读取项目事实源和本文件；可在不做资金写入前提下进行我方生产只读体检和真实单笔测试前置闸门核对，等卡台写能力恢复后再按方案执行；不得执行资金写入。

## 协作分工
- 其他模型：负责本项目升级、优化和改造实施，并对其修改负责测试与变更记录。
- 本窗口：负责需求与方向统筹、变更审查、跨模块对齐、对抗式审查、集成验收、生产前闸门和最终真实链路安排。
- 约束：涉及生产写入、资金、不可逆迁移或跨工作线架构变化必须先停下确认。

## 前端升级工作线
- 其他模型可自主负责前端产品与工程改造，包括信息架构、导航、交互、API/数据展示联动、性能和必要配套改造。
- 本窗口负责方向审查、跨模块数据/业务一致性、对抗式审查和最终验收。
- 仅核心方向变化、真实高风险操作、生产写入及不可逆高影响变更需要停下确认。

## 验证沟通节奏
- 全系统验证连续推进，非必要不逐项请求确认；仅真实资金/生产写入、不可逆高影响变更、权限或关键方向冲突时暂停请求确认。

## 2026-08-24 最新测试前提（用户确认）
- 生产服务器已在用户浏览器中登录（本窗口尚未取得可执行终端会话，因此现场命令仍未核验）。
- 客户 Session 在用户剪贴板中；不得读取或写入聊天/普通日志。
- 卡台当前已恢复正常（用户确认；仍需运行只读检查作为运行证据）。
- 当前明确禁止真实付款；本轮最多执行到付款前的 dry-run/准备验证，不能提交 Plus 付款。

## 2026-08-24 本轮现场验证结果

- 已现场核验生产 release `/opt/pojia/releases/20260823-admin-ui-alert-7587d44`、Web/Worker/MySQL、迁移 037、只读同步 timer 和开关；接单、自动充值派发、Provider 三类写入均关闭。
- HNSKJ 只读 API 与网页核验通过：余额 75.670000 USD、18 张卡、7 张 active；未执行开卡、卡余额充值或其他写操作。
- 客户页付款前 dry-run 已提交一次当前测试 CDK + Session，页面返回“账号 Session 格式不正确，请检查后重试”；未创建订单、未进入付款页、未产生 Permit 或资金动作。
- 本轮 readiness 最终为 `ok=true`，但 `activeTasks=1`：遗留 `ASSIGN_CARD/PENDING` 任务（2026-08-22 创建）仍在生产；本轮未擅自取消。
- 卡目录只读同步发现 2 张上游卡处于 `CARD_QUARANTINED_OR_REVIEW`，并留下一个长期 `VALIDATING` intake batch；该残留和同步行为已记录，不能写成“无遗留”。
- 生产运营后台登录后逐页交叉核验未完成，当前 Chrome 没有已登录的 ops 后台标签页。
- 代码/测试事实：`cd v1 && npm test` 为 409/375/0/34。

## 2026-08-24 第二轮现场复核

- 生产与 HNSKJ 只读复核再次通过；release、迁移 037、Web/Worker、readiness 和所有写开关现场值与上一轮一致，未执行资金写。
- 已登录运营后台逐页只读核验完成：总览、订单、异常队列、资金证据核对、卡余额充值、卡台路线、Browser、卡片库存、CDK 管理均可访问。后台显示累计订单 3、自动处理中 1、三方对账异常 1、资金结果未决 0、卡余额充值待处理 0、待验证新卡 13、本地可分配卡 0；接单/派发关闭，追踪已有订单开启。
- 卡片库存页显示对账未完成并禁止新开卡；本轮没有点击创建开卡、生成 CDK、作废 CDK、路线切换、阈值保存或任何资金按钮。
- 客户 dry-run 再次失败在 Session 格式层：系统剪贴板内容仅 279 字节且不是合法 JSON；页面返回 Session 格式错误，没有新订单、Permit、卡片绑定、Provider 调用或付款页。
- 当前有效阻塞：需要取得完整合法 Session JSON 后才能继续到订单创建前最后一步；遗留 PENDING 任务、VALIDATING intake batch、quarantine/review 卡仍待运营处置，不由本轮擅自改变。

## 2026-08-24 第三轮 Session 重试

- 剪贴板内容为完整 Session JSON 前缀 + 19 个非 JSON 尾部字符；去掉尾部后前缀解析成功，包含标准 Session 顶层字段，但未记录字段值。
- 使用清理后的 JSON 前缀 + 测试 CDK 重试，页面返回“当前暂停接收新订单，请稍后再试”；浏览器未发出 `/api/v1/orders` 请求，未创建订单、未产生错误 HTTP 响应、未进入付款。
- 当前阻塞已从 Session 语法错误推进到接单开关关闭；该开关保持关闭符合本轮禁止真实资金操作的边界，未擅自开启。

## 2026-08-24 第四轮重试

- 09:14 UTC readiness 仍为 `ok=true`，但接单和派发开关均关闭。
- 再次用清理后的剪贴板 Session + 测试 CDK 尝试提交，仍返回“当前暂停接收新订单”；未发出 `/api/v1/orders`，未创建订单或进入付款。

## 最新开关与 Session 安全规则（2026-08-24）

- Session 属于高敏感账号凭证，不得发送到聊天、普通日志或交接文档；只能在用户自己的浏览器页面中粘贴。
- 仅创建订单的安全测试配置：`accept_new_orders=true`、`dispatch_new_recharges=false`、所有 Provider 写入关闭。该配置仍会创建真实订单、消耗 CDK 并写入订单/邮箱等生产数据，测试后必须关闭接单。
- 付款前派发测试配置：`dispatch_new_recharges=true` 且所有 Provider 写入关闭；仍可能创建任务、租约、卡片分配或待处理状态，不能视为零风险。
- Provider 写入或真实付款开关打开前，必须另行确认；不能把“派发开启”当成无风险。
- 用户不需要手动操作后台开关；执行窗口负责按上述配置操作并在测试后复原。
- 当前若剪贴板不是合法完整 Session，不得先打开接单后空等；应先取得有效 Session，再执行有界测试。
