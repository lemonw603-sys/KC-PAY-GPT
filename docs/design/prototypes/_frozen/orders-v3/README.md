# 订单页 v3 定稿快照（2026-09-24 01:5x UTC+8，D-357）

定稿原型：`docs/design/prototypes/step6-orders-v3.html`（提交 `1b72003`，六轮 Lemon 试用后「过」）。它的样式分两层：外壳与列表/筛选形态直接 link `_frozen/cdk-a/`（admin/workbench/cards/cdks 四份冻结 CSS，与 CDK 页同一快照）；订单页特有的 `od-*` 规则内联在该 HTML 的 `<style>` 里，随提交冻结。日常修实现不得回改这份原型；要改先跟 Lemon 沟通。

裁定来源：D-352 ④、D-355、D-356（八问）、六轮反馈（`reviews/2026-09-23-orders-v3-demo.md`）。数据形态 `data/orders-demo.jsonl`（生产 80 单匿名导出）。

结构与节奏（实现必须保持，契约 `parity/orders-page.json` 量这些）：
- 筛选条一行：搜索 flex + 状态四选分段器（角标）+ 产品/路线下拉 + 日期范围 + 近 7 天/全部分段器 + 右侧计数。
- 列：客户（按最长邮箱定宽，`width:1%` + nowrap，右内边距 0）→ 产品（左内边距 4px）→ 路线·卡台·卡尾号（左内边距 32px，主行文字 + 次行等宽卡尾号可点）→ 进度/需要我做什么（左内边距 22px，芯片 + 同行文字链接动作）→ 提交时间（右对齐、等宽数字、右内边距 28px）。
- 每码一主行；历史尝试为邮箱后 18px 小标「N ▾」，展开为行下 `tr.od-hist`（底色 s2、字 12px）。
- 只有邮箱可点开抽屉；卡尾号点开本单抽屉并定位「卡与钱」。
- 抽屉：此刻可做 → 进度（提交/成功时间）→ 客户与卡密 → 卡与钱三问 → 技术证据折叠；付款待核实顶部警示框。
- 令牌未改（`--wb-t3` 3.6:1、`--wb-warn` 3.9:1 的对比度问题登记待 Lemon 定）。
