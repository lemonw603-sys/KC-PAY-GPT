# CDK A 定稿快照（2026-09-21 UTC+8）

沿用 D-313 A 一条流水线与候光规范，叠加 D-314～D-318 用户裁定。冻结样式与原型必须独立于运行实现，日常修实现不得跟改快照。

首屏顺序：四格统计 → 生成条（用途/有效期是本轮已确认新增控件，单张同样可设）→ 筛选与主表。批次退为筛选/导出。生成后提示条＋当前批次筛选，备码仅补登记，批量选择限定本页并明确数量。

模式 Operate；沿用现有外壳/令牌/字体，不发明新视觉系统。按原型 A 的分组、边界与密度制作；无 raster 资产。定稿原型带完整外壳，测量树不含原型专用类；契约独立比较几何，业务另由隔离库与浏览器测试证明。

此次验收须覆盖桌面1440与移动390、空态/失败态/选择态/展开态。几何通过不代表业务正确，静态契约不代表按钮可用。
