# 诊断稿外部样式冻结

D-325交互稿经用户“没问题”确认后，冻结当时引用的admin.css/workbench.css；内联原型布局规则保持原样。原型不再随真实页面全局样式变化而漂移。字体仍通过既有原型服务同源加载。

正式实现为独立diagnostics.css，几何契约 `docs/design/parity/diagnostics-page.json` 对照已确认交互稿，不以实现自身生成一个自证基准。保留真实筛选/分页/业务数据导致的内容高度差异，不强制比较整页高度。
