# V2 架构：CDK 驱动的统一改造（2026-09-14 头脑风暴产出，已并入 D-219 审查修正）

> 状态：**建议稿，待 Lemon 确认。** 09-14 09:30 UTC 对本稿做了对抗式审查（D-219），七处修正已并入。确认后进 DECISIONS、PROJECT_MAP §5 改为指向本文。
> 与 `PLAN_2026-09-14.md`（两天冲刺）的关系：两天冲刺是 V2.0 的第一步，不冲突。

## 0. Lemon 的需求，一句话

**客户拿到 CDK 就等于付了钱，剩下的系统自己走完；运营只做三个选择（哪条路、哪个卡台、接不接单），只在系统真的解决不了时被叫。系统要简洁、稳定，不要花里胡哨。**

## 1. V1 的真实形态（不是文档说的，是我翻代码和生产看到的）

```
                    ┌──────────────────────────────────────────────┐
                    │  服务器 (144.34.180.184)                       │
  客户 ──CDK+Session──▶ pojia-web (73 端点) ──▶ orders/cdks/cards   │
                    │       │                        │              │
                    │  pojia-worker                  │              │
                    │   SUBMIT_RECHARGE ──┬── API ──▶ ZZSHU (黑盒)  │
                    │                     │                         │
                    │                     └── BROWSER ─▶ dispatch job│
                    │  5 个 timer (同步/对账/告警)                    │
                    └──────────────┬───────────────────────────────┘
                                   │ SSH 隧道 13306
                    ┌──────────────▼───────────────────────────────┐
                    │  Lemon 的 Mac                                  │
                    │  production-live-pool-worker (1 lane)          │
                    │    └─▶ BitBrowser (127.0.0.1:54345) ─▶ 菲律宾出口 │
                    │  mihomo · LaunchAgent · supervisor             │
                    └──────────────────────────────────────────────┘
```

**四个系统的真实状态：**

| 系统 | 真实形态 | 问题的本质 |
|---|---|---|
| **后台** | 4 个一级视图、18 个视图函数、73 端点、18 类告警。三周实际操作：设置改动 26 次（19 次是付款开关）、CDK 操作 57 次 | **功能是按"能做什么"长的，不是按"运营要做什么"长的。** 真正在用的集中在 5 个操作，其余是"有用但找不着" |
| **API 路线** | `workflow-handlers.js:228` 按 `executor_kind` 分叉后完全独立；ZZSHU 拿卡号+Session 做浏览器自动化；失败零原因 | 它是黑盒。但它是"那家卡台做的"，**绑死合理**，不该改 |
| **Browser 路线** | 52 个模块、本机 1 lane、BitBrowser 绑本机；执行链有 stage/诊断/现场保留；死点缩到一个函数 | **它是通用引擎，却跑在一台个人电脑上**。项目从没调研过 BitBrowser 能不能上服务器 |
| **客户入口** | 九阶段圆环按 Browser 设计；失败时显示"卡密可重兑" | 客户页知道太多——它按路线的内部阶段画进度，路线一换就不对 |

**共享的部分（这是好的，V2 要保住）**：订单状态机、CDK 状态机、资金栅栏（payment_permits）、消费账本、审计事件。今天 15 次失败一分钱没乱扣，靠的就是这些。

## 2. V2 的中心思想：CDK 驱动

V1 是**功能驱动**——每个功能一块积木。V2 是 **CDK 驱动**——一切围绕客户一次购买的生命周期，六个面各管一段。（D-219 审查把中心从「订单」改为「CDK」：订单是尝试，CDK 才是购买；chenxing 那 12 单在 V2 里是一个 CDK 的 12 次尝试。）

```
                         ┌─────────────┐
                         │    CDK      │  ← 唯一真相：客户的一次购买
                         │ (一次购买)   │     D-219：1 个 CDK 平均 1.43 单、最多 12 单
                         └──────┬──────┘     订单只是 CDK 下的一次尝试
                                │ 1..N 次尝试
       ┌──────────┬─────────────┼─────────────┬──────────┬──────────┐
       ▼          ▼             ▼             ▼          ▼          ▼
    展示面      执行面        资源面        控制面     通知面      对账面
   (客户页)   (执行器)      (卡供给)       (后台)     (告警)    (账本vs卡台)
```

- **展示面**只读 CDK 的交付状态，不知道路线、不知道第几次尝试
- **执行面**是可插拔接口（submit / verify / cancelRenewal），两种实现（API / Browser），CDK 不知道用的哪个
- **资源面**按执行器分池（卡台不同），但管理方式一套
- **控制面**是三个开关 + CDK 生成 + 需要我队列 + 今日订单表，一页工作台
- **通知面**按类别决定推不推，不按数量
- **对账面**每日自动比对账本与卡台，差异进"需要我处理"队列

**今天的事故对照这个图，每一个都是某个面越界了：**

| 事故 | 越界 |
|---|---|
| chenxing 12 单被当 12 个陌生单 | 订单不认 CDK——12 次尝试被当成 12 次购买 |
| 切 API 卡在等卡 | 控制面不知道资源面的状态 |
| 客户看到"可重兑"时运营正在接手 | 展示面、控制面各看各的 |
| 你一天做了 8 种后台操作 | 控制面暴露的是功能，不是订单 |

## 3. 六个面的 V2 边界

### 3.1 执行面：一个接口，两种实现

```
interface RechargeExecutor {
  submit({ attempt, card, session, product, fromPlan })   → { status, evidence }
  verify({ attempt })                                      → { plan, activated }
  cancelRenewal({ attempt })                               → { cancelled }
  // 交付 = 三段都成功（D-219 发现 2：成功一单在生产里是 PAYMENT_SUBMIT →
  //   PAYMENT_CONFIRMED → PLUS_ACTIVATED → CANCELLATION_CONFIRMED 四步，
  //   原稿只写了第一步）。每段可独立重试、独立标 UNKNOWN。
  // submit 的 status ∈ SUCCESS | FAILED_BEFORE_PAYMENT | UNKNOWN_AFTER_PAYMENT
  // FAILED_BEFORE_PAYMENT 必须带 stage + diagnostic
}
```

- **APIExecutor**：包 ZZSHU。它零原因，接口层把它的 `failed` 统一映射成 `FAILED_BEFORE_PAYMENT { stage: 'provider', diagnostic: null }`——**诚实地标"不知道"**
- **BrowserExecutor**：现有 52 模块收进来。它已经有 stage/诊断
- **切换**：`fulfillment_routes.accepts_new_orders` 已有；V2 加**切换前校验**——目标执行器的卡池有可用卡、执行器健康——不满足就拒绝并说原因。**这是"一键切回"的全部含义：一键，且不会切到一个跑不起来的地方**
- **切换必须解耦卡台**（D-219 发现 5）：今天 06:36 Lemon 切一次路线，后台 7 秒内同时动了路线和卡台两条记录——运营看到一个按钮，背后是两个耦合开关。V2 把卡台做成执行器自己的属性：切路线不动卡台；要换卡台是另一个明确的操作
- **5X/20X 的两件事**在接口里是两个参数：`product`（充哪个套餐）和 `fromPlan`（当前是什么）。Free→5X 和 Plus→5X 走同一个接口，执行器内部按 `fromPlan` 决定走"新订阅"还是"升级"

### 3.2 资源面：按执行器分池，管理一套

```
卡池 A (hnskj)    ──▶ APIExecutor    （绑死合理：API 是那家做的）
卡池 B (highvcc)  ──▶ BrowserExecutor（可接任何卡台，已有 browser_card_source_selections）
                       ↓
              统一管理：库存阈值 · 自动开卡 · 余额同步(5min) · 缺卡告警 · 账本推算
```

- **每卡单数与开卡金额按产品**（D-221，Lemon 定）：Plus **3 单/卡、开卡 $50**；5X/20X **1 单/卡、开卡按单价**。两路线一致。现状是全局 `card_max_successful_payments=3` + 全局 `default_open_card_amount=16`，且后者**没有后台入口能改**——V2.0 设置页把这两项做成按产品，自动开卡之前必须先改（否则开出的卡只够一单）
- **自动开卡**：highvcc `openCard` 去掉人工确认词，改为"库存 < N 张就开一张"，每天上限由设置定。hnskj 走 worker 现有 `PURCHASE_CARD` 路径（生产从未成功过：09-05 三次失败是卡台"内测结束升级"，之后没再试）。**200 单/天 = 67 张/天，这条不自动化其余全白做**
- **余额口径**：MIN(同步余额, 账本推算)——代码已改好但**不能直接发**：D-218 实跑发现它会把「手动补过钱的卡」判成不可分配（`funded_amount` 不随补余额更新，`1652` 卡上 $50 被判 −16）。**先由 Lemon 在 D-218 三选一**（补钱时抬 funded_amount / 放宽规则 / 放弃补钱只开新卡），再发
- **矛盾五**（两个卡台能否共用）：**待 Lemon 与大脑一起测**。技术上 ZZSHU 接受任何卡号；业务上是否允许未知

### 3.3 控制面：5 页，职责重划（D-225，不照抄基线也不盲目合并）

原稿"收成 4 页"合并了首页+订单——把基线（Lemon 09-06 确认的 5 页）当参照又擅自合并，是自作主张（D-224 发现 C）。但退回基线原样 5 页也不对：基线也只是过去的决定。Lemon 授权按真实分析给最优。基于真实使用量（CDK 生成 53 次最高频）+ 系统愿景（"运营只在解决不了时被叫"）重新分工，仍是 5 页，但每页职责变了：

| 页 | 内容 | 定位 |
|---|---|---|
| **工作台** | 三开关（接单/路线/卡台）· 今日四数字 · **"需要我处理"队列（核心）** · **发 CDK 快捷入口** · 卡台异常切换 · Worker 心跳 | 日常 90% 停这里；系统顺时只看队列空不空 |
| **订单** | 实时运营板：七列（客户/产品/阶段/卡/付款证据/停留/需要我做什么）+ 详情四块 | 异常下钻与全局，不是日常必看 |
| **CDK** | 列表 · 作废 · 批次筛选 · 导出（发码在工作台） | 管理 |
| **卡片** | 两卡台并列 · 开卡 · 余额 · 导入 | 供给 |
| **设置** | **按产品**：每卡几单 / 最低余额 / 开卡金额（D-221）· 五决定默认值 | 低频 |

**和基线的区别**：工作台 = 基线首页 + 把最高频的发码提上来；订单从"日常必看"降为"异常下钻"。
**和原稿 4 页的区别**：不把七列订单表塞进工作台（会过载）；工作台专注"决策+异常+发码"（动作导向、短），订单专注"明细"（浏览导向、密）。

**诊断 / 审计 / 对账明细 → 折叠成"高级"入口**，日常导航不出现。对账的**结果**不折叠：差异直接进"需要我处理"队列（§3.6）。

**"需要我处理"队列是控制面的核心**：只放系统真的解决不了的事——付款结果不明、人机验证、缺卡且自动开卡失败、同一 CDK 连败 N 次。**其余一律不进队列，不推手机。**

### 3.4 展示面：四态 + 一个时间承诺

```
处理中（预计 X 分钟）        ← X 按最慢的执行器 + 一次重试算（Lemon 定：留足时间）
需要你重新提供登录信息       ← WAITING_FOR_SESSION（D-219 发现 4：客户必须行动的状态不能并进"处理中"）
已开通                       ← CDK 交付完成
没成功，请联系客服            ← 终态失败；不再显示"卡密可重兑"，由客服判断
```

九阶段圆环**降级为内部诊断视图**，客户不看。理由：它按 Browser 的阶段画，API 路线 95 秒就完，两条路体验完全不同——而你定了"客户不感知路线"。

### 3.5 通知面：只在需要人时响

现有 18 类告警收成 **4 类**，且**推不推手机由类别决定，不由数量决定**（D-219 发现 3：两天 77 条告警推了 76 条，真需要人的只有 10 条；而唯一没推的 `CARD_STOCK_EMPTY` 恰是最该推的）：
- **需要你**：进"需要我处理"队列的那几种——付款结果不明、人机验证、同一 CDK 连败 N 次——**推**
- **供给**：缺卡、开卡失败、余额低于阈值——**推**，单独通道，不可静音
- **资金异常**：重复扣款、账本与卡台交易对不上——**推**，最高优先级（原稿漏了这类）
- **信息**：提交、成功、同步、余额变化——**只进后台，永不推**

### 3.6 对账面：账本 vs 卡台，每日自动（原稿漏了，Lemon 明确要的"基础对账能力"）

现状：`card_transactions` 41 行、12 张卡、最近同步 09-14 11:39；`card-consumption-audit.js` 是只读比对，**从未接进任何流程**。
V2：每日一次自动比对「消费账本」与「卡台交易记录」，差异进「需要我处理」队列，并作为"资金异常"告警的唯一数据源。
**不做**：三方对账、退款自动识别（基线已定不做；`refund_cases` 代码引用仅 3 文件 7 行，与对账零交集，可删）。

## 4. 精简清单（基于真实使用量，不是猜）

### 4.1 直接删（三周零使用且基线已列入删除）

> **执行须知（D-224）**：下表行数多取自 2026-09-06 基线，Browser 主链路上线后已有多处变化——`payment_permits` 0→26、`cdk_delivery_events` 0→43 都已在用，`card_funding_manual_actions` 也从 0→1。**删任何表前必须当场重查行数与代码引用，不照搬本表。**

| 对象 | 证据 |
|---|---|
| `order_tags` `order_notes` `order_compensations` 表及其端点/UI | 0 行，基线删除清单 |
| `refund_cases` 及退款观察 UI | 0 行，08-19 后无写入，基线"当前明确不做" |
| `checkout_artifacts` `browser_artifact_secrets` | 0 行 |
| `card_funding_manual_actions` | 0 行，08-27 后无写入 |
| `card_stock_jobs`（旧每分钟自动开卡架构） | 09-05 后无写入，CURRENT_STATE 标"已废弃" |
| `browser_interventions` | 1 行，09-06 后无写入 |
| 后台 `exports` 端点 | UX 4.2「CSV 导出不知道是干嘛的」 |
| 后台 `reconciliation-cases` 页面 | 1 行；UX 4.3「从没用过」。由 §3.6 对账面替代：差异不再是一个页面，是队列里的一条 |

### 4.2 收（有用但分散）

| 现在 | V2 |
|---|---|
| 18 类告警 | 4 类（需要你 / 供给 / 资金异常 / 信息） |
| 73 端点 | 预计 ~30（订单 6、CDK 4、卡 6、设置 4、开关 3、诊断 ~7） |
| 4 视图 + 18 函数 | 5 页 + 1 高级入口（工作台/订单/CDK/卡片/设置，职责重划） |
| 20 处弹窗输入、13 处弹窗确认、19 处密码 step-up（基线普查） | 确认只保留：开卡、打开付款、作废 CDK、关闭订单（基线已定） |

### 4.3 保留不动

订单/CDK 状态机、资金栅栏、消费账本、审计事件、Session 替换、卡资格 SQL（换新口径）。

## 5. 迁服务器：三条路，云 Windows 最优（D-225 调研：BitBrowser 无 Linux 版）

**2026-09-15 官网实查**（bitbrowser.net/download）：BitBrowser 官方只有 **Windows 64 位 + macOS(Apple Silicon/Intel)**，**没有 Linux 版**；Local API（54345）依赖桌面客户端，客户端无 Linux → Local API 在 Linux 无官方支持。

要解决的真问题是"不依赖 Lemon 的 Mac 一直开着"，**不等于"迁 Linux"**。三条路：

- **路 3（最优）：云端 Windows 服务器**。BitBrowser 有 Windows 版，开一台 Windows 云主机（4 核 8G、disk>50G、RDP 保持会话）装 BitBrowser + worker，Local API 照旧，**代码几乎不改**——跨境电商跑指纹浏览器的成熟做法，绕过"无 Linux 版"。
- **路 2（后备）：headless Chrome 重做 runtime**。给 `chrome-control-runtime` 补代理（mihomo 服务器可跑）+ 指纹 + 扩展；Linux 可跑，但指纹质量能否达到 BitBrowser 水平未知（风控风险高），工作量大。
- **路 1（已排除）：BitBrowser Linux 版** —— 官网无此版本。

**V2.2 = 路 3**：本机 Mac 换成云 Windows。前提从"调研 Linux 版"变成"开一台按量 Windows 实例试一天，验证 BitBrowser Local API 可控"——小成本可验证。**这让 200 单/天的关键路径有了确定可行的方案，不再赌一个不存在的 Linux 版。** 在 V2.2 落地前，Mac 仍需开着（V2.0/V2.1 期间的约束）。

## 6. 分阶段（三天时间盒，Lemon 2026-09-14 定，D-222）

| 阶段 | 日（Lemon 定的时间盒，D-222） | 架构目标 | 可见成果 |
|---|---|---|---|
| **两天冲刺** | 09-15 | Browser 主线能跑真单且失败可诊断 | 死点定位入库、切换校验、A3 发布 |
| **V2.0** | 09-16 | 执行面接口三段 + 切换解耦卡台 + 资源面按产品 + 控制面 4 页 | 一键切换不踩坑；后台每天只看工作台 1 页 |
| **V2.1** | 09-17 | 5X/20X 进接口 + 展示面四态 + 通知面 4 类 + 对账面 + 精简落地 | 客户页统一；只在需要时被叫；端点减半 |
| **V2.2** | 09-18 起 | Browser 迁服务器 + Plus→Pro 升级 | 真正无人值守，Mac 可以关（前提：BitBrowser Linux 调研） |

**验收口径（D-222，不再用时间）**：每个交付物的完成 = ①针对性测试通过（含对抗性夹具）②`state-check`/`wrapup-check`/`ready-check` 相关项一致 ③生产只读复验。**稳定性是运营期观察，不作交付门槛。** 每天的可当场核实完成定义（DoD）与外部依赖见 `PLAN_2026-09-14.md` 与 D-222 表格。

## 7. 要一起做的（不是大脑能单独定的）

1. **矛盾五实测**：ZZSHU 能不能用 highvcc 的卡——一单就知道
2. **BitBrowser Linux 调研**：官方有没有服务器版——决定 V2.2 走哪条路
3. **"需要我处理"的边界**：哪些情况算"系统解决不了"——这决定了告警推什么、后台队列放什么，**是整个 V2 最重要的一条线**，只有你能画

## 8. 这份方案的弱点

- **接口统一要动付款路径**，是全系统最敏感的地方。缓解：先在 Browser 侧做（它已有 stage/诊断），API 侧只包一层，不改 ZZSHU 调用
- **后台 4 页是重做不是改**，2366 行前端要重写。缓解：先做工作台一页，其余三页沿用 V1 直到 V2.1
- **5X/20X 的 `fromPlan` 逻辑没有真实样本**——D-138 之前决定不做升级，是因为"业务量很小"。V2.1 做之前要 Lemon 确认现在业务量是否支持
- **三天时间盒（D-222）极紧，控制面是最大工作量**（2366 行前端）。按 D-222 补记**不预设降级**：归执行 B 窗口专做、大脑当场验收；若量级确实超一天，是"多窗口够不够"的问题，摆到桌面和 Lemon 定，不私自砍成半成品。**另：D-224 审查发现基线是 5 页（首页/订单分开），4 页是本方案自作主张，页数待 Lemon 定。**
- **新验收口径只证"做对了"，证不了"跑久了稳"**——稳定性仍需运营期观察，只是不再拿它当交付门槛（不再用"连续 N 天"）。放量前要补一个后台自动统计的成功率/失败分布，作为运营期里程碑的数据源
