# V2.0 执行账本（唯一，不依赖对话上下文）

> **这是 V2.0 升级改造的单一执行账本。** 接手先读 `AGENTS.md` 四份顺序 + 本文，能用 `state-check.sh` 对现场。
> 进度、真实地图、每个面的确认方案、验收证据全在这里——**不靠读几十轮对话，不靠转述**。
> 相关决策：D-219（六个面）、D-221~D-228（三天盒/按产品/审查/调研/控制面/深化/销卡）。
> 建立：2026-09-15 01:50 UTC，大脑窗口。

> **2026-09-17 起的阅读指引（D-252）**：**当前唯一有效的方案 = §3.A 里「面一～面五」五段定稿 + 「整体连贯审补丁」+ 「做的顺序」**。§2（测绘）、§2.8（供卡核查）、§4（大摸排）、§5（减法审查）是 2026-09-15 的过程证据，其中的结论凡与五面定稿冲突的以定稿为准（已知过时点：§2.1 highvcc「无 API」、§2.8/A4「主力 highvcc/hnskj 排后」、§4.2 缝 12「highvcc 无 API 删卡」仍对、§5「A6 砍」仍对、§5.1 删表清单以面五④为准、D-219 发现 3「缺卡告警没推」已被面四纠正）。§3.B/§3.C 的 B1-B4/C1-C3 只剩「已并入」指针。落实每块先按该面定稿里的「现状证据」重查一遍代码/生产，不凭 §2 的行号。

## 当前进度（**接手先看这里**，每轮更新）

| 阶段 | A 组（执行+资源） | B 组（通知+看板） | C 组（展示+控制） |
|---|---|---|---|
| **0 真实测绘** | ✅ 读透（§2.1/2.2/2.3） | ✅ 读透（§2.4/2.5） | ✅ 读透（§2.6/2.7） |

> **测绘诚实说明（2026-09-15，含整体自查结果）**：🟡 = 已读透结构/关键路径、够支撑方案方向,**不是每行读透**。**2026-09-15 整体自查(供卡颗粒度,A/B/C/D 四批)证实这个预警是真的**——B/C 组测绘查出多处不准:§5.1删表"删了不会死"是账面推断(多表overview在读)、§2.4告警产生点4处实为7处、§2.5 getOverview读20+表(面间坑远超账本认知,漏card_sync_jobs)、§2.2 API落表行号不准。**都不推翻方案方向,但坐实一条硬门槛↓**
> **【阶段3硬门槛】每个面落实前,必须先像这次自查一样读透对应代码(重查行数/引用/字段),不凭本账本 B/C 测绘直接改。** 尤其 C2/C3(overview 是牵连20+表的巨物)。
| **1 逐面方案 + 审** | ✅ 过审+排序（§3.A） | ✅ 过审（§3.B） | ✅ 过审（§3.C） · **三组全过审+6缝已补** |
| **2 整体大摸排** | ✅ **完成**（正向6段+逆向6处+减法审查+供卡深挖+整体自查A/B/C/D，2026-09-15） | | |
| **2.5 业务实际终审** | ➡ 改为 D-243「清账本·逐面重问」（D-244 协作方式）：**面一 卡台与路线 ✅（D-246）→ 面二 供卡 ✅（D-247）→ 面三 执行/交付 ✅（D-248）→ 面四 通知/对账 ✅（D-249）→ 面五 展示/控制 ✅（D-250）**。**五面全过 → 整体连贯审 ✅（D-252）→ 下一步 D-243 步④落实，顺序见 §3.A「做的顺序」** → 面四 通知/对账 → 面五 展示/控制（顺序可调） | | |
| **3 落实** | ⏸ 待六面过完 + 整体连贯审（D-243 步③）；C2 隔离直调验证可先行（Lemon 当次确认） | | |

> **流程纠正（2026-09-15，Lemon 指出我跳阶段）**：我在①阶段0 B/C 组只做到结构级(5处没读透、地基没牢)②阶段1 三组方案 Lemon 一组都没拍板 的情况下，就去谈阶段2大摸排——**跳阶段，违反 §0"前一个不过不进下一个"**。根因＝用产出密度抢跑。**§3 的三组方案降级为草稿**。**退回阶段0：先把六面(尤其 B/C)测绘读到 A 组资源面那个透度，再逐组出方案、Lemon 逐组拍板，三组全过才进大摸排。**

**下一步 = 阶段 2.5 业务实际全审（2026-09-17 Lemon 加，D-242）**。文档层面阶段 0/1/2 已走完，但 Lemon 指出"没按业务实际最后审过"——落实前须把六面方案（A/B/C 全部，从 A 组起）拉到真实业务里再过一遍，把账本里"照文档推、没对上真实"的地方（如 A4 hnskj 排后错判）逐条揪出改。全审过了才进阶段 3 落实。落实顺序与硬门槛见 §6；**每面落实前先跑该面的"读透+行数+引用"核查，不凭 B/C 测绘直接改。**

## 0. 工作流程（Lemon 2026-09-15 确认，四阶段 + 三条贯穿原则）

**四个阶段，前一个不过不进下一个：**

| 阶段 | 做什么 | 谁做 | Lemon 花时间吗 |
|---|---|---|---|
| **0 真实测绘** | 摸六个面的真实代码/生产，对照文档，过时/做错/没落地的全现形 → 真实地图（§2） | 大脑窗口 | 不用 |
| **1 逐面方案 + 分组审** | 每个面：真实现状 + 怎么改 + 影响哪些面 + DoD；按耦合分三组给 Lemon 审、拍板（§3） | 大脑出，Lemon 审 | 审方案，不重提需求 |
| **2 整体大摸排** | 六个面方案拼起来，验全局连贯（下方清单）→ 没缝才算方案定稿（§4） | 大脑出，Lemon 一起过 | 一起过一次 |
| **3 落实** | 开专门执行窗口，强耦合先串行、弱耦合再并行；每面做完给真证据（§5） | 执行窗口 | 只看验收证据 |

**阶段 2 大摸排具体查什么（面间连贯）：**
- **数据流首尾接得上**：一个 CDK 从兑换到交付，端到端走一遍六个面，数据不断链。
- **状态在面间传递不丢**：执行面的结果，展示面/通知面/看板读得到、读得对。
- **无重复、无冲突、无缝隙**：没有两个面抢做同一件事；没有面间规则打架（如销卡时机 vs 一卡多单）；没有"谁都以为对方做了"的缝。
- **红线闭环**：资金安全、销卡、零税、订阅成功不再动，在拼起来的全链路上仍然成立。
- **主动发现 > 对照清单（Lemon 2026-09-15 强调）**：巡检清单（`UX_PUNCHLIST` 23 条 + `SYSTEM_REMEDIATION` 19 条）只是**已发现问题的一部分**，大摸排拿它当**对照子集**确保不漏，但**绝不以它为边界**——重点是系统走查一个 CDK 的完整旅程，**主动找清单外、还没人列过的新缝**。做着做着退化成"逐条核对清单"＝失败。

**贯穿原则一：需求灵活变通。** 需求不是一次冻死（销卡就是深入才长出来的）。发现前期没考虑到的、或面间打架的，**大脑主动停下跟 Lemon 核对，不自己拍板改需求**。

**贯穿原则二：一贯对着真实核实。** 每个面的设计/落实/验收都对真实代码和生产核实（CLAUDE.md 三道闸门当动作做），不回文档拍脑门。验收由测试（真实响应）+ 脚本 + 生产只读复验证明，不由自述。

**贯穿原则三：每轮必须有给 Lemon 看的文字输出。** 只调工具不写字，Lemon 那边看到的是空白（2026-09-15 实际发生过：连续几轮无可见输出）。任何窗口每轮至少写一句"在做什么 / 发现了什么"。**超长会话是诱因**——所以 V2.0 落实阶段用专门的新窗口，靠本账本恢复状态，不靠对话历史。

## 1. 面的分组（阶段 1 分三组审）

- **A 组｜执行面 + 资源面**（强耦合）：接口三段 submit/verify/cancelRenewal、切换解耦卡台、按产品每卡单数/开卡金额、自动开卡、余额口径、**定期销卡（新，D-228）**、付款不明先 verify（D-227）。
- **B 组｜通知面 + 对账看板**（数据共享）：告警理顺（推手机 vs 进看板两维度）、**余额变化要通知（D-228）**、运营看板（单数/花费/卡数/两路线耗时/异常支出含拒付/当天突发，D-227）、自动对账。
- **B 组前置（D-230）**：**highvcc 交易从未同步进 `card_transactions`**（两卡台交易接口语义不兼容：hnskj 按卡查、highvcc 按账户查全部）→ 看板算不出 Browser 路线花费/拒付/回收，**做看板前必须先补这条同步**。
- **C 组｜展示面 + 控制面**（前端）：客户页四态、九阶段降内部诊断、后台 4 页（工作台/CDK/卡片/设置，D-226）、精简删表（当场重查行数，D-224）。

## 2. 真实测绘地图（阶段 0 产出，待填）

> 每个面一节：真实代码路径 · 真实表/列 · 生产在跑的 release/开关 · 真实运行行为 · 文档 vs 真实的差异（过时/做错/没落地）。

### 2.1 资源面 · 卡台能力（销卡相关，2026-09-15 测绘）

**两个卡台的真实 API（`v1/src/providers/*.js`，我们已接的）：**
- **highvcc**（backup-a）：**不是卡台 API,是"网页 token 扒取"同步工具**(2026-09-15 读代码+Lemon 确认钉死)。真相:provider 用**你在后台手动贴的网页登录 token**(`app_settings.highvcc_access_token_ciphertext`,`/highvcc/token` 端点 setToken),带 `Bearer {token}` 打 `www.highvcc.com` **内部地址**读数据;token 会**登录态过期**(401 报"登录/过期",过期需 Lemon 重贴)。→ **highvcc 无官方 API**:开卡/删卡你都在网站手动做。**但「读卡」是自动的**——`pojia-highvcc-snapshot-sync.timer` 每小时用这 token 拉全量卡、刷 `cards.current_balance`(§2.3 生产核实:enabled、次次 SUCCESS),唯一单点是 token 过期时同步失败、需重贴,**不是"随时不可用"**。账本任何"highvcc newCard API/自动开卡"表述都是错的,已删。**三件事务必分清:①开/删卡=手动 ②读卡余额=timer 自动 ③钱包账户余额+交易流水=至今未入库(§2.4/D-230)**;按**无官方 API 卡台**对待(开删手动、读卡自动)。
- **hnskj**（legacy-primary）：有 `withdraw`（`POST /cards/{id}/withdraw`，提走卡余额），已接但没接成自动流程。

**销卡机制对比：**
- highvcc = 删卡（删了余额自动回钱包），Lemon 手动能做；**highvcc 卡台没有删卡 API（Lemon 2026-09-15 确认，目前都是他手动删）** → **highvcc 侧自动销卡不可行**。务实方案：系统判断"该销哪些卡（已交付+不再服务新单+到销卡时机）"→ 列待销清单 → 提醒 Lemon 手动删（**半自动**，符合"系统解决不了才叫人"）。这是 A 组销卡方案的硬约束。
- hnskj = withdraw（提余额），API 已接。

**文档 vs 真实**：无既有文档描述销卡能力，本次测绘首次落实；D-228 记的需求现状＝highvcc 侧能力未知、hnskj 侧有 withdraw 未自动化。

### 2.2 执行面 · 骨架与 verify 现状（2026-09-15 测绘）

- **路线分叉点**：`workflow-handlers.js:228` 读 `recharge_executor_kind`（API/BROWSER）→ 235 分 BROWSER、255 分 API，分叉后两条独立。V2 执行面接口要统一，就动这里。
- **交付四步（两路线粒度不同，2026-09-15 读透）**：Browser 侧细分四步落 `browser_operations.operation_type`（PAYMENT_SUBMIT/CONFIRMED/…）；**API 侧是 ZZSHU 单次黑盒**（落 `provider_calls`+`recharge_attempts`，写入在 `provider-call-repository:23`/`recharge-attempt-repository:425`；`workflow-handlers:399/513` 是路由分叉非落表点，自查B5订正），无细分步骤，ZZSHU 拿卡+Session 一次做完全套（含取消续费）。→ **执行面接口统一时 API 实现的 verify/cancelRenewal 多为 no-op 或映射单次结果，不能假设两边都有四步。**
- **verify 能力已存在且完整（关键，利好 D-227 + V2.1）**：`chatgpt-post-payment-verifier.js`（深读 2026-09-15）——
  - `confirmPlus()`：轮询 `/backend-api/accounts/check` 核实账号 `hasActive && plan 含 plus` → **正是 D-227"付款不明先 verify"，已存在**。
  - `confirmCancellation()`：读订阅 → 调 `/backend-api/subscriptions/cancel` 取消续费 → 确认 `willRenew=false`。交付第四步，已存在（apple/google 来源不支持退，会标 `unsupportedOrigin`）。
  - `openUpgradeDialog()`：**5X/20X 升级雏形已存在**——账号已 Plus 后开升级对话框、选 tier、停在"Confirm plan changes"**不按 Pay now**。→ **D-224"5X/20X 无真实样本"要更新：代码骨架有了。**
  - 读订阅真实字段：`hasActive` / `subscription_plan` / `will_renew` / `purchase_origin_platform`。
- **SUBMIT_UNKNOWN 不是死锁**：状态机允许 `SUBMIT_UNKNOWN → RECHARGE_SUCCESS / CANCELLATION_PENDING`（`order-status.js:59`），worker 有 `POLL_RECHARGE` / `RECHECK_CANCELLATION` 轮询已有订单（`worker-runtime.js:20`）；`confirmPlus` 在 Browser 付款流程里被调（`payment-executor.js:265`、`shared-live-composition.js`）。→ **D-227"付款不明先 verify"零件齐全，是接线/调线级别，不是重建**。最后一厘米（POLL_RECHARGE 对 Browser 的 SUBMIT_UNKNOWN 具体怎么核实）留方案时钉。

### 2.3 资源面 · 卡供给现状（2026-09-15 当场重查）

- **卡资格 SQL**（`card-inventory-eligibility.js`）：A3 账本推算口径**仍未发布**（生产 `7efec865` ≠ 工作区 `525b7e1b`）；配套已齐（补余额开关已关成 `false`），**可以发了**。三处读 `card_max_successful_payments`。
- **每卡单数 = 全局 `card_max_successful_payments=3`**（后台能设 1-4），**非按产品**。5X/20X 启用要按产品——V2.0 建。
- **开卡金额 = 全局 `default_open_card_amount=16`**，**无后台写端点**（`create-app` 无）。Plus 3 单需 $50 → V2.0 加按产品端点。
- **自动开卡（纠正）**：只有 **hnskj**——`PURCHASE_CARD`(worker 任务,provider purchaseCard,幂等键)是活的自动开卡能力,现 `card_auto_replenishment_enabled=false`+09-05后没成功;**改作故障补充、非主力(D-235,§2.8)**。**highvcc 无自动开卡、也无 API,后台手动开——但产能足(100/天可扩)、是 V2 主力供卡**(Lemon 2026-09-15)。
  - 更正（2026-09-15，Lemon 质疑后复核）：此前写"flag 关、有人工确认词"**两处都不准**。服务端 `openCard` 的 `confirmation` 是机器校验字段（须等于 `开卡 {vid} {amount}`），按 09-06 基线**由页面填充、运营不手打**；后台卡片页有完整开卡表单（卡段/金额/报价/开卡按钮/钱包）。我此前引用的 `--confirm` 证据来自本机 CLI，**不是业务路径**。
- **余额同步（2026-09-15 生产核实精化）**：`pojia-highvcc-snapshot-sync.timer` 每小时自动跑、`enabled`、次次 `status=0/SUCCESS`——但它**只刷卡余额**（`cards.current_balance`，走 manual-card-import，见 `highvcc-snapshot-sync-service.js` 顶部注释：nothing writes cards directly / import service refresh balances），**不写钱包账户余额、不写交易流水**（那两样才是 §2.4/D-230 说的 highvcc「没入库」，[D]/[E] 实测 0 条）。近几轮日志 `committed=NO_CHANGE`＝token 有效、拉到卡、卡数据没变所以不写（**非空转、非过期**）。`pojia-card-read-sync.timer` + 订单触发 `order-demand-sync` 另在跑（hnskj 侧，未细核）。
- **销卡**（见 §2.1）：highvcc 无 API（半自动），hnskj 有 `withdraw`。
- **费用记录缺口（D-229）**：开卡费 $0.5 **未记进** `card_transactions.fee`；**backup-a(highvcc) 交易一笔都没有** → Browser 路线费用在后台空白，**B 组做看板前必须先补数据源**。
- **A 组待深挖（仅剩一项）**：API 侧的交付四步落在哪张表（Browser 侧落 `browser_operations`）。

### 2.4 通知面 · 推送机制（2026-09-15 测绘）

- **推送逻辑**：`alert-notification-repository.js` `enqueueOpenAlerts` 把所有 OPEN 告警塞 Bark 队列，**除 `PHONE_SILENT_TYPES` = `['BROWSER_PAYMENT_UNKNOWN','BROWSER_PAYMENT_CONFIRMED']`**（D-176 静音）。即"默认全推 + 两个例外"。
- **近3天真实分布**（`operator_alerts`）：SUBMITTED 31 · FAILED 24 · **PROVIDER_BALANCE_CHANGED 6** · PAYMENT_UNKNOWN 6(静音) · PAYMENT_CONFIRMED 4(静音) · COMPLETED 4 · STALLED 2 · STOCK_EMPTY 1 · HUMAN_REQUIRED 1 · WAITING_FOR_CARD 1。≈80 条，静音 10，**推手机 70**。
- **余额变化告警只满足一半（深读更正 2026-09-15）**：`PROVIDER_BALANCE_CHANGED` 触发在 `card-provider-snapshot-service.js:205`（余额与上次快照不同就报），**但快照只有 hnskj**（`provider='hnskj'`）→ **highvcc（Browser 主力卡台）余额变化根本不通知**（highvcc 钱包从没入库，D-230）。Lemon"余额变化要通知"**只满足了次要的 hnskj 那半，主力那半空白** → V2 要补 highvcc 余额入库+变化告警。
- **告警产生点（自查订正 2026-09-15：实为 7 处 INSERT operator_alerts，非 4 处）**：真实写入点——`browser-alert-repository:41`(Browser类,非账本原写的browser-admin:1141)、`card-stock-service:178`(`CARD_STOCK_LOW`)、`card-stock-job:430`、`card-provider-snapshot:205`(余额变化,仅hnskj)、**`workflow-repository:353`、`workflow-repository:462`、`card-transaction-repository:42`**(此前漏)；`order-cancellation:156` 是 resolve 非产生。→ B1 改分类要动**这 7 处(先厘清哪些是产生、哪些 upsert/resolve)** + 推送逻辑,不是原说的 4 处。
- **bark 推送**：`bark-client` `send({title,message,severity})` → `POST {serverUrl}/push`，group=AI充值业务，severity 分级。
- **真噪音**：`ORDER_SUBMITTED`(31,每单提交就响) + `ORDER_FAILED`(24,大多是抓取问题) = 55/80。
- **V2 通知面方案**：把"默认全推+两例外"**倒过来**——信息类默认只进后台/看板，需要人的才推；但**余额变化虽 info 也要推**（Lemon 业务判断，D-228）。

### 2.5 对账看板 · 数据源盘点（2026-09-15 当场核实）

**看板不是从零建（深读 `admin-read-service.js:552 getOverview` 更正）**：后台首页 overview 已是巨型聚合，已算：今日单数/成功/失败/处理中、各细分状态、卡库存(available 用 `eligibleInventoryCardSql`/assigned/depleted/held/needs_funding)、**hnskj 钱包余额**、路线状态、告警计数。V2 看板 = **补齐缺的 + 重组展示**，不是新建。
- ✅ overview 已有：单数/成功率、卡库存、hnskj 钱包
- ❌ 缺：**花了多少钱**（`card_transactions` 只有 hnskj、highvcc 全空，D-230）· **两路线各自耗时**(orders 有底数据但 overview 没算) · **拒付**(实测全在卡 6807；`chargeback.occurred_at` 为 NULL 时间要另取) · **highvcc 钱包**(overview 只取 hnskj)
- **A6 复用**：overview 已用 `eligibleInventoryCardSql` → A6 客户页可用性查询是**复用同一口径，非新建**
- **overview 极臃肿（读透后半 SQL）**：641 行后还聚合资金风险(`funds_risk_pending`)、卡录入待处理(`card_discoveries`)、**补余额风险**(`card_funding_attempts`,D-218弃后是死肉)、hnskj purchaseEnabled 等一大堆——印证"后台按能做什么长"。C2/C3 重组时这些要甄别去留。
- **面间坑（自查订正 2026-09-15：远不止2张）**：getOverview 实读 **20+ 表**(自查B4),待删/废表就有 `reconciliation_cases`/`refund_cases`/**`card_stock_jobs`/`card_funding_attempts`/`card_discoveries`**,还有账本先前没提的 **`card_sync_jobs`**(读5次) → **删任何一张都先改 overview**,否则首页报错。删表牵连比账本原估大得多。
- **自动对账真相（深读 `card-consumption-audit.js` 更正）**：它比对的是**次数**（账本 CONSUMED 数 vs 卡台 purchase success 数），抓账本计数错误（D-207/D-229），**不是资金对账**；且依赖 `card_transactions`，highvcc 空 → 对 Browser 卡**全假阳性**。→ B2 是 B4 的前置；且"资金看板对账"与"次数对账"是两件事，别混。

### 2.6 展示面 · 客户页读透（2026-09-15，`customer.js` 872 行 + `customer-stage.js`）

**客户页是两层**：
- **10 种视图态**（`customer.js:57`，后端态→呈现）：QUEUED/PREPARING/PAYING/ACTIVATING/VERIFYING/CONFIRMING(处理中类,轮询) · REVIEWING/**ACTION_REQUIRED**(warn,带工单) · SUCCESS/FAILED(终态,停轮询)。决定 tone/轮询间隔/是否显示工单。
- **9 阶段进度环**（`customer-stage.js`）：另一层，只画进度百分比；中间几步是 Browser 内部步(CHECKOUT_LOADING/PAYMENT_SUBMITTING)，API 路线不走 → D-219 降内部诊断。

**canRetry 真实逻辑**（`order-status-service.js:173`）：`canRetry = !cdkReturnWouldBeBlocked(order)` ＝ **无扣款证据才能重兑**（扣了钱卡密不能再用）。**复用 A 组付款证据判断(资金栅栏)**，逻辑正确，C1 保留不动。`renderOrder:424` 据此控制 hint 文案 + retry 按钮显隐。

**轮询**：30 分钟超时停；`document.hidden` 降频到 30s。

**C1 降四态映射（真相）**：6 处理态→处理中 · `ACTION_REQUIRED`→需要你重新提供登录信息 · SUCCESS→已开通 · FAILED/REVIEWING→没成功联系客服；9 阶段环→内部诊断。

### 2.7 控制面 · 后台前端读透（2026-09-15，`admin.js` 2366 行，函数地图 + switchView 实读）

**5 视图真实分发**（`switchView`）+ 各装什么：
| 视图 | 加载函数 | 真实内容 |
|---|---|---|
| `overview`(首页) | `loadOverview`→`renderDecisions` | 三个决定开关(接单/路线/卡台) + 就绪 + 今日数字 |
| `orders`(藏 overview 后,非独立 nav) | `loadOrders`+`openOrder` | 订单行 + 详情**6 块**(资金结果/客户会话/卡片/身份运行/履约成本/时间线) + 手动付款确认(`confirmManualCancellation`)/取消 |
| `cdks` | `loadCdkBatches` | 批次生成/下载/作废 |
| `stock`(卡片) | `loadStock`+`loadProviderRoutes`+`loadCardFundingAttempts` | 卡列表/开卡表单 + 卡台路线切换 + **补余额记录** |
| `diagnostics`(诊断) | `loadDiagnostics`+`loadReconciliationCases`+`loadBrowserDispatchJobs`+`loadBrowserRuns`+`loadBillingAddressSettings` | 就绪/心跳 + 对账案例 + dispatch + **browser runs(含手动付款/升级确认/解决未知控制按钮)** + 账单地址设置 |

**读透后的真相（改前必知）**：
- **设置散落 ≥3 视图**：开卡金额/最低余额在 `stock`、账单地址在 `diagnostics`、三开关在 `overview` → C2 收拢 settings 要从 3 处掏。最低余额已按 plan(`minimumRequiredCardBalanceByPlan`)，开卡金额/每卡单数仍全局。
- **diagnostics 是大杂烩**：诊断/对账/dispatch/runs/账单地址 5 类不相干；**"需要我处理"要用的 Browser 运行控制按钮(手动付款确认/升级确认/解决未知)埋在诊断页最深处** → C2 工作台要把它们提到前面。
- **死肉**：`stock` 里 `loadCardFundingAttempts`(补余额记录) UI——D-218 已弃补余额，C3 删。
- **面间坑**：`overview` 读 `reconciliation_cases`/`refund_cases`，删表(C3)前先改前后端。
- **弹窗确认**：`askForm`(629) 统一弹窗；确认词由页面填(§2.3 更正)。

### 2.8 供卡链路深度核查（2026-09-15，选项1战略前置，全生产只读核实）

> 起因：核战略矛盾「Browser 主力但卡只能手动开」，用 §2.1 那种颗粒度把供卡真实链路查到底。**中途两次凭配置推断被数据打脸**（"Plus 走 API"被 [O] 纠正、"矛盾不成立/Browser 用 hnskj 卡"被 [T]/[U] 纠正）→ 教训：任何中间推断都不能停，只认真实数据。

**Provider 真身份（[N2] `provider_accounts`）：**
| acct | provider_code | 供Browser | 自动开卡 | 熔断 | 说明 |
|---|---|---|---|---|---|
| 101 | **hnskj** | ✅ | **✅** | CLOSED/0败 | 真卡台,能自动开;当前主要供 API 路线 |
| 102 | zzshu | — | — | CLOSED | 充值供应商(非卡台) |
| 103 | **manual_excel** | ✅ | ❌ | CLOSED | 手动导入卡台;**highvcc 是它的数据来源**(token扒取→Excel→manual-card-import 导入),非卡台本身 |

- **命名纠正**：账本原把 103 叫「highvcc 卡台」是混淆——数据库层 103=manual_excel(手动导入卡台)，highvcc 只是喂它的来源之一。这解释了 snapshot-sync 为何走 manual-card-import（目标卡台就是 manual_excel）。

**Browser 主力实际用 103（[T]/[U] 实锤，冻结 vs 实际分配交叉验证）：**
- 冻结卡台 [T]：BROWSER→103 **53**单 · BROWSER→101 4单（2026-09-17 实查订正：全 `CANCELLED_PRE_SUBMISSION`、0 次付款） · API→101 7单
- 实际分配 [U]：BROWSER→103 **38**单 · BROWSER→未分卡 18单 · API→101 7单
- **坑**：路线表 `fulfillment_routes.card_provider_account_id` 全配 101，但实际选卡用 103 → **该字段不是实际选卡依据，别信它。**

**规模真相（[I]/[J]/[O]）**：近14天≈65单、均 **4.6单/天**、峰值24(09-13)，几乎全 Plus（pro_5x 零单）；离 200单/天差 8-40 倍。Browser 56单(主力) vs API 7单。**成功率差：Browser 29失败/14成功；Browser 18/56 单未分到卡**（缺口原因待查，不编）。

**hnskj 自动开卡 [P]**：`PURCHASE_CARD` 全表仅1条 COMPLETED、停在 2026-08-19，近月没跑，能否真成未近期验证。

**A4 战略矛盾——已解（2026-09-15 Lemon 拍板 D-235）**：原以为"Browser 主力用 103(不能自动开卡)→200单撑不住"，实为假设错误——**highvcc 手动产能 100 张/天(可申请扩) > 200单需的 67 张/天**，手动够用、非死结。出路＝**主力 highvcc 手动开卡，hnskj 自动开卡(Browser 能用但卡台常故障)只作补充、不作依赖**；答案"为什么 Browser 用 103 不用 hnskj"＝hnskj 卡台经常服务器故障不可靠(Lemon)。详见 A4。

## 3. 六个面的确认方案（v2，基于 §2 真实测绘重出，旧稿作废）

> 每条：怎么改 · 真实依据(§2.x) · 牵动哪些面 · DoD。**先做横贯前置 G，它们是多个面共用的地基。**

### 3.0 横贯前置 G（多个面的共用地基，最先做）

**G1 · highvcc 数据入库** — 已并入面四②。

**G2 · 账本计数可靠** — 已并入面四②（人工收口写账本 + 历史补记）与面二④。

### 3.A A 组（执行面 + 资源面）· 2026-09-17 起按「逐面重问」重写（D-243/D-246）；2026-09-15 过审版作废、只作历史

**面一 · 卡台与路线（Lemon 2026-09-17 拍板，D-246；讨论稿与全部证据：`docs/tasks/2026-09-17-ledger-face-1-card-and-route.md`）**
- **决定**：①「用哪个卡台」两条路都要有——一张「产品 × 执行器 → 卡台」选择表，各选各的，切路线不动卡台；②**卡台可插拔是原则**：分派只看 `provider_accounts` 能力位、不看名字（实查 v1/src+browser-mvp/src 名字分支 52 处/20 文件；C1 只清「选卡台→分卡→付款前查卡→交易核对」这条路上的，其余归面二/对账面）；新卡台 = 一行账户 + 一个适配器；③**C2 已做（D-253，2026-09-17 13:54 UTC）：ZZSHU 拒 highvcc 卡 40020「该卡头暂不支持提交」，未扣款 → ZZSHU 按 BIN 白名单放行、不是「不认卡台」；选择表 API 行初值 = 101；**Lemon 定：接受 API 只走 hnskj、客服不加 BIN → 解耦结论：Browser 任意卡台、API 固定 hnskj；选择表 API 行固定 101、后台不给选择控件**；④hnskj 卡分卡按需同步（归面二），C1 的「目标卡池可分配>0」校验用按需同步后口径（缝）。
- **C1 做什么**：选择表替代 `browser_card_source_selections` + `fulfillment_routes.card_provider_account_id`；intake 从表取冻结卡台、删 `provider_code='hnskj'` 硬写；切路线/切卡台端点前跑四项校验（目标路线唯一、目标卡池可分配>0、卡台 `circuit_state=CLOSED`/`read_enabled`、版本对）不过即拒并说原因；`resolveCurrentCardProviderAccount` 改为遍历所有 `purpose='CARD'` 账户按能力位分派（有 API 同步位的才发请求）。**不改付款行为、不改资格规则、hnskj 限流不受影响。** 是减法：删三处硬写、合两表一。
- **现状证据（2026-09-17 实查）**：API 冻结卡台三处硬写（路线表字段/intake SQL/provider-route-service）；`setDefaultRechargeMethod` 切 API 不查任何东西、切 Browser 只查 dispatch 开关+profile+心跳；D-219「切路线连带动卡台」实为 Lemon 手动点两次，代码不耦合；历史 Browser×101 **4 单全 `CANCELLED_PRE_SUBMISSION`、0 次付款**，API×103 组合不存在（intake 不允许）→ 两台「能否走另一条路」系统内均未验，只有 Lemon 手动经验（hnskj 卡走 Browser 手动能成）。
- **牵动**：执行面（intake 前段）· 资源面（供给分派）· 控制面（两个切换按钮 + 校验提示）· 面二（按需同步口径）。
- **DoD**：C2 一单真实响应留证；C1 单测（三产品×两执行器选表、四项校验各拒一次、能力位分派 hnskj 发请求/103 不发）；`grep` 证明 intake/provider-route-service/workflow-handlers/pool worker 选卡路径无 `'hnskj'`/`'manual_excel'` 字面量；切路线测试证明不动卡台；`state-check` 一致；生产只读复验。
- **作废**：旧 A1「切换解耦卡台」段、旧 A2「切换前校验」归属（任务书笔误，账本 A2 本是 verify）、旧 A4 全段（D-235 已被 D-242 推翻）、§2.8「BROWSER→101 3 单」。

**面二 · 供卡（Lemon 2026-09-17 拍板，D-247；讨论稿与全部证据：`docs/tasks/2026-09-17-ledger-face-2-card-supply.md`）**
- **决定**：①供卡改「库存水位驱动 + 订单兜底」：每卡台 × 每产品设目标可分配张数（Plus 每台 2；Pro 每台 0、来单即开），调度器每分钟看一次，低于水位且日限内且钱包预检过 → 按该台适配器开一张；`WAITING_FOR_CARD` 仍作兜底触发。②开卡金额按产品：Plus $50 / 5X $100 / 20X $150（吸收旧 A3；每卡单数 Plus 3 / Pro 1 不变）。③三条开卡线合一：hnskj `card-stock-job-runner` 挂定时（人工 job 与自动 job 同一执行者，不再 ssh）；highvcc `openCard` 接调度，`HIGHVCC_OPEN_NO_PAN` 自动转 `recordExistingCard`；worker `PURCHASE_CARD` 死线删；**生产 930 条旧架构 `card_stock_jobs REVIEW_REQUIRED` 先归档（不删行）**，否则调度器永远 `FUNDS_REVIEW_REQUIRED`。④两台都有库存眼睛：`CARD_STOCK_LOW` 按台 × 产品、阈值 = 水位；highvcc `wallet()` 入库（G1 一半）；余额变化通知（D-228）。⑤钱包：硬底线 hnskj $30 / highvcc $20（开卡前预检 `余额 − 金额 − 手续费 ≥ 底线`，否则不开并推手机）；$50 告警线只告警；**基线「HNSKJ 开卡要求余额 250 以上」作废**。⑥每台每日开卡上限起步 20（资金保险丝，可调）；卡台在线 100 张上限另计。⑦**故障转台双向、按「该台此刻能不能开」判**（D-252）：hnskj 故障（连续失败 N 次 / 维护 / 5xx）→ 转 highvcc；highvcc 时段外无 token → **Browser 缺卡自动用 hnskj 开一张顶上**，不叫人；转向后开出的卡冻结到订单（只改 WAITING_FOR_CARD 且无分配的单，复用 `safeWaitingPredicate`）。API 路线冻结 hnskj、不转（待 C2 通过后再议）。⑧highvcc token（约 2 小时失效、登录有滑块、不自动续，D-249/D-251）：**「每日时段」模式**——Lemon 每天固定时段贴一次 token，系统把所有要碰 highvcc 的活（全量卡快照、交易流水、钱包余额、按水位开够当天的卡）集中在该时段做完；其余时间不碰 highvcc（付款用库内凭证不需 token；卡余额靠账本推算，次日对账）。时段外只有突发缺卡要现开才叫一次。⑨分卡时候选卡同步过期就**当场同步这一张再判**（面一缝闭合），定时全量同步降频；限流预算：按需 ≤2/单 + 开卡 ≤3/张，200 单/天仍远低于 60/分钟。⑩销卡待办做实（吸收旧 A5，D-232 范围内不自动删）：不再服务新单（用满 N / DEPLETED / RETIRED）+ 最短存活期已到 → 待销清单 → 「需要我处理」→ 两台手动删 → 事后同步确认。**这是资金效率的主杠杆**（钱占用 = 卡上金额 × 存活时间）。⑪删补余额整条线（`card_balance_recharge_enabled` / `card_funding_attempts` / funding timer）；两批旧卡标终态（hnskj 12 张卡台已作废；highvcc 5 张 Lemon 已注销、余额已回钱包）。
- **现状证据（2026-09-17 实查）**：24 次等卡平均 9～12 小时、13 单最终关闭；自动开卡两开关 false；930 条残留卡死调度器；worker PURCHASE_CARD 无创建点；后台人工开卡 job 无执行者；`CARD_STOCK_LOW` 只算 hnskj、阈值 1；highvcc 钱包/交易 0 入库；`default_open_card_amount=16`；hnskj 12 张 $0 卡、highvcc 9 张 AVAILABLE 仅 1 张真可分。
- **牵动**：资源面全部 · 执行面（分卡按需同步）· 控制面（设置页：水位/金额/日限/底线按台按产品）· 通知面（缺卡/开卡失败/token 过期/余额变化必推）· 对账面（钱包与卡余额入库）· 展示面（Pro 来单即开时客户页「准备中，预计 X 分钟」）。
- **DoD**：调度器单测（水位/日限/钱包预检/故障转台各一条，真实响应夹具）；两台各真开一张（Lemon 当次确认）；930 条归档后 `scheduleAutomaticJob` 不再返回 `FUNDS_REVIEW_REQUIRED`；按需同步单测 + 生产只读复验「分卡不再因 15 分钟窗口等卡」；待销清单单测（用满/DEPLETED/存活期）；`grep` 证明 funding 线与 PURCHASE_CARD 死线已删；`state-check` 一致。
- **作废/吸收**：旧 A3（并入②）、旧 A4 残余（并入①③⑦）、旧 A5（并入⑩）、缝 3/8/9/10/12/14 与逆向缝 A/C/D（§4）均在本面闭合；G2「账本计数」留给对账面。

**面三 · 执行与交付（Lemon 2026-09-17 拍板，D-248；讨论稿与全部证据：`docs/tasks/2026-09-17-ledger-face-3-execution-delivery.md`）**
- **决定**：①**不做「执行面接口三段统一」**（旧 A1 作废：两路线在 v1 层已共用状态机/资金栅栏/账本，差异是天然的执行细节，统一是加壳不是减法）；改做**三张契约表 + 测试**。②**表一 交付判据**：目标套餐确认 + 取消续费确认 → `RECHARGE_SUCCESS`，两路线各自实现、测试断言同条件；**API 单取消续费超时不再阻塞交付**（去 `CANCELLATION_REVIEW_REQUIRED` 卡单）：照常成功 + 推手机 + 卡进待销清单（Lemon：ZZSHU 几乎不会取消失败，失败就算了、销卡兜底）。③**表二 付款不明**：两路证据（账号状态 + 卡台侧扣款记录）自动定、窗口放长；**highvcc 卡的流水拉取算 D-251 时段外的「突发例外」：token 有效就拉一次，失效才叫人、带「账号未开通 + 卡台流水待核」**（D-252）；**定不了才叫，叫时必须带两路证据结果**（Lemon：别谎报；历史 14 次里 8 次人工多为实际未扣款）；API 补 `markAttemptUnknown` 后排 `POLL_RECHARGE` + 后台 RESOLVE 入口（现只能手写 SQL）；**G1 highvcc 交易入库是前置**；崩溃 `RECONCILE_ONLY` 不再是终点，重启后进补核。④**表三 人工兜底点唯一清单**（面五「需要我处理」队列与面四推送判据的输入）：Session 无效/已是 Plus → 系统打回客户换、不叫；付款前失败现场保留 → 叫（等接手）；人机验证 → 叫；付款不明定不了 → 叫（带证据）；取消续费未确认 → 提醒 + 待销，不卡单；崩溃 → 补核；缺卡 → 自动开、开不出才叫；token/卡台故障 → 叫；**ZZSHU 零原因失败 → 不退码、停单等 Lemon 看**（进队列，带原始响应）。⑤**Pro 两条路都走**（D-245 同型）：Browser 结账按订单 plan 选 tier（navigator 已有控件）、`confirmPlus→confirmPlan(target)`、取消续费同，**前置一次非付款 PoC 冻结到 contracts**；API `pro_5x→pro5`、`pro_20x→pro20` 映射（ZZSHU 文档实证，`docs/contracts/2026-09-17_zzshu-third-party-api-plans-excerpt.md`）+ 真单验证；删 `UPGRADE_DIALOG_STOP` / `MANUAL_20X_HANDOFF` / `COMPLETE_20X` 整条线与 `fromPlan`。⑥付款前抓取类失败（Browser 失败的一半）**先攒 D-239 样本再改**，本面不加机制。⑦时延不作本轮目标。⑧客户页第一步提示「Session 至少剩 30 分钟」→ 面五。
- **现状证据（2026-09-17 实查）**：20 单成功里自动跑完 10 单（Browser 6 均 279s、API 4 均 1670s）、人工收口 10 单；API 付款不明无自动恢复（`markAttemptUnknown` 不排任务）、历史 0 次；Browser 不明 14 次（6 自动 / 8 人工）；Browser 45 单失败里付款前抓取类 23、真拒付 3；人工点散在订单态/run 态/后台 9 动作三处；ZZSHU 文档支持 `pro5/pro20`；仓库 `对接api.md` 是 GPT-KCCatk 文档非 ZZSHU。
- **牵动**：通知面（叫人清单 = 推送判据）· 控制面（队列 = 表三；删 COMPLETE_20X 等动作）· 对账面（G1 前置、卡台扣款证据）· 展示面（Session 前置提示；Pro 成功文案）· 面二（待销清单接取消失败）。
- **DoD**：三张表落 `docs/contracts/`；交付判据两路线单测（真实响应夹具）；API 不明 → POLL → 收口/RESOLVE 单测；补核两路证据单测（一致收口 / 不一致叫人带证据）；Pro 非付款 PoC 留证 + API 一单真实响应；`grep` 证明 UPGRADE/MANUAL_20X/COMPLETE_20X 已删；生产只读复验。
- **作废/吸收**：旧 A1 全段、旧 A2 全段、V2 §3.1 接口伪代码与 `fromPlan`、基线「5X/20X 自动化前置」第 1/2/3/4 条。

**面四 · 通知与对账（Lemon 2026-09-17 拍板，D-249；讨论稿与全部证据：`docs/tasks/2026-09-17-ledger-face-4-notification-reconciliation.md`）**
- **决定**：①推手机改**白名单**（替代 `PHONE_SILENT_TYPES` 排除法），四类：叫人（面三表三 A 项：付款前现场保留、人机验证、付款不明定不了带证据、缺卡开不出、token 要用而没有、卡台故障、ZZSHU 零原因失败）/ 供给（缺卡预警、开卡失败、钱包低于告警线 $50）/ 资金（**拒付必推**；余额变化每笔推、可设「每日汇总」、开卡费不推）/ 客户动态（客户提交了充值）。其余（PAYMENT_CONFIRMED / COMPLETED / 同步类 / 补款类 / REFUND_CANDIDATE）只进后台。**先坐实 D-176 静音为何未生效**（生产实证：静音表在、进程跑当前 release、PAYMENT_UNKNOWN/CONFIRMED 仍各插通知行并 SENT，唯一入队来源是 `enqueueOpenAlerts` 的 `NOT IN (?, ?)`；原因未知），再改同一段代码。新增产生点：token 失效（按「要用而没有」触发，一段失效期只推一次）、卡台故障（面二⑦）。②**数据源三件**（吸收 G1/G2）：highvcc `allTransactions()` 归卡写 `card_transactions`、`wallet()` 写 `provider_balance_snapshots`，并入面二⑧「每日时段」那趟（token 失效则该时段任务失败、叫一次；highvcc 数字最多滞后一天，Lemon 接受，D-251）；hnskj 开卡费 $0.5 记 `card_transactions.fee`（现全 0）；**人工收口（CONFIRM_MANUAL_PAYMENT / RESOLVE_UNKNOWN=CHARGED）必须写消费账本 CONSUMED**，历史 8 单补记（正式脚本、dry-run、审计；可能使个别卡立即「用满」进待销，Lemon 已同意）。③**对账每日一次、两种分开**：次数（`card-consumption-audit` 挂 timer）+ 金额（开卡金额 − 卡台交易合计 vs 卡余额）；差异**前期只进看板 + 每日一条汇总推**，连续两次日对账仍在才进「需要我处理」（Lemon 担心误判/延迟误推）；删 Browser 付款后固定 marker 假核对，改读真交易。④**看板只答六问**（C 组做页面时用；不新建表）：今日单数/成功率/**自动完成率**（不靠人收口的比例）· 花了多少钱（开卡+付款+拒付，按台）· 卡用了几张剩几张（按台按产品）· 两路线各自耗时（建单→成功中位数）· 异常支出（拒付哪张卡多少）· 今天叫了几次为什么 · 卡台状态（余额/token/故障）；`getOverview` 21 张表的技术指标降「高级」。⑤token：登录含随机滑块 → **不做自动登录**（D-249 补记）；采用面二⑧「每日时段」模式（D-251），不按过期叫，时段外突发缺卡才叫一次。
- **现状证据（2026-09-17 实查）**：近 7 天推 99 条（SUBMITTED 33 / FAILED 26 / BALANCE 13 / PAYMENT_UNKNOWN 10 / …），真需要人 <10；**D-219 发现 3「CARD_STOCK_EMPTY pushed=0」是误读**（`sent_at` 有值，告警关闭后通知行被标 CANCELLED）；highvcc token 06:06 贴 08:38 失效、快照 service failed、无告警；`card_consumption_ledger` CONSUMED 12 vs 成功 20（Browser 7 单 RELEASED、API 1 单无行；卡 1657 实用 3 单账本记 1）；highvcc 交易/钱包 0 入库；`card_recharge.fee` 全 0；拒付 4 笔入库（6807×3、1652×1）；`card-consumption-audit` 无调用；Browser 备用卡核对恒匹配；`getOverview` 读 21 表。
- **牵动**：面二（钱包水位、待销靠账本准）· 面三（两路证据靠 highvcc 交易）· 面五（队列 = 表三 + 对账持续差异；看板页；「今天叫了几次」要记推送）· 资金安全（账本补记影响分卡上限）。
- **DoD**：白名单单测（四类各一推、其余不推）+ 静音根因坐实记录；highvcc 交易/钱包入库后 backup-a 两表非 0 且抽一张卡与卡台后台一致；开卡费 fee 非 0；补记脚本 dry-run 输出 + 审计行 + 补记后 `card-consumption-audit` 差异归零；日对账 timer 有心跳、人为造差异能进看板/汇总推；看板六项每项能点进明细、只读复验与底表一致；`grep` 证明 marker 假核对已删。
- **作废/吸收**：旧 B1/B3/B4 全段、G1/G2 全段、D-219 发现 3 的结论句、§2.4「默认全推+两例外」描述。

**面五 · 展示与控制（Lemon 2026-09-17 拍板，D-250；讨论稿与全部证据：`docs/tasks/2026-09-17-ledger-face-5-display-control.md`）**
- **决定**：①**客户页小改不重做**：10 态压四态——处理中（6 个处理态 + REVIEWING「正在人工确认」，含面三 ZZSHU 零原因停单）/ 需要你重新提供登录信息（ACTION_REQUIRED）/ 已开通 / 没成功（按 `canRetry` 说可否重兑）；九阶段进度环保留、只在处理中显示；**不加 Session 有效期提示**（Lemon：反而迷惑）；不放联系方式（CDK 即工单号）；Pro 短名已支持不动；不展示取消续费。②**后台 4 页 + 高级入口**（工作台 / CDK / 卡片 / 设置；Lemon 放开页数，此为接手模型按「三个决定 + 三件日常事 + 改数字」重算的结果，落实出视觉方案时可增减改）：**工作台** = 三开关 + 两个卡台选择（按路线，带面一四项校验）+ CDK 生成 + **「需要我处理」队列**（面三表三 A 项 + 面四持续差异 + 面二待销 + token/卡台故障，每条带对应按钮，按钮从诊断页搬来）+ 今日订单七列 + 面四看板六问；**设置** = 按台 × 按产品一张表（水位 / 开卡金额 / 每卡单数 / 最低余额 / 每日开卡上限 / 钱包底线与告警线）+ 全局（推送汇总、Session 门槛、账单地址）；CDK / 卡片（两台并列：水位/可分配/余额/token/故障/开卡/待销/导入）沿用旧页到最后再收；诊断降高级入口。**工作台 + 设置先做**（面二/三落实要用）。③**UI/交互**：落实（D-243 步④ C 组）时先出 2～3 版视觉/交互方案给 Lemon 挑（可参考客户页「候光」），验收含 Lemon 实际用一天的反馈；**Lemon 原话：工作台要用起来舒服、交互好，V1 太差。** ④**删（先改引用再删）**：0 行 5 表（order_tags/notes/compensations/checkout_artifacts/browser_artifact_secrets）+ browser_interventions；refund_cases / reconciliation_cases 删页面端点、表归档（`getOverview` 与资格 SQL 读它们，先改）；exports 端点；card-funding 整条线（面二⑪）；COMPLETE_20X / UPGRADED_20X（面三⑤）。**保留**：payment_permits / cdk_delivery_events（在用）；**card-intake 链路保留**（它是 hnskj 新卡自动录入：catalog-sync 每 5 分钟 `discover()`，后台页只降高级，不删）。⑤Session 门槛：**统一 30 分钟不动**（D-252；13 次打回 8 次不回来的问题留运营期观察，不加提示）。
- **现状证据（2026-09-17 实查）**：客户页 873 行四屏 + 查询、10 态 + 九阶段、`withProduct()` 已支持 Pro 短名、30 分钟后停自动刷新；后台 5 nav、首页「需要处理」= REVIEW_REQUIRED 前 8 + 告警前 10、设置散 3 页、开卡金额/水位/日限/底线无入口、Browser 三个处置按钮在诊断页；删表当场行数表见讨论稿 §1.3；`getOverview` 读 21 表。
- **牵动**：全部前四面在页面上的落点；面三 b 客户态；面二 Pro 来单即开的等待说明。
- **DoD**：客户页四态渲染测试（含 REVIEWING 文案、API 路线无九阶段也对）；工作台加载 + 队列每条按钮可点 + 四项校验拒切能显示原因；设置页每项带审计；删表 `grep` 全项目无残留 + 各页不报错 + `state-check` 一致；UI 方案 ≥2 版 + Lemon 试用反馈记录。
- **作废/吸收**：旧 C1/C2/C3 全段、A6、缝 5、D-226 页数作为「已定」的说法（改为「当前判断」）。

**整体连贯审补丁（D-243 步③，2026-09-17，D-252；全文 `docs/tasks/2026-09-17-coherence-review.md`）**：打架 3 → 面四②账本补记先于面二⑨；打架 4 → 待销口径加「取消续费未确认」；打架 5 → 客户页「正在人工确认」态提示改「完成后用卡密到订单查询查看」；缝 b → 305/306 重开条件 = Browser PoC 冻结合同 且 API 一单 pro5/pro20 验证，各自达标各自开；缝 c → 「需要我处理」队列不新建表、工作台端点聚合各来源；缝 d → 「今天叫了几次」从 `alert_notifications.sent_at` 统计；缝 e → 开卡成功后立即触发一次 card-intake discover；缝 f → 面一 C1 ④ = 面二①调度器同一实现；缝 g → 「API 取消超时提醒」入白名单资金类；缝 i → hnskj 限流预算净下降；缝 j → 人工收口两按钮搬进队列且写账本。红线六条拼后仍成立；日志脱敏未审，落实每步 grep。

**第⑤块收窄补丁（D-275，2026-09-18，Lemon 认）**：面四③金额对账降级——无可验证期初的卡「无法核对」，不自动判异常/一致，次数对账保留；未知扣款与已确认手动消费分开、后者不猜不藏；「连续两次」只认正式批次；拿掉 `DAILY_DIGEST` 与看板「今天叫了几次」；手动用卡 = 标 RETIRED + 原因（不新建表）；**面五④删表清单缩小：`reconciliation_cases` / `browser_interventions` 在用不删**。设计责任：面四③公式默认 `funded_amount` 可靠，未核（D-274 推翻）。落实为 ⑤b 小块，过了再开⑥。

**第⑦块前置事项（D-275 时从 Codex 评估 §5 归入，写 ⑦ 任务书时必须带上）**：a. 付款不明补核的金额区间 `plausiblePlusAmount` 只认 Plus 价位（USD 14~22 / PHP 900~1200），改为按订单套餐取区间，否则 Pro 每单叫人；b. **按产品每卡上限要接线到同一规则**：现分卡 `workflow-repository.js` 与 `card-inventory-eligibility.js` 仍读全局 `card_max_successful_payments=3`，⑥ 设置页写按台×产品的值、⑦ 分卡/资格 SQL/待销必须同口径读它，Pro 开放前逐处验；c. 旧两阶段实现清单（⑦删）：`production-live-pool-worker.js:224` 非 Plus 选 `UPGRADE_DIALOG_STOP`、`browser-card-transaction-reader.js:52-59,163` Plus 金额范围、`browser-admin-service.js:1011-1025` Pro 人工确认含 Plus 阶段、`admin.js:699` 文案；d. browser reader 对 hnskj `tradeTime` UTC+8 串直接 `Date.parse` 偏 8 小时（第④步发现 2，白名单内可修）；e. Pro 非付款 PoC 仍是前置，⑥ 不固化旧升级流程。**f. 第⑦块拆两段（Lemon 2026-09-18 认）：⑦a 只用既有 rehearsal 模式对 20X 跑一次非付款演练（付款开关关、`PRE_SUBMIT_STOPPED`），把结账选套餐/报价/税/币种冻结到 `docs/contracts/`，只读、不改执行器，做完 Codex 审；⑦b 再改执行器、删旧两阶段分支、白名单卡死、全量测试 + 演练、Lemon 在场看一单真实 Pro。生产付款链路不加任何新的停止点。Fable 额度够则 Fable 做 ⑦，否则按此两段走。**

**做的顺序（D-243 步④输入，按依赖非按面）**：① C2 真单（highvcc 卡走 ZZSHU）+ API `pro5/pro20` 映射验证（可同一单）→ ② 面四②数据源三件 → ③ 面一 C1 + 面二①③（选择表/切换校验/水位调度/三线合一/930 归档/每日时段）→ ④ 面二⑨⑩ + 面三②③④（按需同步/待销/契约表落地）→ ⑤ 面四①③（白名单先坐实静音根因/日对账）→ ⑥ 面五②工作台 + 设置页（UI 多方案）→ ⑦ 面三⑤ Pro（PoC → 改造 → 重开 305/306）+ 面五①客户页四态 → ⑧ 面五④删表。每步做完放回主线验。

**browser-mvp 改动纪律（Lemon 2026-09-17 顾虑：Browser 自动化代码脆弱，D-254）**：
- 哪些块碰 browser-mvp：②⑤⑥⑧ **不碰**；③ 只改 pool worker「按卡台名字判交易读取器」处与 `shared-encrypted-materials` 卡台校验（按能力位）；④ 只改付款**之后**的补核链路（`recovery.js` 崩溃进补核、`browser-card-transaction-reader.js` 删假 marker、`live-post-payment-recovery.js` 两路证据）；⑦ Pro 同型改造是唯一碰付款链路的块（组合层 plan、navigator 选 tier、`confirmPlan`、删 UPGRADE/MANUAL_20X 线），**前置非付款 PoC**，排最后。
- 每份碰 browser-mvp 的任务书必须列**允许改的文件白名单**；白名单外改动 = 越界，停下来问 Lemon。
- **付款前三件任何块不许改**（`billing-address-fill.js`、`live-chatgpt-payment-adapter.js`、`payment-executor.js` 的 submit 段），除非 Lemon 单独批；D-238/D-239 的教训：先攒样本坐实真因再改。
- browser-mvp 一动：全量测试（现 292 项）+ 一次 rehearsal 演练（不花钱）都绿才算改完；常驻 worker 重启前问 Lemon。
- 每块收尾放回主线验时，含一次 Browser 演练确认未被波及。
- **发现要报、修要立项（Lemon 2026-09-17）**：落实中发现任务书范围外的问题（代码、生产数据、文档过时），**必须**写进本块收尾的「发现」段并登记到账本/DECISIONS，**不许顺手改**；修不修、排在哪一块由 Lemon 定。样板：接手核对当天发现 930 条残留、静音未生效、账本少记 8 单，全部报、一条没改。

**A1 · 执行面接口三段统一** — **不做**（面三①，D-248）。

**A2 · verify 作交付判据** — 已并入面三②③（交付判据 + 付款不明两路证据）。

**A3 · 按产品化** — 已并入面二②（开卡金额 50/100/150、每卡单数按产品），不再单独立项。

**A4 · 供卡策略（两台并重，D-242）** — 供给侧「自动动作只认 hnskj」已归面一 C1 ④；其余（两台开卡自动化、缺卡预警、缓冲池、钱包水位、NO_PAN 自动补记、日限、按需同步）归**面二「供卡」**逐面重问，本条不再单独立项。历史：2026-09-15 D-235「主力 highvcc 手动够用/hnskj 排后」已被 D-242 推翻，原稿删（见 git 历史）。

**A5 · 销卡待办** — 已并入面二⑩（用满/DEPLETED/RETIRED + 最短存活期 → 待销清单 → 手动删；资金效率主杠杆）。

**A6 · 卡可用性只读查询** — 砍（§5 减法审查；面五不做前置提示）。

### 3.B B 组（通知面 + 对账看板）· **正式待审版**（2026-09-15，建在读透 §2.4/2.5 上）

> 前置：**G1（highvcc 数据入库，已在 A 组前置）**——B1 highvcc 余额告警、B3 看板花费、B4 对账都依赖它。

**B1 · 通知面** — 已并入面四①（白名单四类）。

**B3 · 运营看板** — 已并入面四④（六问）。

**B4 · 两种对账** — 已并入面四③。

### 3.C C 组（展示面 + 控制面）

**C1 · 客户页** — 已并入面五①。

**C2 · 后台** — 已并入面五②③。

**C3 · 删表** — 已并入面五④。

## 4. 整体大摸排（阶段 2，2026-09-15 开走）

**走法（Lemon 用第一性+对抗审查纠正后定）**：不是一条线,是**两条主线 + 两个横切面**。原"从客户输入起步"的单线被推翻——起点是 Lemon 生成 CDK(CDK 是已付款权益,生命从生成开始),终点是钱回钱包(闭环非直线),且 Lemon 是主线的一部分(人机交界最易出缝)。

**主线一 · CDK 的一生**：你生成CDK(产品/批次)→交付渠道→客户输入(A6前置)→兑换下单→分卡(A3/G2)→缺卡开卡(A4/G1)→付款(A1)→核实交付(A2)→客户看四态(C1)→用满进待销(A5)→到点手动删→钱回钱包。
**主线二 · 钱的一生**：钱包→开卡扣款(+$0.5)→卡上钱→付款扣($15.72+支付费)→剩余→销卡退回→钱包;异常支出拒付($0.1/$0.4)→B4顺便推。查:水位/回笼/账准。
**横切面一 · Lemon 被打扰的时刻**：通知(B1)/需要我处理队列/手动删卡/切路线/发CDK。查该叫的叫没、不该叫的还吵没。
**横切面二 · 并发与规模**：同时20单抢卡/缓冲池抽干/开卡日限/lane排队。查200单/天哪先塌。

**原则**：主动发现 > 对照清单;每个交界用真实代码/生产核实,不凭方案文档。

### 4.1 走查发现（逐段填,缝记这里）

**第一段「你生成 CDK → 客户输入」(2026-09-15 走查)**
- ✅ 产品身份流通：CDK 生成绑 `cdks.plan_type` → 验证返回 planType → 建单读 plan_type，三处同源。
- **缝1（确认 A3 落点+历史不一致）**：`order-intake-repository.js:45` 建单时**最低余额已按产品**(`minimum_required_card_balance:pro_5x/:pro_20x`)、**开卡金额还读全局** `default_open_card_amount`。A3 不是新加,是补当年漏做的那半、与最低余额对齐。
- **缝2（面间一致性）**：A6 前置提示的卡门槛 必须与 建单门槛 **同源**,否则"提示有卡、下单没卡"割裂客户。A6 DoD 的"同源"从方案一句话升级为必验交界。

**第二段「兑换下单 → 分卡 / 缺卡开卡」(2026-09-15 走查)**
- ✅ 分卡并发保护有：`workflow-repository` 用 `FOR UPDATE SKIP LOCKED`,同时来单不抢同一张卡。
- **缝3（并发缓冲）**：SKIP LOCKED 保证不抢同张、保证不了卡够分。A4 的 1~2 张缓冲在高峰会被抢光→排队等开卡(几十秒)。缓冲池大小要按高峰并发算,不能真只 1~2 张。
- **缝4（修正：我制造了假对立，Lemon 纠正）**：原写"每卡多单不提高并发→该重新想每卡几单"是**错的**——每卡几单(一张卡一生服务几单,串行)与并发(此刻多少张不同卡在跑)**正交、不冲突,都要**。①每卡多单**必须做**：开卡费 $0.5 + 占 $50 资金,一卡多付几次才摊薄回本(一卡一单只用 $15/浪费 $34 周转)。②并发靠多卡。**保留的有用观察**：A4 缓冲池按"高峰同时需要多少张**空闲卡**"估(≠总单量÷每卡单数),别把缓冲算小。

**第三段「付款 → 核实交付 → 客户看四态」(2026-09-15 走查)**
- ✅ 状态映射完整：`order-status-service` 内部态→客户 10 视图态齐全(SUBMIT_UNKNOWN→VERIFYING、WAITING_FOR_SESSION→ACTION_REQUIRED)。
- **缝5（四态无家的态）**：`REVIEWING`(人工审核中,来自 RECONCILIATION_REQUIRED/CANCELLATION_REVIEW_REQUIRED)压成 C1 四态时无归属——塞"处理中"会与时间承诺打架、塞"没成功"会吓客户。C1 降四态必须定它归哪。
- **缝6（抬高 A5 工作量）**：交付成功后现只有 `RELEASED`(释放活动分配,让卡服务下一单),**不是销卡**。A5"用满→进待销→到点删"整条链现在零存在,是**从零新建**,非"加提醒"。
- **缝7（时机）**：销卡时机(用满N单)≠释放分配时机(每单后)。A5 必须接"用满"点,错接"释放"会导致卡服务完第1单就被销、后单没卡。

**第四段「钱的一生（主线二）」(2026-09-15 走查)**
- **缝8**：开卡现在**不预检钱包余额**(直接开、失败才知道)。A4 自动开卡必须加"开卡前预检钱包",否则钱包空时疯狂开卡失败(09-05 那种);钱包数据依赖 G1。
- **缝9**：`card_balance_return`(销卡回笼)只有 hnskj 入库,**highvcc(主力)回笼记录没入库**→钱的闭环断在主力侧,A4 回笼算不准、B3 回收额空。G1 要补的含这笔回笼。
- **缝10（最易算错）**：钱分散五处(钱包/卡上未花/花掉/卡上剩余待回笼/销卡回笼中锁定)。**钱包余额≠真实可用资金**,④⑤是"在途锁定"的钱。A4 水位必须盯"整个资金池健康"(钱包+卡上未花+待回笼),非钱包单数,否则高峰失真。→ 写进 A4 硬要求。

**第五段「你被打扰的时刻（横切面一）」(2026-09-15 走查)**
- **缝11**：**你被打扰几次,现在没人统计**。V2 目标是少打扰,打扰量得能衡量→看板加"今日打扰 N 次",否则"更省心"只是感觉。
- **缝12**：highvcc 无删卡 API→**你手动删完卡,系统不知道**。待销清单"已删"状态、回笼确认,只能靠 G1 事后同步推断(有延迟),不能假设"提醒了=你删了"。
- **缝13**：连败熔断(同一CDK连败N次)**现在不存在**,新建;且熔断停止后**客户页显示什么要和 C1 对齐**,否则客户干等页面转圈。

**第六段「并发与规模（横切面二）」(2026-09-15 走查)**
- **缝14**：自动开卡日限 `card_replenishment_daily_limit=10` << 67 张/天(200单)。A4 日限必须大幅调高,现值是旧架构的。
- **缝15（跨阶段）**：Browser 本机 1 lane,高峰至少要 2 lane,多 lane 受本机限、真扩要迁云 → **200单/天的 Browser 并发卡在 V2.2**(与 D-224 发现 A 同源)。

### 4.2 逆向大摸排（失败/并发/崩溃/红线，2026-09-15 真读代码，慢查）

> 起因：Lemon 质疑我上一轮"对抗审查"只 grep 3 处就下结论、不可信。改为一类一类真读代码。

**第一类「扣钱没拿到货 · 开卡」(真读 highvcc-card-service.js:195-324)**
- **纠正我上轮说法**：不是"没处理"。`HIGHVCC_OPEN_NO_PAN` 处理很完善——错误明说"钱扣了/卡开了/别重开/用 reconcile 补记",`recordExistingCard` 补记从不再花钱,`pan_hmac` 唯一防重复。手动开卡撞上它很周到。
- **真缝A（A4 具体、会重复扣钱）**：这套补记是**手动的**(operator 跑脚本)。A4 自动开卡撞 NO_PAN 若简单重试 `openCard` → 卡未入库无 pan_hmac 挡不住 → **再开一张再扣一次钱**。A4 必须遇 NO_PAN **自动转 recordExistingCard 补记**,不是重试开卡。→ 补进 A4。

**第二类「付款崩溃/结果不明的对账」(真读 browser-execution-repository.js:940-1009)**
- **无缝,底座扎实**：付款提交是 COMMITTED+唯一 operation_id;崩溃→`RECONCILE_ONLY`(只对账不再动)+`SUBMIT_UNKNOWN`+`funds_risk=UNKNOWN`;三处 `affectedRows!==1` 抛 CONFLICT 防并发;幂等重放。CLAUDE.md 红线"点击后崩溃先对账不重点"**真落实了**。→ 纠正我上轮"红线没验/大缝"的污蔑。

**第三类「A1 统一后零税红线」(真读 zzshu-recharge.js:71-145)**
- **真缝B（红线不对称）**：Browser 事前拦税(`POLICY_REFUSAL_PATTERN`);**API 侧无零税校验**——`buildDirectOrderRequest` 只发卡号/CVV/token/planType,ZZSHU 黑盒付款,我们**事后**才拿到 `payment_amount`。→ A1 接口设计不能假设两路线一样:**API 侧零税结构上做不到"事前拒付",只能事后核对**。
- **Lemon 裁决(2026-09-15)**：ZZSHU 零税信得过,**API 侧不加事后核对兜底**。红线缺口→Lemon 确认可接受。但 A1 接口文档要写明"零税:Browser 事前拦/API 托付 ZZSHU",不能糊成"统一事前拦"。

**第四类·点1「A4 自动开卡撞并发」(真读 workflow-repository.js:325-360)**
- **底座对**：开卡**解耦**——订单缺卡只标 `WAITING_FOR_CARD`(幂等)、**订单绝不直接开卡**,调度器统一开(带 live rule/余额/日限检查)。注释直接警告"别在订单流程开卡,会绕过检查+并发重建失败任务"。天然防并发多开。
- **真缝C（给 A4 指明路）**：A4 给 highvcc 开卡**挂进现有调度器层**(订单标记→调度器统一开),不做"订单直接开卡"(否则并发多开/绕日限)。**顺带白得缝8预检钱包、缝14日限保护**。→ A4 别从零建,挂现有调度器。

**第四类·点2「A5 待销 vs 卡状态机」(真读 order-cancellation-service.js:355-400)**
- **底座**：现有卡停用只有 `RELEASED`→按余额变 `AVAILABLE`/`DEPLETED`(钱不够),并发 CONFLICT 保护。
- **真缝D（A5 口径要放宽）**：该销的卡不只"用满次数"的,**还包括 `DEPLETED` 的**(钱不够停用但仍占着/仍会被点升级拒付/卡上还剩钱要回收)。A5 待销口径应是"**不再服务新单(用满 OR DEPLETED)+ 到可销时间**",只看次数会漏掉一批 DEPLETED 卡。

**第四类·点4「B1 连败熔断 vs SUBMIT_UNKNOWN」(真读 workflow-handlers.js:104/496/587)**
- **底座**：现有只有 `task.max_attempts`(单订单内重试上限),**无 CDK 级连败熔断**(管不到"同一 CDK 整体连败几次")。B1 连败熔断是新建(印证缝13)。
- **真缝E**：熔断要基于"**同一 CDK 失败次数**"(现有 task 级看不到 CDK 级);和 SUBMIT_UNKNOWN 不冲突(订单级锁 vs CDK级停,层级不同),但**计数时 SUBMIT_UNKNOWN 不算"失败"**(它是"不明"可能成功,算进会误熔断)。

### 4.3 逆向大摸排总结（6 处真读代码，2026-09-15）

**方法纠正**：上一轮"对抗审查"3 个 grep 就下"四硬伤"结论,被 Lemon 质疑不可信→改真读代码,一类查透一类。

**核心结论——资金安全底座扎实,我上轮"四硬伤"是污蔑**：
- 付款不重复(COMMITTED+唯一 operation_id)、崩溃锁死(RECONCILE_ONLY)、并发防护(affectedRows/FOR UPDATE SKIP LOCKED)、开卡解耦(订单标记→调度器统一开,带日限/余额检查)——**都是真功夫,经得起查**。

**真缝全在一个规律:新建/统一的东西(A4/A5/A1/B1)接现有底座时的衔接**:
- 缝A：A4 撞 NO_PAN 要自动转补记(否则重复扣钱)
- 缝B：A1 零税两路线不对称(Browser事前拦/API托付ZZSHU,Lemon确认可接受)
- 缝C：A4 挂现有调度器(别订单直接开卡,顺带白得预检/日限)
- 缝D：A5 待销口径含 DEPLETED 卡(非只用满次数)
- 缝E：B1 熔断基于 CDK 失败数,SUBMIT_UNKNOWN 不算失败
- **规律**：底座不用动,A4/A5 落实时"复用底座的模式"而非"另起炉灶",就避开这些缝。



**方向没被推翻**——六面方案没有致命缝,但有一批要补。按处置分:
- **坐实/精化方案落点(方案更实)**：缝1(A3补开卡金额对齐,order-intake:45)·缝2(A6门槛同源)·缝6(A5销卡从零建整条链,工作量大)·缝7(A5接"用满"非"释放")
- **要补进方案的新缝**：缝3(A4缓冲按高峰并发)·缝5(C1四态给REVIEWING归属)·缝8(A4开卡前预检钱包)·缝9(G1含回笼记录)·缝10(A4盯整个资金池非钱包单数)·缝11(看板加"打扰N次")·缝12(A5"已删"靠事后同步)·缝13(连败熔断新建+停后客户态对齐)·缝14(A4日限调高)
- **跨阶段约束**：缝15(200单Browser并发卡V2.2)
- **已修正**：缝4(我的假对立,每卡几单与并发正交都要)

**集中点**：补的缝多落在 **A4(资金/开卡)** 与 **A5(销卡)** ——这两项是全案最复杂、最该多花心思的,大摸排把它们的隐藏工作量挖出来了。



## 5. 第一性减法审查（逐件事:砍废壳/补真洞，2026-09-15）

> 方法(D-233)：不猜"废壳嫌疑犯",而是**走一遍这件事真实链路**,走过=必需、没走到=死肉;每步问"删了会死吗"。

**第1件「收」**：客户侧仅 4 端点(验CDK/下单/查状态/换Session),**全必需、无废壳**。→ **A6(卡可用性前置提示)砍**:不做不会死(缺卡时下单卡"处理中"即可),连带缝2不用管。

**第2件「充」**：走执行器主链路 10 步(validate-card→填卡→填地址→填邮箱→**等零税**→终检→提交→人机验证门→看结果→confirmPlus+取消续费),**每步删了都充不成/破红线/丢交付=全必需,主链路精瘦砍不动**。臃肿不在主链路,在**周边**:AB赛道(champion/fallback/cohort)、artifact录制、多套config、上号器A/B——**主链路一步都不调它们=死肉,可清**(PoC期实验留的)。

**第3件「卡」**：端点 25 个＝臃肿重灾。本质＝"搞到一张卡"长出**五套并行机制**。Lemon 定卡来源(2026-09-15,含两处纠正):
- **highvcc**(喂 Browser)：**无可依赖 API,全手动**(后台开卡/网页删卡,Lemon 反复确认)。**V2 不做 highvcc 自动开卡**(做不了)。供卡:主力 highvcc 手动够用(产能100/天>200单需的67/天、可申请扩,D-235),矛盾已解、非死结(A4)。
- **hnskj**(喂 API 路线)：**`PURCHASE_CARD`(workflow-handlers:116,订单驱动+幂等键)是活的自动开卡能力,provider='hnskj'**。**作故障补充、不作依赖(D-235)**——hnskj 卡 Browser 能用但卡台常服务器故障不可靠,不押规模化供卡在它身上;能力保留、可 highvcc 不可用时补位,不必优先跑通。⚠️**上轮我曾把它误砍**(混淆"旧 card_stock_jobs 废实现"与"PURCHASE_CARD 能力"),已纠正保留。
- **手动导入**：兜底,留。
- **砍(废实现,非能力)**：`card_stock_jobs`旧每分钟架构(workflow-handlers 不碰它)、card-intake 录入、card-funding 补余额(D-218弃)。砍前照 C3 查全引用。
- **教训**：减法差点误砍 API 自动供卡能力——**砍"废实现"时别把"能力"一起砍**,是 Lemon 抓出来的。

### 5.1 废机制砍除清单（V2 减法定稿，2026-09-15）

> 规律：六件必需事本身都不臃肿(收4端点/充10步/钱不乱全必需/人兜底就几个)。臃肿全来自历史废机制,每套配了表+端点+前端堆在各件事周围。**砍废机制、六件必需事一个不动、零风险。**
> **铁律(D-233)**：删了会死吗?不会才砍。**砍前照 C3 规矩:当场重查行数+全项目查引用,防误伤(尤其别把"能力"当"废实现"砍——见第3件教训)。**

> **⚠️ 自查订正（2026-09-15，批A/B 生产核实）**：下表原"删了会死吗=不会"多为**账面推断,没做本节铁律要求的"当场重查行数+查引用"**。真相：多张表 getOverview/其他代码在读,**不是裸删就没事,删前有前置改动**。已按生产实测订正。

| 废机制 | 表(实测行数) | 删了会死吗（自查订正） | 依据 |
|---|---|---|---|
| 补余额 card-funding | `card_funding_attempts`(6行) | 数据废,但 **getOverview 读它**→删前改 overview | D-218+自查A3 |
| 旧每分钟开卡 card_stock_jobs | `card_stock_jobs`(**969行**) | timer停(对),但 **overview:680/cancellation:123/readiness:54 三处读**→删前改3处,非裸删 | 自查A3 |
| card-intake 录入 | `card_discoveries`(**5732行**) | **活链路,非死肉**:server:85创建/worker/admin-read读(manual-import不碰它)→删要拆整条 card-intake | 自查A3/A11 |
| 对账案例 reconciliation-cases | `reconciliation_cases`(1行) | 数据废,但 **overview 读**→删前改 overview | D-231+自查A3 |
| 履约成本投影 | (前端) | 不会,看板已有"花多少钱" | 第6件 |
| 0行真死表(可裸删) | order_tags/notes/compensations/checkout_artifacts/browser_artifact_secrets | 会安全,真0行无引用 | 自查A2 |
| refund_cases | `refund_cases`(0行) | 0行但 **overview 读**→删前改 overview | 自查A3 |
| browser_interventions | `browser_interventions`(**1行**非0) | 早废,行数订正(原标0行错) | 自查A2 |
| 周边PoC实验 | AB赛道champion/fallback/cohort、artifact录制、多余config | 不会,主链路10步一步不调 | 第2件 |
| A6 卡可用性前置提示 | (无表,新加的) | 不会,缺卡时下单卡"处理中"即可 | 第1件 |

> **删表铁律再钉（自查证实）**：getOverview 是读 **20+ 表**的超级巨物(自查B4;含 `card_sync_jobs` 账本先前没提的表)——**任何删表都先查全 getOverview + 所有读该表的面,改完再删**;C2/C3 工作量按此上调。

**保留不动(核心,砍表时绝不碰)**：payment_permits/funds_risk/browser_operations(钱不乱)、cdks/orders/cards/card_transactions(四件事主数据)、执行器主链路10步、hnskj PURCHASE_CARD(自动开卡能力,故障补充 D-235)、highvcc 后台手动开卡(无API,半自动)、手动导入。

**V2 六面方案 vs 减法**：六面里"对账面"已砍成顺便推、A6该砍;其余(执行接口统一/按产品/销卡/看板/通知分类)对照减法**是补真洞或去臃肿,不是加废物**——但**执行面接口三段统一**要用"不做会死吗"再过一遍(它是架构整理,不是业务必需,可能过度)。

## 6. 落实进度与验收证据（阶段 3）

**落实顺序（A组已排，B/C 接后）**：G1 → G2 → A3 → A6 → A2 → A5 → A4 → A1 → B1 → B3 → B4 → C1 → C2 → C3。强耦合串行、弱耦合可并行。

> **⚠️ 阶段3硬门槛（2026-09-15 整体自查后立，D-236）**：每个面动手前，**先跑该面的"读透+行数+引用"核查**（像 2026-09-15 供卡核查/整体自查那样：真读对应代码、当场重查表行数、grep 全项目引用），**不凭本账本 B/C 测绘直接改**。已知测绘不准点：§5.1 删表清单(删前置)、§2.4 告警7处、§2.5 getOverview 读20+表(含 card_sync_jobs)。

**每面 DoD 通用**：针对性测试(真实响应非夹具) + `state-check`/`wrapup-check` 一致 + 生产只读复验。

**新窗口接手指引**：读 `AGENTS.md` 四份顺序 + 本账本；落实依据＝§2(测绘)/§2.8(供卡)/§3(方案)/§4(缝)/§5(减法)；落实进度每面在下方登记。

---

_（每个面：状态 / 提交 / 测试与只读复验证据 —— 落实时逐面填）_

---

### 第②步「数据源三件」（面四②）· 2026-09-17 · **代码已上生产，T3 待 Lemon 确认 dry-run**

**发布**：release `20260917-datasources-c1a026a`（提交 c1a026a，2026-09-17 15:28:44 UTC 切换）。无数据库迁移。发布前自动备份 `pojia-20260917T152820Z.sql.gz.enc`（`backup_integrity=OK`）。回滚点 `20260916-unified-4334dc2`。
**边界守住**：`git diff --stat 4334dc2..HEAD -- browser-mvp/` **为空**——browser-mvp 一个字没碰（D-254）。

| 件 | 提交 | 状态 |
|---|---|---|
| T1 highvcc 流水 + 钱包入库 | `4f5e158` | 代码已上生产；timer 每小时自然写入 |
| T2 开卡真实成本记账 | `c1a026a` | 代码已上生产；**真实开卡时才执行，尚无真实样本**（第③步两台各开一张时验） |
| T3 账本补记脚本 | `50384e2` | ✅ **已 apply（Lemon 同意后）**：7 单补记、Dqcn 按证据规则拒绝。账本 CONSUMED 12→19 |

**测试**：v1 全量 **771 项 0 失败**（新增 30 项：T1 的 11 + T2 的 19）。真实响应夹具，非自造形状。

**对真实响应/生产的核实（不是照文档写的）**：
- highvcc 授权流水真实字段形状（2026-09-17 15:0x UTC，22 笔）：`status` 实有 **COMPLETE(19) / PENDING(3)** 两种（provider 注释原只记 COMPLETE）；`merchantCountry` 可为 `null`；`tags` 实为 `null` 不是数组；`unit='USD'`；`merchantCurrency` 实见 PHP 与 USD。
- 生产库时区 = UTC（`NOW()` = `UTC_TIMESTAMP()`），highvcc epoch ms 直读不平移这条成立。
- hnskj 的 `trade_time_raw` 是**卡台给的 UTC+8 本地串**（与 highvcc 的 ISO UTC 不同）：两个独立样本——开卡流水 `2026-09-17 09:34:34` vs `cards.created_at 01:34:35 UTC`；购买流水 `2026-08-27 15:51:03` vs 该单 07:51 UTC 付款成功。**是推断，非卡台文档确认。**
- 开卡成本两个干净样本：开卡 $16 → 106.06→89.48（成本 $0.58）；补值 $50 → 121.77→71.52（0.5%）。
- `persistCardTransactions` 那段会改账本的副作用（RECONCILIATION→RELEASED）：生产 RECONCILIATION 仅 2 行，都是 **hnskj** 卡（4643、1013），T1 只对 103 跑，碰不到。
- MANUAL_IMPORT 卡的分配资格走 `sync_tier` 分支、**不看** `last_transaction_synced_at`（对生产资格 SQL 实测），T1 写入不改变谁可分配。

**决策**：D-255（T2 选 B 观察余额差）、D-256（补记只补有流水证据的单，8→7）。

**范围外发现（只报未改，修不修 Lemon 定）**：
1. **`card-consumption-audit.js:33` 是假阴性**：只认 `LOWER(status)='success'`，而生产 purchase 的 status 有 `success`/`SUCCESS`/`SETTLED`/`failed` 四种，highvcc 还会带 `COMPLETE`。1652、5980 两张卡有真实扣款却被算成 0 笔。**任务书验收「补记后 hnskj 差异归零」因此只对 6807 成立。** → Lemon 定：排到第⑤步对账那块一起做。
2. **无主扣款 3 笔**：5371 的 09-13 16:01:49 $15.75、5501 的 09-06 05:51 $15.79、0601 的 09-08 11:18 $15.75，都没有对应的成功订单。
3. **反向缺口**：5501 的 RCbA 单账本记 CONSUMED，但卡台该时段无对应流水。
4. **`close-manually-fulfilled-order.mjs:42-44` 拒绝给 RECHARGE_FAILED 单记 `--card-used`** —— 5 单停在 RELEASED 的直接原因。修法＝允许但强制要流水证据（与 T3 脚本同一规则）。
5. **`card_stock_jobs` 的 `REVIEW_REQUIRED` 实为 965 条**，不是本账本 §3.A 面二③写的 930（那是 2026-09-15 的数）。已写进第③步任务书。
6. **两张卡台表当场实查确实矛盾**：`fulfillment_routes.card_provider_account_id` 四条全 101；`browser_card_source_selections` 三产品全 103。实际在跑的是 103。→ 第③步 C1 的合并前提，已写进任务书。
7. **卡的三种叫法在文档里混用**：`last4`（如 1657）/ `provider_card_id`（hnskj 如 5276）/ `external_card_id` 后四位（highvcc 如 29bb）。同一张 hnskj 卡「5276」的真实 last4 是 `0577`。落盘时写明用的是哪一种。

**T1 生产实证（2026-09-17 15:52:25 UTC，exit 0）**：那一趟快照走了 `NO_CHANGE` 短路，**流水与钱包照样写入** —— 坐实了「必须挂在 runner 层而非 service 内部」这个判断（按任务书字面挂进去的话这趟什么都不会写）。独立查库：103 的 `card_transactions` 22 笔 / 9 张卡 / 0 笔归不到；`provider_balance_snapshots` 1 条 $23.68。逐卡分布与手工比对的 22 笔吻合。

**T3 apply 与复核（D-256 补记）**：7 单成功、Dqcn 拒绝。新连接核实：CONSUMED 12→19、7 单各有 `consumed_at`+`provider_transaction_id`+1 条审计行。FqFn 的人工复核用订单自身事件时间做的独立验证（07:50:31 提交 → 流水 07:51:03 → 07:51:10 确认成功），**hnskj 的 UTC+8 至此被证实、不再是推断**。补记后可分配卡仍是 2 张，没减少。

**集成测试补跑（D-258）**：发布时漏跑，事后补。当前版 vs 基线 `4334dc2` 对照：两边同为 43/28 pass/**14 fail**，**当前版独有失败 0 条**；覆盖本块改动的那条单独跑通过。那 14 个是**既有失败**（串行跑相同），根因 `Order route cannot assign a card` 未查，已立项。

**范围外发现追加**（接上面 7 条）：
8. **对账判据不是「放宽白名单」能修的**（D-257，推翻我自己先前的建议）：实测宽判据只能让 14 张里 7 张归零，其余差异来自符号不统一（有笔 `amount=-15.97`）、PENDING 也是真实扣款、无主流水（系统外人工用卡）、反向缺口。归第⑤步认真设计。
9. **集成测试发布门槛过不了**（D-258），已立项。
10. `v1/README.md:44` 说 12 个集成用例会跳过，实际 66 个。

**下一块**：第③步任务书 `docs/tasks/2026-09-17-impl-step3-card-source-and-supply.md`。

---

### 第③步「卡台选择表 + 供卡调度」（面一 C1 + 面二①③）· 2026-09-18 · **A/B/C/D 完成；E 演练两次均停在付款前，报价段未验，等无试用资格的 free 号补验（D-264）**

**发布**：release `20260918-step3-supply-ba28273`（提交 ba28273，2026-09-18 00:11:53 UTC 切换）。**迁移 053**（只加不删）。备份 `pojia-20260918T000214Z.sql.gz.enc`（OK）。回滚点 `20260917-datasources-c1a026a`（新表/新列闲置无害，可直接退）。
**边界守住**：`git diff 4c5d95e..ba28273 -- browser-mvp/` 只有 `production-live-pool-worker.js:215` 与 `production-live-worker.js:283` 各一行（去掉 `manual_excel` 名字分支）；付款前三件一个字没碰；`shared-encrypted-materials` 实查校验是按 id 相等，无名字分支，**没改**。

| 块 | 做了什么 | 证据 |
|---|---|---|
| A 选择表 | `card_source_selections`（产品×执行器→卡台，六行；API 三行固定 101 且 `locked=1`）成唯一真相；intake 冻结卡台只读它；`resolveCurrentCardProviderAccount` 删除，8 个 hnskj 调用方改为按适配器码/能力位解析（不再依赖路线表旧列）；切路线/切卡台前四项校验（ROUTE_UNIQUE / SOURCE_HEALTHY / TARGET_POOL_AVAILABLE / VERSION_MATCH）不过即拒并返回逐项原因；后台前端切换带 `expectedVersion` / `expectedCurrentMethod`；旧表 `browser_card_source_selections` 与路线表列原样保留、代码不再读 | 单测：三产品×两执行器取表、四项各拒一次、锁定行拒切、切路线不动卡台（断言无任何对选择表/旧列的写）；生产六行与旧表逐行一致（见 D-260 前贴给 Lemon 的表） |
| B 供卡调度 | `card_supply_policies`（台×产品：水位/金额/日限/卡段，D-247 数）；调度器每轮按台×产品算「max(水位, 等卡单) − 可用 − 在途」，日限（按台、上海日、所有来源）、钱包预检（余额 − 金额 − 手续费 ≥ 底线；手续费用 `CARD_ISSUE_FEE` 最近 5 条中位数，无观察用 10%+$1 上限）、故障双向转台（仅 Browser 需求，转向后开出的卡按 `safeWaitingPredicate` 接管等待单；API 不转）；三线合一：runner 按 `open_adapter` 分派 hnskj/highvcc，T2 记账两台通用；highvcc `openCardForSupply`（NO_PAN 自动补记）；worker `PURCHASE_CARD`/`VERIFY_CARD` 及仓库 5 个方法删；`CARD_STOCK_LOW` 改由调度器按台×产品唯一产生（旧两处删） | 调度器单测 12 条（水位/日限/钱包/转台/API 不转/故障重试窗口/禁开/总闸/Pro 兜底/整数分算术）；适配器单测（hnskj 用真实响应夹具、highvcc 用真实钱包形状）；highvcc 供卡入口 5 条；`grep` v1/src 无 PURCHASE_CARD/VERIFY_CARD/commitPurchasedCard |
| B 口径修正（D-259） | 水位与切换校验改用 `stockCountingCardSql`（资格规则去掉 15 分钟新鲜度那一句） | 00:19:42 UTC 生产实测同一时刻：旧口径 hnskj 0 / 库存口径 1 |
| C 归档（D-260） | 965 条 → `ARCHIVED_LEGACY`，不删行 | 新连接：ARCHIVED_LEGACY 965 / REVIEW_REQUIRED 0 / 调度器阻塞检查 0 |
| D 真开卡（D-262） | hnskj 5622（$50，卡段 23），走人工 job + 手动跑一次执行器 | 钱包 89.48 → 38.73；`LOCAL_ISSUE_FEE_5622` = $0.75（T2 第一个真实样本）；highvcc 那张 Lemon 定等充值 ≥ $76 + 贴 token 再开 |
| C 启用调度（D-263） | timer `enable --now`（00:50 UTC，60s）；`set-supply-scheduler-flag.mjs on --apply` 只开总闸（补余额开关未动，审计一行） | 首轮总闸关：DISABLED 只刷告警；开后那轮 `WALLET_BELOW_FLOOR`（highvcc 23.65 − 50 − 6 < 20）不开卡、推 2 条告警、0 job、两台故障态 OK；hnskj 2/2 不缺 |
| E rehearsal（D-264） | 切 BROWSER（四项校验生产首次实证）→ 停池 → 关开关 → 建单 → `run-live-rehearsal.sh once` ×2 → 收口 ×2 → 作废 CDK → 切回 API → 开回 → 常驻池自动拉起 | 第一次 `RefreshAccessTokenError`（复用了 C2 那份 Session）；第二次过身份探测/页面签名/卡料预检，进结账找不到升级按钮 = 免费试用 offer 页（Lemon 看窗口确认，同 D-216）。**报价段未验**；证明的是 browser-mvp 到卡料预检没被波及。两单 CLOSED、3336 回池、CDK 退回后作废、非终态 0 |

**测试**：v1 全量 **809 项 743 通过 0 失败**（新增 38 项）；browser-mvp **303 项 294 通过 0 失败**。数据库集成测试：本机临时 MySQL 干净库 53 个迁移串行跑，当前版 16 失败 vs 基线 4c5d95e 同环境 20 失败，**当前版独有失败 0 条**；服务器隔离库 `pojia-integration-test.sh` 799 项 779 通过 19 失败，名单与本机基线逐条对上（少的那条是被重写并通过的旧「自动补卡」用例）。那 19 条是 D-258 立项的既有问题。

**与任务书不同、当场重查后改的**（都先报了 Lemon）：
1. 两张卡台表**不矛盾**：intake 本来就是 API 读路线表列、Browser 读 bcs，各管一半；路线表 BROWSER 行的 101 是被 `resolveCurrentCardProviderAccount` 撑着的（Plus 走 Browser 时 8 个 hnskj runner 全靠它启动），所以只能「新表成真相、旧列不再被读」，不能清旧列。
2. 965 vs 930：全部残留 965，卡住调度器的 930，分母不同。
3. `shared-encrypted-materials` 无名字分支，不用改。
4. browser-mvp 两处按 `sync_tier` 而非账户能力位（D-261，白名单限制）。
5. 缝 e「开卡后立即 discover」对 stock-job 路径不适用：`openStockCards` 直接 `register` 入库，不经 intake。
6. 103 `read_enabled=0` 是「没有只读 API」不是「被停用」：健康判定只对有 API 同步能力的账户要求 read_enabled，否则四项校验永远拒切到 103。

**范围外发现（只报未改，修不修 Lemon 定）**：
1. `operator-watch.mjs` 的全局 `CARD_STOCK_EMPTY`（`card-stock:no-eligible`）与新的按台×产品 `CARD_STOCK_LOW` 重叠 → 归第⑤块。
2. 后台「库存阈值」控件（`card_stock_low_threshold` / `setThreshold`）已不驱动任何告警；`card_replenishment_daily_limit` 全局日限被策略表按台日限取代但设置服务与总览仍读它 → 归第⑥块设置页。
3. 新开的 hnskj 卡 `sync_tier` 落库是 `AVAILABLE` 而不是 `register()` INSERT 的 `INVENTORY`（5276、5622 均如此），被谁改写未知、待查。
4. `card-source-mysql-integration` 等集成用例仍写 `browser_card_source_selections` 已改为写新表；但 `manual-card-import.test.js` 仍断言迁移 048 含旧表名（历史迁移，不动）。
5. `browser-execution-repository.js:216` 与 browser-mvp `mysql-upstream-adapter.js:120` 仍 SELECT 路线表旧列 `card_provider_account_id`（只投影、不校验；校验用的是 frozen），第⑧步删列前要先改这两处。
6. `CARD_STOCK_LOW` 现在会推手机（白名单是第⑤块）：D 那次执行器跑完两台各一条 OPEN，highvcc 那条会一直开着直到充钱开卡。
7. highvcc 钱包 D-247 提到的「$20 押金」含义未确认（`usdDeposit` 字段），预检只看 `usdBalance`。
8. **`close-rehearsal-order.mjs` 前提过时 + 后台无「付款前失败、放弃并放卡」动作**（D-264）：本次已扩脚本一处（`ac4f8ab`）；后台按钮要不要加，归第⑥块工作台/队列。
9. **D-239 诊断事件只在「点击前填写失败」落**，进结账前找不到按钮（`CHECKOUT_NAVIGATION_FAILED`）不落，只有一句 reason → 归第⑤块。
10. **`setOrderAcceptance`（开/关接单）不写 `admin_setting_events`**：E 期间两次开关审计表里没有行 → 归第⑥块（设置页每项带审计的 DoD）。
11. **D-216 待作废的免费试用码 4 天没作废**（本次已作废）；「检测到免费试用资格怎么办」D-216 三个方向 Lemon 至今未定，本次又撞上一次。
12. supervisor 的「残留 worker」判据 `pgrep -f production-live-pool-worker` 会被任何命令行含这串的瞬时进程（如运维手动 pgrep）误触发一轮，自愈但日志噪音 → 记着，不立项。

**下一块**：第④步任务书 `docs/tasks/2026-09-18-impl-step4-on-demand-sync-retire-contracts.md`。

### 第④步「按需同步 + 待销清单 + 三张契约表」（面二⑨⑩ + 面三②③④）· 2026-09-18 · **已完成**（release `20260918-step4-251a441` 06:26:47 UTC 切换；四批 apply 已写；rehearsal 报价段跑通；常驻池已换新代码。D-267/D-269/D-270）

**先做五项前置核查（任务书要求），全部贴了查询与原始输出，与任务书/CURRENT_STATE 不同的当场改**：
1. 同步节奏：`scheduleDueCardSyncJobs` 条件实为 `next_sync_at <= now`（不是 `now-60min`），每小时一次的真因是 dedupe 桶 `scheduled-card-sync:<card>:<60min桶>` 撞唯一键；生产 24h `card_sync_jobs` 40 COMPLETED（5276 整点各一、5622 00:27 起）+ **14 REVIEW_REQUIRED 全是 highvcc 卡被排进 hnskj 同步**（`Hnskj API error: 请检查填写是否完整`，全时段 114 条）。合格窗口 = 同步后 15 分钟。
2. 分卡函数：`assignAvailableCard` 找不到卡 → `refreshableInventoryCardSql` 挑一张 → INSERT `card_sync_jobs` priority 10 → handler 5s 重试 → runner 15s 领。
3. 两批旧卡：hnskj 12 张 FAILED/REFUND_WATCH/$0（5 张已有 RETIRED override，7 张没有，每 24h 仍被排同步一次）；highvcc HELD_FOR_REVIEW **8 张**（尾号/账面/最后在快照/首次缺席见 D-268 第 3 条；3 张已有 override：2911/7428/5501）。
4. `CANCELLATION_REVIEW_REQUIRED` 产生点两处（`commitCancellationStatus(exhausted)`、后台 RESOLVE CHARGED 未勾续费）；历史 1 次（09-08 API 单 7EYS）；当前 0；7 单 RECHARGE_SUCCESS 带 `cancellation_review_required=1`（老 flag）。`markAttemptUnknown` 不排任务；API SUBMIT_UNKNOWN 历史 0（14 次全 Browser）；`RECHECK_CANCELLATION` max_attempts=60。
5. `sync_tier` 落库 `AVAILABLE` 的改写者是 `completeCardSyncJob`（`initialSyncTier(inventory_status)`，首次同步在开卡后 15 秒内），INVENTORY 只是 register 初值/legacy 别名——**设计如此，不是 bug，已查清**。`browser-execution-repository.js:216` 仍投影旧列（第⑧步删列前改）。

| 块 | 做了什么 | 证据 |
|---|---|---|
| A 按需同步 | `findStaleInventoryCandidate`（只读）+ handler 当场同步（`card-on-demand-sync-service`，与 runner 共用 `syncHnskjCardReadOnly`，runner 内联实现已删）+ 分卡里排 job 段**已删**；AVAILABLE 档 10min→3h；scheduler 排除 MANUAL_IMPORT / RETIRED | 单测：handler 3 条（当场同步→分配；同步失败→不分不开卡 60s；无候选不调 provider）、仓库 2 条、同步服务 3 条（hnskj 真实响应夹具）、scheduler 排除 1 条；集成：`mysql-integration` 分卡用例改写（候选→不排 job→模拟同步后同一规则放行）；**生产只读实证 05:17:01 UTC**：两张 hnskj 卡 16 分钟未同步，正式资格口径可分配 **0**（等卡现场），新候选 SQL 选中 **5276** |
| B 待销清单 | `card-retirement-service`（派生查询；口径含 FAILED——生产实跑口径时发现 7 张作废卡没 override 不在清单，当场补）+ 两端点 + 迁移 054（一个设置键）+ 快照缺席不覆盖 RETIRED + `retire-legacy-cards.mjs` | 单测 7 条（用满/DEPLETED/存活期/取消未确认/活动分配/Pro/FAILED + confirm 三处写/拒/幂等）；生产只读实跑口径 SQL：28 行候选原始行（见对话贴出）；**dry-run**：hnskj-voided 12 张全对上、highvcc 8 张全列（apply 等 Lemon） |
| C 表一 | `commitCancellationStatus(exhausted)` + RESOLVE CHARGED 未勾续费 → `RECHARGE_SUCCESS` + flag + `ORDER_CANCELLATION_UNCONFIRMED` | 仓库单测 + handlers 既有 3 条 + `browser-resolve-unknown-payment-mysql-integration` 断言改（真实 MySQL）；`grep CANCELLATION_REVIEW_REQUIRED v1/src` 只剩枚举/投影/历史单收口 |
| C 表二 API | `markAttemptUnknown` 排有界 POLL（30×60s）；`reconcileUnknownSubmission` 两路证据（hnskj `trade_time_raw` 按 UTC+8）；`escalateUnknownSubmission`；后台 `resolve-unknown-submission` | handlers 六种情形（真实流水行形状）、evidence 3 条、仓库 escalate、resolve 3 条、attempt repo 排任务 |
| C 表二 Browser | v1 `recoverExpiredRun` 三路进补核（deadline 30min）；`escalatePaymentVerification` 带 `evidenceSummary`；白名单 `browser-card-transaction-reader.js` 支持 `ledgerSource`（未注入保留旧 marker）、`live-post-payment-recovery.js` UNKNOWN 带两路证据 | 恢复仓库 3 条；browser-mvp reader 3 条 + recovery 1 条 |
| C 表三 | 12 个人工点唯一清单 + 缝 j 核对 | `docs/contracts/2026-09-18_human-intervention-points-contract.md` |

**边界守住**：第一轮 `git diff --stat -- browser-mvp/` 只有 `browser-card-transaction-reader.js`、`live-post-payment-recovery.js` 两文件（白名单内）；`recovery.js` 未动（实查无调用者，死代码——报）；付款前三件一个字没碰。**Lemon 批 D-268 ① 后第二轮**（D-269）：再改两个 worker 的工厂各三行（白名单外，Lemon 批）+ reader 删假 marker；browser-mvp 308/299/0、v1 842/776/0、`npm run check` 过。

**测试**：v1 全量 **842 项 776 通过 0 失败 66 跳过**（基线 810/744/0，+32）；browser-mvp **307 项 298 通过 0 失败**（基线 303/294/0，+4）。生产 schema：`customer-sql-probe` 全过；`sql-probe` 602 条 0 失败（81 条含插值跳过——其中本块新写的 11 条关键 UPDATE/INSERT/SELECT 另行 PREPARE 全过）。本机临时 MySQL（迁移 001–054）集成：842 项 828 通过 13 失败 1 跳过（基线 810/794/15/1）；**相对基线新增失败 0 条**，13 条全是 D-258 立项的既有失败；被改名重写的旧「排 job」用例现通过。另：`Bark notification claims…` 用例在脏库上基线代码同样失败（同库先后各跑两次均失败），是残留/时序型既有 flaky，与本块无关，干净库全量下通过。

**与任务书不同、当场重查后改的**：
1. 崩溃进补核的落点是 v1 `browser-recovery-repository.recoverExpiredRun`（RECONCILE_ONLY 分支只改 status、补核领不到），不是白名单里的 `browser-mvp/src/recovery.js`（无调用者）。
2. 待销口径加 FAILED（见 B）。
3. 「删假 marker」只能做到「注入即真证据、未注入保留旧行为」——工厂在白名单外（D-268 第 1 条）。
4. `sync_tier=AVAILABLE` 不是 bug（前置核查 5）。
5. ZZSHU 零原因失败「停单不退码」未做（与队列一起，归第⑤/⑥块）。

**范围外发现（只报未改，修不修 Lemon 定）**：
1. scheduler dedupe 桶 60 分钟 → ASSIGNED / RECHARGE_PROCESSING 档实际也每小时一次 → 归第⑤块（对账面）。
2. browser reader 对 hnskj 卡的 `withinIntentWindow` 直接 `Date.parse` UTC+8 串，服务器 UTC 下偏 8 小时（Browser 单几乎都走 highvcc，未暴露）→ 归第⑦块或 Lemon 单独批（白名单内可修）。
3. `browser-mvp/src/recovery.js` 死代码 → 第⑧步清理。
4. CURRENT_STATE 原「条件 next_sync_at <= now-60min」写错，已按代码改。
5. `card_sync_jobs` 全时段 REVIEW_REQUIRED 307 条（hnskj 旧卡 193 + highvcc 114）是历史残留，本块只堵产生源（scheduler 排除），未归档 → 要不要像 965 条那样归档，Lemon 定。
6. 7 单老的 `cancellation_review_required=1` 只有 3159 一张卡还在库里可关联（其余卡已不在或未 assigned）→ 待销清单只会因此列出 3159，其他老单的卡不会再被提醒。
7. **资金：highvcc 3336 余额 $145 → $2.46**（02:52 UTC 快照），一笔 $142.54 / 8919.64 PHP `OPENAI *CHATGPT SUBSCR`。**Lemon 答：是他手动用这张卡给客户付的一笔 20X，没经过 Browser 也没经过 API**（D-269）。该卡已按「手动付 20X」标 RETIRED。**副作用**：这类手动付款系统完全不知道——账本用量仍 0、待销清单不会因「用满」列出它、第⑤块的日对账会把它报成无主扣款。要不要给「运营手动用卡」留一个登记入口，归第⑤/⑥块。
8. **`plausiblePlusAmount` 只认 Plus 价位**（USD 14~22 / PHP 900~1200）：Pro 单的扣款一律判成不匹配 → 将来 Pro 走 Browser 时补核永远定不了、每单叫人。归第⑦块（Pro 同型），改成按订单套餐取价位区间。3336 那笔 142.54 正好是现成反例（只读验证第 3 例）。
9. **建演练单没有正式脚本**：这次我用底层 `storeCdkBatch` 造码漏了 `cdk_batches` 行（D-270 ②）。要不要把「建演练单」做成带批次的正式脚本，归第⑥块（工作台/运维工具）。

**Lemon 已定（D-269）并执行（2026-09-18 06:2x UTC）**：工厂已注入（提交 345f0fa）；**发布 `20260918-step4-251a441` 06:26:47 UTC 切换**（054 已应用，新 SSH 独立核实 web/worker PID 与 cwd，`/health/*` 200，3 分钟 journal 无 error）；**四批 apply 全部写入并新连接核实**：hnskj 12 张 RETIRED、highvcc 8 张 + 3336 RETIRED（`card_state_events CARD_RETIRED_CONFIRMED` 21 条、overrides RETIRED 35）、`card_sync_jobs` 307 条 ARCHIVED_LEGACY（REVIEW_REQUIRED 0）。`run-live-pool.sh` 已加 `BROWSER_PAYMENT_VERIFICATION_WINDOW_MS=1800000`（常驻池重启后生效）。**apply 的连带**：3336 标 RETIRED 后 highvcc 可分配 0 → 调度器（Lemon 刚充值，钱包 23.45 → 142.49）06:27 自动开 **4022**（$50，highvcc 自动开卡首次生产实跑，T2 观察扣 50.50）；06:28 第二张 job cdd7d90d `HIGHVCC_RECONCILE_NOT_READY` → REVIEW_REQUIRED、103 FAULT、调度器停开。只读核对：卡台已开出（`HG3c6ea2…` 尾号 8718，$50，详情已完整），钱包 41.49 = 钱已扣、库里无此卡 → **补记（`reconcile-highvcc-card.mjs`）等 Lemon 一句**。**rehearsal 已做（D-270，06:5x～07:0x UTC）：报价段首次跑通**（`PRE_SUBMIT_STOPPED`，PHP 982.14 / 税 0.00，付款提交 0 次），补上了 D-264 的遗留；演练单 `PJV1-8DB4vPHrXotbcNBJHETH` 已收口（卡回池、非终态 0）；切回 API 时四项校验按库存口径放行（分卡口径当时 0 张），是 D-259 在切换校验上的生产实证；常驻池 07:06:55 由 supervisor 拉起 PID 67131，**已带 30 分钟核实窗口**。演练顺手坐实三个缺口（收口脚本守卫认不出新形态→已扩、我建 CDK 漏批次行→加 `--skip-cdk-return`、supervisor 残留判据被我的 pgrep 误触发）。**备用卡台真证据路径演练走不到**，另做了生产真实流水的只读验证（1657 窗口内匹配 / 窗口外不匹配 / 3336 的 Pro 价位不匹配，三例全对）。

**下一块**：第⑤步任务书 `docs/tasks/2026-09-18-impl-step5-notification-whitelist-daily-reconciliation.md`。

---

### 第⑤步「推送白名单 + 日对账」（面四①③）· 2026-09-18 · **代码完成、测试全绿；等 Lemon 批发布 + 装 timer**（D-271/D-272）

**边界守住**：`git diff --stat -- browser-mvp/` **为空**——browser-mvp 一个字没碰（D-254：②⑤⑥⑧不碰）。未删表、未改付款行为、未动一卡多单上限值。

#### A 先坐实静音根因（任务书第一要求）

**D-176 的静音代码没错，是 `pojia-bark-notifications` 这个常驻进程不在发布重启名单里，五天一直跑旧 release。** 完整证据链见 D-271，要点：

| 查什么 | 原始输出 |
|---|---|
| bark 进程 | `MainPID=2324`，`ActiveEnterTimestamp=Wed 2026-09-16 23:36:28 UTC`（= 服务器 boot，不是任何一次发布） |
| 它跑的代码 | `readlink -f /proc/2324/cwd` = `/opt/pojia/releases/20260916-unified-4334dc2/v1`，而 current = `20260918-step4-251a441`——**落后四个版本** |
| 发布脚本 | `deploy-release.sh:86/91` 只 restart `pojia-web` + `pojia-worker`，没有 bark |
| 闭环反证 | `HANDOFF_LOG.md:2367` 记 09-16 10:46 UTC current = `20260913-orderno-6dcb458`，服务器实测该 release **有** `PHONE_SILENT_TYPES`；而 `alert_notifications` 里 `BROWSER_PAYMENT_UNKNOWN` id=438347 `created_at=2026-09-16 11:09:35.571 / sent_at=11:09:36.261`——current 含静音的同时仍在插行并推 |
| 排除别的解释 | 全项目 grep：INSERT 只有 `alert-notification-repository.js:14` 一处；`information_schema.TRIGGERS` 对两张表无触发器；`created_at` 无 `ON UPDATE`；两个静音类型的**全部 20 行** `created_at` 最晚停在 09-16 11:10，都在 boot 之前 |
| 范围 | 生产常驻 service 只有三个（web/worker/bark），其余 8 个 pojia-* 都是 timer 拉起的 oneshot，每次新进程走 current，不受影响 |

**同一个洞 D-220（2026-09-14）修过一次，当时只修了 worker，没回头数一遍常驻服务。** 已给 `deploy-release.sh` switch 段加上 bark 重启，并打印 `bark cwd=`；ROLLBACK 行同步带上；RUNBOOK §2.7 与 §5 写明「发布后核对 worker cwd 与 bark cwd 两行」。

#### B 各块做了什么

| 块 | 做了什么 | 证据 |
|---|---|---|
| A 白名单 | `PHONE_SILENT_TYPES` 排除法 → `domain/alert-push-policy.js` 白名单四类（叫人 10 / 供给 4 / 资金 4 / 客户动态 1），不推的 9 种逐条写理由；`enqueueOpenAlerts` 的 **CANCELLED→PENDING 复活路径补上类型过滤**（以前没有，白名单外类型只要推过一次就能一直复活）；`claimNext` 也加白名单；`PROVIDER_BALANCE_CHANGED` 可切「每日汇总」（设置键 `provider_balance_change_push_mode`，默认 EACH） | 单测 10 条（四类各一推 / 其余不推 / 未知类型不推 / 两个 D-176 类型仍静默 / 两表不重叠 / DAILY_DIGEST 只摘余额变化 / 入队与复活两处都过滤 / claimNext 过滤 / 缝 d 按 sent_at 统计） |
| A 新产生点 | `PROVIDER_TOKEN_EXPIRED`（`sync-highvcc-snapshot.mjs` 失败分支，贴回 token 后下一趟成功即 RESOLVE）· `CARD_SUPPLY_FAULT`（挂进 `markSupplyFault` 本身，五个调用点自动带上，`clearSupplyFault` 时 RESOLVE）· `CARD_CHARGEBACK`（拒付必推） | 单测 8 条（故障产生/恢复/已是故障不重写、token 一段失效期只一行、拒付按类型判不看 status 文案、重复同步不重推、正常购买不误判） |
| A 缝 d | `countPushesByType({sinceUtc, untilUtc})` 按 `alert_notifications.sent_at` 统计「今天叫了几次」，给第⑥块看板 | 单测断言查的是 `sent_at` 而不是 operator_alerts |
| B 判据 | `domain/card-transaction-audit.js`：类型归一化、状态只认见过的三个成功词 + PENDING、未知状态归 `UNKNOWN_STATUS` 进报告、金额取绝对值、0 元授权不算、**不认商户名**；`card-consumption-audit.js` 改用同一份判据（旧的 `LOWER(status)='success'` 漏 hnskj `SETTLED` 3 笔 + highvcc `COMPLETE`/`PENDING` 23 笔） | 单测 6 条，每行形状抄自生产真实行 |
| B 日对账 | `daily-reconciliation-service.js`（次数 / 金额分开）+ `daily-reconciliation-runner.js`（心跳 `daily_reconciliation_heartbeat_at` + 每日一条汇总 + 只读）+ `pojia-daily-reconciliation.{service,timer}`（UTC 04:00，排在备份之后）+ 后台只读端点 `GET /api/v1/admin/reconciliation/daily` | 单测 14 条；**生产只读实跑两次**（见下） |
| B 连续两次 | 差异指纹存 `app_settings.daily_reconciliation_last_report`（不新建表，缝 c 精神）；`persistent:true` 才升 critical | 单测：指纹按「卡 + 两个结论」定 |
| C.4 押金 | `walletPreflight` 加 `heldBalance`：余额 − 押金 − 金额 − 手续费 ≥ 底线（D-272 ②）；hnskj 不报押金按 0，行为不变 | 单测 3 条（41.49 含 20 押金开 $50 拒开 / null 时与以前一致 / 边界值） |

**测试**：v1 全量 **885 项 819 通过 0 失败 66 跳过**（基线 842/776/0/66，**+43**）。browser-mvp 未动，不跑。
**生产 schema 校验**：`sql-probe.sh` **609 条 0 失败**（基线 602），跳过含插值的 81 条；本块新写的判据 SQL（`chargeRowSql` / `settledStatusSql` 插值进 `card-consumption-audit` 的那条）**另行拿到生产 PREPARE，通过**。

#### C 日对账生产只读实跑（本块的硬证据）

第一次（08:04:31 UTC）报 **11 条差异**。**其中 9 条是判据自己的问题，当场查清并修**：

- 7 条（1013/1284/1666/4643/5980/7705/9051）全是 hnskj 那 12 张作废旧卡：`funded 16.00 / balance 0.00 / 零笔扣款`。卡进终态后余额被清零或退回钱包，「开卡金额 − 扣款 = 余额」这条式子不成立 → **终态卡不做金额对账**。
- 2 条（1013/4643）是把账本的 `RECONCILIATION` 占位当成了已确认消费——那是「付款未知、资金锁着」的占位，不是消费 → **确认消费只数 `CONSUMED`**，占位单列 `AWAITING_RESOLUTION`。

第二次（08:05:16 UTC，修后）：

```
cardCount 30 / discrepancyCount 2 / persistentCount 0
pendingRegistrationCount 7 / fundingIncompleteCount 3 / retirementDueCount 4

--- 真差异 ---
1657 inv=AVAILABLE consumed=3 provider=3 funded=50.00 charged=47.20 expected=2.80 balance=1.80 delta=-1.00
3159 inv=AVAILABLE consumed=3 provider=3 funded=50.00 charged=47.25 expected=2.75 balance=1.05 delta=-1.70
--- 待登记（卡台扣了、系统没记）---
8590 c=0 p=1 OPENAI          0237 c=0 p=1 ANTHROPIC* CLAUDE SUB     0601 c=0 p=2 OPENAI×2
3336 c=0 p=1 OPENAI(手动20X)  5371 c=2 p=3 OPENAI                   5501 c=2 p=3 OPENAI×2      7402 c=1 p=2 OPENAI
```

**`card-consumption-audit` 修判据后对生产实跑**（同一时段，只读）：`cardCount 30 / discrepancyCount 7 / backfillReview 7 / ledgerReview 0`，7 张与日对账的「待登记」**完全一致**（8590 / 7402 / 5501 / 0601 / 5371 / 0237 / 3336）。修之前被旧判据算成 0 笔扣款的 hnskj 卡（1652、5980、6807、6185）**不再出现在差异里**——D-256 发现 1 的假阴性到此了结。

**逐条解释**（验收要求）：待登记 7 张里 5 张有既有解释——3336 = Lemon 手动付的 20X（D-269）；5371/5501/0601 各多 1 笔 = D-256 发现 2 的三笔无主扣款；7402 = Dqcn 单证据不足未补记（D-256）。**没有解释的是 4 条**：0237 的 `ANTHROPIC* CLAUDE SUB` 100.00、8590 的那笔 15.97，以及 **1657 / 3159 的金额差 -1.00 / -1.70**。这四条按规矩只写「差多少、原因未知、待查」，见下面发现 7/8。**所以「差异只剩已知项」这条验收只达成了一半，如实记。**

#### D 前置核查（任务书列的五项，全部当场重查，数字与任务书里 05:xx 的不同）

1. 静音根因 —— 见 A。
2. 第④步四个新告警类型：`ORDER_CANCELLATION_UNCONFIRMED` / `ORDER_PAYMENT_UNKNOWN_REVIEW` 生产 **0 行**（还没产生过）。
3. `card-consumption-audit` 判据与账本分布：CONSUMED 19 / RELEASED **50** / RECONCILIATION 2。
4. `operator-watch` 的 `CARD_STOCK_EMPTY`：门槛写死 Plus 16、用分卡口径（含 15 分钟新鲜度），最近一次 2026-09-12 12:42 已 RESOLVED。与按台×产品的 `CARD_STOCK_LOW` 重叠 → **归白名单外**（第③步发现 1 的处置）。
5. 第④步两件现场一致：本机常驻池 PID **67131** 在跑、心跳 07:53:23 UTC 新鲜、`run-live-pool.sh:48` 带 `1800000`。**备用卡台那一路仍未被真单付款自动调用过**（无新真单）。

**告警产生点实查 10 处**（§2.4 测绘写的 7 处不准）：`browser-alert-repository`(9 型) · `card-transaction-repository` · `workflow-repository` ×3 · `unknown-submission-resolve-service` · `browser-admin-service` · `card-provider-snapshot-service` · `card-supply-scheduler-service` · `card-stock-job-service` · `operator-watch.mjs` · `card-catalog-sync.js`。全量 19 种类型，逐一在白名单或理由表里有归属。

#### E 范围外发现（只报未改，修不修 Lemon 定）

1. **bark 服务的启动日志丢过**（**本条已更正，原因不是我先前写的那个**）：发布前 `journalctl -u pojia-bark-notifications` 整个 boot 内一条日志都没有，我当时写成「Node 非 TTY 下 stdout 块缓冲」——**发布把这个解释推翻了**：旧进程 2324 退出时打出了 `Bark notification runner stopped`（08:29:04），新进程 1202626 启动时**立刻**打出了 `Bark notification runner started`（08:29:05），同一份代码同一个 `console.log`。**事实**：旧进程（服务器 boot 时 23:36:28 起来的那个）的启动日志从未进 journal，退出日志进了；新进程的启动日志立刻进了。journal 无 rate-limit 记录。**原因未确定**（疑与 boot 早期 journald 尚未就绪有关，未证实）。现在日志是通的，所以这条从「服务是哑的」降级为「boot 时启动的那一次丢了日志，原因未查」。
2. **`ORDER_WAITING_FOR_CARD` 两处产生点共用同一 dedupe_key** `order-waiting-card:${orderId}`（`workflow-repository:480` critical「开不出卡」vs `card-stock-job-service:354` warning「会继续尝试」），同一行被两处互相覆盖 severity。白名单按类型收，两种语义一起推 → 要不要拆成两个类型，Lemon 定。
3. **D-239 的 `CHECKOUT_NAVIGATION_FAILED` 诊断落点在 browser-mvp**（`executor.js:426` / `browser-order-preflight.js`），**本块碰不了**（不碰 browser-mvp）→ 任务书 C.2 做不了，归第⑦块。
4. **第④步发现 6（browser reader 的 hnskj UTC+8 解析）已确认在 `browser-mvp/src/` 内**，本块只报不改 → 归第⑦块。
5. **任务书 C.5 的答案**：`SYNC_CARD_TRANSACTIONS` 不是节奏线——它是**按订单排的任务**（`admin-read-service:540` 手动排 / `transaction-sync-smoke`），替代不了 scheduler 的档位。付款后核实走的是第④步接的按需 refresh，不依赖 scheduler，**所以 60 分钟 dedupe 桶不影响对账面**。要不要把 `ASSIGNED`/`RECHARGE_PROCESSING` 的同步节奏提上来，是供卡面的改动，未改。
6. **`funded_amount` 对 highvcc 卡多数不可信**：5501 funded 2.00 / 扣款 159.16、7402 funded 49.00 / 158.62、0601 funded 3.27 / 143.01、0237 funded 0.00 / 100.00。**原因未知**。日对账已把这类标 `FUNDING_SOURCE_INCOMPLETE` 不算差异，但这意味着 highvcc 侧的金额对账目前只对新卡成立（3336 的 145 − 142.54 = 2.46 完全对上）。
7. **1657 / 3159 金额差 -1.00 / -1.70，原因未知**。两张都是 AVAILABLE 的 highvcc 活卡、次数对得上、流水齐。不猜原因。
8. **0237 卡上有一笔 `ANTHROPIC* CLAUDE SUB` 100.00（08-28）与本业务无关**；8590 卡有一笔 OPENAI 15.97 账本无对应。两笔都进「待登记」，原因未知。
9. **highvcc 卡实查 16 张，`CURRENT_STATE` 写 17 张**（AVAILABLE 8 / RETIRED 9），实为 AVAILABLE 7 / RETIRED 9。**差 1 张未查清**，本轮只报。
10. **0237、0601 两张 `inventory_status=AVAILABLE` 却带 `RETIRED` override**（早期手动标的，reason 在库里是 UTF-8 双重编码乱码「æ‰‹åŠ¨æµ‹è¯•å¡...」）。状态与 override 不一致，且乱码影响后台显示。
11. **`state-check.sh` 的 `pojia-web` 一行偶发取值失败**（07:5x 那次报「现场 状态 取不到值，按漂移处理」，而实际 web active、PID 1118884；08:2x 收尾再跑同一脚本该行通过）。**偶发、不是必现**，多半是并发查询下的取值竞态，未深查。
14. **本机有个跑了 7 天的测试 MySQL 容器 `pojia-stage1-mysql`**（`mysql:8.4`，127.0.0.1:54186，内含 12 个历史测试库 stage1/2/3/d240 等），而上一窗口的 `HANDOFF_NOW` 写的是「本机临时 MySQL 容器已删」。**`wrapup-check.sh` 的「本机没有遗留的调试服务」也没抓到它**。本块用它建了独立库做验证、验完只删自己建的那个；**容器不是我起的，没动**。要不要清理、要不要把判据加进 wrapup-check，Lemon 定。
13. **我自己加的那行 `bark cwd=` 第一次就打了假值**：`deploy-release.sh` 在 `systemctl restart` 之后立刻取 MainPID，而 bark 旧进程要 4 秒才退干净（08:29:00 停 → 08:29:04 stopped → 08:29:05 新进程起），那一刻 MainPID 还是 0，`readlink -f /proc//cwd` 打印 `/`——看起来像「没换成功」。**这一行存在的意义就是核对代码有没有真的换掉，打假值比不打更坏**，已改成等到有真 PID 再取（有界 15 秒）。当次靠新连接独立核实确认 bark 真的换了（PID 1202626、cwd 新 release）。
12. **`CURRENT_STATE` 多处漂移**（已在本块更新）：OPEN 告警 119→**122**、账本 RELEASED 47→**50**、`CARD_STOCK_LOW` 从「OPEN 2」→ 4 条全 RESOLVED、可分配卡 4→**2**（hnskj 两张出了 15 分钟窗口）、`HANDOFF_NOW` 里常驻池 PID 写成 74272（实为 67131）。

#### F 执行段（Lemon 当次批准后做的，2026-09-18 08:2x～08:3x UTC）

**发布 `20260918-step5-740bc1d`**（08:29:00 UTC 切换）。无数据库迁移。备份 `pojia-20260918T082829Z.sql.gz.enc`（`backup_integrity=OK`）；manifest 1135 文件 OK。回滚点 = `20260918-step4-251a441`。

- **prepare 后复核**：新 release 里 `alert-push-policy.js` / `card-transaction-audit.js` / `daily-reconciliation-service.js` / runner 四个新文件都在；`alert-notification-repository.js` 三处 SQL 都是 `alert_type IN (?)`；与本机同文件 **SHA256 逐字节一致**。（复核时我先 grep 错了关键词——`PHONE_PUSH_TYPES` 是 `alert-push-policy.js` 里的常量名，repository 引的是小写函数 `phonePushTypes`，而唯一匹配到 `PHONE_SILENT_TYPES` 的是那句「旧的已删」注释。查清后才继续。）
- **三个常驻服务全部换到新 release**（新 SSH 连接独立核实）：web PID 1202546 / worker PID 1202551（均 08:29:00 起）/ **bark PID 1202626（08:29:05 起）**，三者 cwd 都是 `/opt/pojia/releases/20260918-step5-740bc1d/v1`，NRestarts=0。`/health/live` `/health/ready` 200，admin 登录页 200。**D-271 的洞到此闭合：bark 第一次跟着发布换了代码。**
- **装 `pojia-daily-reconciliation.timer`**（Lemon 批）：`enable --now`，`list-timers` 显示下次 `Sat 2026-09-19 04:01:19 UTC`，`is-enabled=enabled`。

**生产实跑与只读复验**：手动 `systemctl start` 一次 → `Result=success ExecMainStatus=0`。新连接独立核实：

| 验收项 | 生产证据 |
|---|---|
| timer 有心跳 | `app_settings.daily_reconciliation_heartbeat_at = 2026-09-18T08:29:39.596Z`；`daily_reconciliation_last_report` 已存指纹 |
| 每日一条汇总 | `DAILY_RECONCILIATION_SUMMARY` / `daily-reconciliation:2026-09-18` / **info**（persistentCount=0 所以没升 critical）/ 文案含「差异 2 张…待销到期 4 张」 |
| **新增通知行只来自白名单类型** | 08:29 之后 `alert_notifications` **只新增 1 行**：`DAILY_RECONCILIATION_SUMMARY`，`SENT`（08:29:42）。查白名单外的 6 种类型（`BROWSER_PAYMENT_UNKNOWN`/`BROWSER_PAYMENT_CONFIRMED`/`BROWSER_ORDER_COMPLETED`/`CARD_STOCK_EMPTY`/`REFUND_CANDIDATE`/`BROWSER_UPGRADE_HANDOFF`）在同一时段的新增行 → **0 条** |
| 不补推旧告警 | 上线前后都实查：OPEN 告警全部已有 BARK 通知行、无任何 `PENDING/RETRY/SENDING/DEAD` 待推行；实际新增就 1 行，印证 |

**这次生产复验的限度**：期间只产生了一个新告警，所以生产只证明了「白名单类型能推、白名单外没被新插入」。**下面这些生产验不了的，补在隔离库里跑完了。**

#### G 隔离库端到端验证（补验收「人为造差异能进看板/汇总推」，2026-09-18 08:5x UTC）

发布后发现**这半条验收本来漏了**：生产第一跑 `persistentCount=0`（没有上一次可比），所以**「连续两次差异才升 critical」整条判断从未被执行过**，只有单测。在本机既有测试 MySQL 里新建独立库 `pojia_step5_recon`（迁移 001–054 全量跑通，不动同实例里的历史测试库），造两张卡：

| 造的数据 | 期望 | 实跑结果 |
|---|---|---|
| 卡 9001：开卡 50、零扣款、余额却只剩 40，另有一笔 `card_recharge` 让它「有流水可对」 | 真差异 `AMOUNT_DIFF` | ✅ `delta -10.00`，进 `discrepancies` |
| 卡 9002：卡台扣 2 笔（COMPLETE + PENDING）、账本 0 | **不算差异**，进「待登记」 | ✅ `pendingRegistration`，`discrepancyCount` 不含它 |
| 第一跑 | `persistent=false`，汇总 **info** | ✅ `persistentCount 0`，告警 `info`「今日对账汇总」 |
| **第二跑（差异没修）** | `persistent=true`，汇总升 **critical** | ✅ `persistentCount 1`，同一 dedupe_key 的告警 `info → critical`，标题变「对账差异连续两天还在」，文案「差异 1 张（其中 1 张连续两天还在，要处理）」 |
| 汇总告警走白名单入队 | 进队列、能被领走 | ✅ `enqueueOpenAlerts` 后 `PENDING`，`claimNext` 领到 `DAILY_RECONCILIATION_SUMMARY / critical` |

同一个库里顺手补了**生产验不了的白名单两条**：

| 造的数据 | 实跑结果 |
|---|---|
| 一条 OPEN 的 `BROWSER_PAYMENT_UNKNOWN`（白名单外）+ 一条 `BROWSER_HUMAN_REQUIRED`（白名单内） | 入队行数 **0 / 1** —— 白名单外的没进队列 |
| **手工给白名单外那条塞一行 `PENDING`**，模拟白名单收窄之前遗留的队列 | `claimNext` 连领三次只领到 `BROWSER_HUMAN_REQUIRED`，**那条遗留 PENDING 一直没被领走** —— 这正是生产没有样本的那条 |

验完 `DROP DATABASE pojia_step5_recon`，同实例里其余 12 个历史测试库一个没动。

**下一块**：第⑥步任务书 `docs/tasks/2026-09-18-impl-step6-workbench-and-settings.md`。

